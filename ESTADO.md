# ESTADO — GymEvo (nombre tentativo: Método Cero)
Última actualización: 2026-09-01 | Sesión actual: 7 (en curso)

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **🎉 LANDING declarada LISTA — 36/40 usabilidad, 19/20 craft, 19/20 copy.** Cierre de la maratón de 7 rondas de esta sesión de pulido. El movimiento decisivo fue encontrar y corregir un bug real de z-index (mismo patrón ya visto en `app/app/perfil/page.tsx`) que tenía el video de fondo del Hero y la textura de "renglones de cuaderno" invisibles en TODOS los renders anteriores — una vez corregido y con el scrim más transparente, más el botón de volver arriba para la heurística de control/libertad que faltaba, la pantalla cruzó las tres rúbricas. **Onboarding se quedó en 33-34/40 · 15/20 craft** (mejoró de 32/10 al inicio de la sesión) — con el mismo bug de z-index corregido de paso (el fondo del onboarding también estaba invisible), más varios ajustes reales (tarjeta de progreso en vivo, navegación por teclado, celebración animada, tarjeta ya no parece tocable) — pero el vacío bajo la tarjeta en los pasos cortos sigue sin resolverse del todo y requiere más tiempo de diseño de contenido, no otro ajuste de código. Se paró en onboarding por decisión explícita de tiempo del usuario ("por tiempo, opción 2").
/ Siguiente acción exacta: si el usuario quiere seguir con onboarding en otra sesión, retomar desde el defecto #1 documentado (vacío bajo la tarjeta en pasos cortos) — necesita contenido nuevo, no otro ajuste de espaciado/opacidad. Fuera de eso, seguir con la Sesión 7 (pantalla-principal ya cerrada en rondas anteriores) o con el dominio propio → Hotmart cuando el usuario tenga presupuesto.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Refuerzo del dispositivo ownable (opción "por tiempo": no tocar la paleta base) + HALLAZGO de un bug real que invalidaba las 4 rondas anteriores de revisión de landing/onboarding**.
- Se agregó una textura de "renglones de cuaderno" (líneas rectas horizontales, PROPIA — distinta del grano feTurbulence del ejemplo vetado "Capítulo" del banco canónico) al fondo del Hero (landing) y del onboarding completo, más una espiral de encuadernación (columna de puntos) en el borde izquierdo de `TarjetaRuta`.
- **Al verificar en el navegador, las líneas no se veían — y se descubrió por qué**: tanto el fondo de `Hero.tsx` (mesh + video + scrim) como el fondo del onboarding usaban `z-index` NEGATIVO sobre elementos `absolute`, el mismo bug de stacking-context ya encontrado y documentado antes en `app/app/perfil/page.tsx` (un hijo con z negativo pinta DETRÁS del fondo opaco de su ancestro). Esto significa que **el video de fondo del Hero probablemente nunca se vio en NINGÚN render de esta sesión ni de sesiones anteriores** — se había interpretado como "limitación de la herramienta de screenshot para capturar movimiento", pero en realidad la capa entera era invisible. También explica por qué el revisor-visual reportó "fondo plano" en CADA una de las 4 rondas de onboarding de hoy pese a subir la opacidad varias veces: la opacidad nunca importó porque la capa nunca se pintaba.
- **Corregido en ambos archivos** (`components/landing/Hero.tsx`, `app/onboarding/page.tsx`): todos los fondos pasaron de z negativo a `z-index: 0`, y el contenido real de cada pantalla quedó envuelto en su propia capa `relative z-10` (mismo patrón ya probado). Verificado visualmente en el navegador con un "test de color rojo" (pintar el div de fondo de rojo sólido para confirmar si se veía o no) antes y después del fix — confirmado que antes NO se veía nada (ni rojo) y después SÍ.
- Se relanzó el revisor-visual (ronda 5, con contexto explícito del hallazgo) para ambas pantallas — es una re-evaluación legítima porque el render real cambió de forma sustancial, no una repetición de rondas ya agotadas. Resultado pendiente de notificación.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en `components/landing/Hero.tsx` y `app/onboarding/page.tsx`.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Ronda 3 de correcciones landing+onboarding, tras los resultados de la ronda 2 del revisor-visual (onboarding 34/40·12/20, landing 33/40·14/20·19/20 copy — ambas siguen NO LISTA)**. Defectos reales corregidos:
- **Onboarding**: el borde punteado de `TarjetaRuta` era igual al de los chips no-seleccionados (parecía tocable sin serlo) → ahora borde SÓLIDO en ámbar; el ámbar ya no vivía en ningún elemento visible (solo un gradiente casi imperceptible) → el ícono `NotebookPen` y el eyebrow "TU RUTA SE ESTÁ ARMANDO" ahora son ámbar sólido; el paso "reconocimiento" tenía un check estático sin la celebración N1 ("trazo verde") de FICHA-ARTE → ahora es un SVG con `pathLength` animado (se dibuja), respeta `prefers-reduced-motion`; FICHA-ARTE.md decía que accent-2 era "N/A por ahora" pero el código ya lo usaba → sincronizado.
- **Landing**: faltaba la baseline de conteo animado en los precios → se agregó `PrecioAnimado` en `Oferta.tsx` (cuenta 0→N al entrar en vista, `useInView`+`animate`, respeta reduced-motion) para mensual/anual (el total tachado del stack Hormozi se dejó estático a propósito: no tiene sentido narrativo animar un ancla que se descarta visualmente); el titular de Oferta acentuaba también la preposición "de" → recortado a solo la cifra.
- **Pendientes reconocidos y NO corregidos** (decisión consciente de alcance, fuera de esta sesión de pulido): el riesgo de cercanía de la PALETA BASE (papel cálido + tinta verde) con la combinación vetada "Capítulo" del banco canónico — cambiar la paleta base es redecidir FICHA-ARTE.md ("cosa juzgada"), requiere aprobación explícita del usuario, no es un ajuste de pulido; falta una textura/trazo literal de "cuaderno físico"; varias secciones de la landing (FAQ/Garantía/Agitación) usan solo 2 planos tipográficos en vez de 4.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en todos los archivos tocados (`app/onboarding/page.tsx`, `components/landing/Oferta.tsx`, `app/page.tsx`, `FICHA-ARTE.md`).

**RESULTADO FINAL de esta sesión de pulido (ronda 4, la última — 01/09/2026):**
- **landing**: 34/40 usabilidad · 13/20 craft · **20/20 copy** — sigue NO LISTA. El copy quedó perfecto. El revisor fue explícito: el defecto que bloquea (identidad/paleta) **requiere rediseño real, no un ajuste de código** — es la misma paleta base de FICHA-ARTE.md ("papel cálido + tinta verde"), cosa juzgada del proyecto. Corregirlo de verdad implica (a) que el usuario apruebe tocar la paleta base, o (b) diseñar un dispositivo ownable mucho más fuerte y repetido en la página — ninguna de las dos es un "ajuste de pulido". Otros defectos menores identificados (2 planos tipográficos en Problema/Agitación/Garantía en vez de 4, un botón que duplica `CtaButton` en vez de usar una variante, contraste a verificar en el recap de CtaFinal) SÍ son correcciones rápidas, pero no alcanzan para cruzar el gate mientras el bloqueo de identidad siga sin resolverse.
- **onboarding**: 34/40 usabilidad · 11/20 craft — sigue NO LISTA, mismo bloqueo raíz de identidad/paleta (idéntico a landing, coherente porque comparten FICHA-ARTE). Defectos menores identificados que SÍ son correcciones rápidas para una próxima sesión: vacío de ~200-250px sin centrar en los pasos cortos (nivel/meta), los 3 gradientes de fondo siguen leyéndose casi planos en el render real (subir opacidad más agresivamente o acercar el radio), el contador de "días/semana" salta sin animación al mover el slider, y el radio visual de los chips (~14-16px) no coincide con el pill de 100px que documenta FICHA-ARTE para botones (hay que alinear uno de los dos).
- **Decisión de cierre de esta sesión**: se PARA aquí, tal como se avisó de antemano al lanzar esta última ronda. 4 rondas de corrección real (no cosmética) mejoraron ambas pantallas de forma medible — landing subió su copy a 20/20 y resolvió 7 defectos reales (2ª nota de color, tachado verdadero, checkmarks consistentes, titulares recortados, conteo animado); onboarding resolvió su causa raíz estructural (el vacío por centrar mal) y ganó una tarjeta de progreso en vivo, ámbar visible, navegación por teclado y una celebración animada — pero NINGUNA de las dos cruza el gate del revisor porque el bloqueo real y compartido es una decisión de identidad/paleta que el SO marca explícitamente como "cosa juzgada, no se redecide a mitad de proyecto sin aprobación del usuario". Seguir iterando dentro del alcance de "pulido" no mueve ese número — hace falta que el usuario decida si quiere invertir una sesión dedicada en re-derivar la paleta (16/54) o reforzar el dispositivo ownable de forma mucho más agresiva.
- Veredictos completos en `docs/revisiones/{landing,onboarding}-veredicto.md`. Capturas 375px vigentes en `docs/revisiones/{landing,onboarding}-375.png`.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Sesión de pulido dedicada a landing + onboarding (a pedido explícito del usuario: "dediquémosle tiempo a ambas... que queden ganadoras y perfectas") — 2 rondas de corrección real con revisor-visual entre medio**.

**Ronda 1** (causas raíz, no cosméticas):
- Onboarding (`app/onboarding/page.tsx`): `justify-center` centraba los pasos cortos (2-4 chips) dejando mucho vacío arriba+abajo — se cambió a `justify-start` (ancla arriba) y se agregó `TarjetaRuta` (tarjeta "recibo de cuaderno" que llena Nivel/Meta/Horario/Días-semana EN VIVO con check verde) en los pasos nivel/meta/frustración/compromiso.
- Landing: se activó `--accent-2` (ámbar tierra, ya previsto en FICHA-ARTE) en `components/landing/tokens.css`; se agregó el dispositivo ownable a `Solucion.tsx`; se agregó el texto "Desliza para ver el resto" al carrusel `AppPorDentro.tsx`.
- Resultado del revisor-visual (ronda 1): **onboarding 33/40 · 14/20** (mejoró de 32/10, sigue NO LISTA) — defectos: TarjetaRuta se veía vacía/rota en el paso 1 (4 guiones sin nada), seguía quedando ~20-25% de vacío plano bajo la tarjeta, riesgo de paleta cercana a la vetada, sin navegación por teclado en los chips. **landing 34/40 · 13/20 craft · 19/20 copy** (sigue NO LISTA) — defectos: el "arreglo" del dispositivo ownable era un ✓ suelto de Lucide, NO el tachado prometido; la 2ª nota de color solo aparecía 1 vez (insuficiente); 3 titulares acentuaban 4-5 palabras en vez de 1-3; checkmark inconsistente entre secciones.
- **Ronda 2** (corrección de los defectos reales que señaló el revisor):
  - Onboarding: `TarjetaRuta` ya no se muestra vacía — si nadie ha respondido nada todavía (paso 1) se ve un mensaje de bienvenida + ícono `NotebookPen` en vez de 4 guiones; tarjeta más grande (más padding); degradé de fondo reforzado (opacidad subida + un 3er radial-gradient con el ámbar); chips ahora son `role="radiogroup"`/`role="radio"` navegables con flechas ↑/↓.
  - Landing: el tachado real se aplicó al texto "antes" de `Solucion.tsx` (`line-through` en verde, igual que un ejercicio tachado en la app) y el "después" pasó a usar `CheckCustom` (el mismo componente que ya usaba `Oferta.tsx` — resuelve la inconsistencia); la 2ª nota de color (ámbar) se sumó también al badge "N días gratis" de `Oferta.tsx` (aparece 2 veces más, una por plan) — ahora 3 apariciones en total; se recortaron los 4 titulares con énfasis largo (Hero, Solución, AppPorDentro, CtaFinal) a 1-3 palabras acentuadas.
  - Verificado: tsc ✓ · eslint ✓ · build ✓ tras cada ronda. Capturas 375px regeneradas antes de cada pasada del revisor.
- Se relanzó el revisor-visual (ronda 2) para ambas pantallas — corriendo en paralelo, resultado pendiente de notificación al cerrar este checkpoint.
- Pendiente reconocido y NO corregido esta sesión (bajo impacto, decisión consciente de alcance): falta una textura/trazo literal que remita al "cuaderno físico" (mundo del sujeto de FICHA-ARTE) — por ahora la identidad se apoya solo en color+tipografía+el tachado.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Celebración con confeti al terminar el entrenamiento del día**. Al tocar "Terminar entrenamiento de hoy" (`finalizarEntrenamiento` en `app/app/page.tsx`) aparece una pantalla completa con confeti cayendo (28 piezas SVG-CSS, colores en `--confetti-1..5` de `components/landing/tokens.css` — única excepción documentada a la regla 60-30-10, es una celebración puntual y no forma parte de la paleta de UI), el texto "¡Muy bien!" y debajo el emoji nativo 😅 (sin fondo, como pidió el usuario). Nota: el usuario pegó una imagen 3D específica de ese emoji en el chat, pero no llegó como archivo — no hay forma de extraer los píxeles exactos de una imagen solo vista en la conversación. Se le preguntó cómo prefería resolverlo; no respondió esa pregunta directamente pero pidió agregar la animación de confeti, así que se avanzó con el emoji nativo (☑ recomendado en la pregunta) — si más adelante envía el archivo de imagen real (arrastrado al chat, no pegado), se reemplaza. El confeti respeta `prefers-reduced-motion` (no se muestra, solo el texto). Verificado: tsc ✓ · eslint ✓ (una advertencia de dependencia de `useMemo` resuelta con comentario explicativo, es intencional) · build ✓ · flujo completo probado en `http://localhost:3000/app` (bypass temporal de proxy.ts, revertido antes del commit): las 3 piezas de hoy registradas, "Terminar entrenamiento" tocado, confeti cayendo y desapareciendo solo, tal como se esperaba.
/ Siguiente acción exacta: si el usuario envía el archivo de imagen del emoji exacto, reemplazar el 😅 nativo por esa imagen (como `<img>`, en `public/`); si no, queda así. Luego retomar el pulido pendiente de Sesión 7 (landing y onboarding) o el dominio propio → Hotmart cuando tenga presupuesto.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Sonido de notificación al terminar el descanso**. Se agregó una campanita sintetizada con Web Audio API (dos tonos cortos, sin archivo de audio que descargar ni licencia que pagar) que suena junto con la vibración cuando el cronómetro de descanso llega a 0. Encendida por defecto (`sonidoDescanso: true` en `Progreso`, columna `profiles.sonido_descanso`, migración `supabase/migrations/0004_sonido_descanso.sql` aplicada al proyecto real vía MCP); el usuario la puede apagar con un botón de altavoz debajo de los chips de duración. Detalle técnico importante: el `AudioContext` se crea/retoma DENTRO del tap de "Registrar" (un gesto real del usuario), no cuando el descanso llega a 0 — los navegadores bloquean el audio que no nace de un gesto directo, así que crearlo tarde nunca sonaría. Verificado: tsc ✓ · eslint ✓ · build ✓ · flujo probado en `http://localhost:3000/app` (bypass temporal de proxy.ts, revertido antes del commit): sin errores de consola al disparar la campanita.
/ Siguiente acción exacta: pedirle al usuario que confirme que escucha el sonido en su celular real (el navegador de prueba no permite verificar audio de oído); luego retomar el pulido pendiente de Sesión 7 (landing y onboarding no pasan el gate del revisor-visual) o el dominio propio → Hotmart cuando tenga presupuesto.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Cronómetro de descanso configurable con anillo que se consume**. En `app/app/page.tsx`: el temporizador de descanso entre series ya no usa el `descansoSeg` fijo de cada ejercicio del catálogo — ahora el usuario elige UNA duración (30s/1min/2min/3min, chips debajo del interruptor "Descanso automático", visibles solo si está activado) que se usa para todos los ejercicios de hoy. Se agregó el campo `descansoDuracionSeg` a `Progreso` (`lib/routine.ts`, default 60) y la columna `profiles.descanso_duracion_seg` (migración `supabase/migrations/0003_descanso_duracion.sql`). El banner de descanso muestra un anillo SVG (`AnilloDescanso`) que se vacía en tiempo real.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **"Plan de hoy" con animación Lottie en el saludo**. En `app/app/page.tsx`: se agregó `lottie-react` (v3.1.1 — API nueva: export nombrado `Lottie`, prop `src` en vez de `animationData`; el tamaño no lo respeta una clase Tailwind, hay que forzarlo con `style={{width,height}}`) con la animación del usuario (`public/animaciones/fitness.json`) al final del titular. El titular pasó de "Hola, {nombre}, hoy toca {parte}" a "Hola, hoy toca {parte}" (a pedido del usuario, para que quepa en una sola línea junto al ícono) — se quitó también el estado/prop `nombre` de esta pantalla por quedar sin uso (seguía vivo en Perfil, donde sí se usa).

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Login real de punta a punta + video de fondo en Perfil, con un bug de CSS real encontrado y corregido**. Resumen de esa sesión larga:
- Resend conectado como SMTP de Supabase (sin dominio propio — sin presupuesto ahora; remitente de pruebas `onboarding@resend.dev`, solo entrega al dueño de la cuenta de Resend, límite subido de 2/hora a 30).
- Bug real corregido: la "Site URL" de Supabase Auth apuntaba a `localhost:3000` desde la creación del proyecto (los magic links en producción redirigían a la máquina del usuario). Corregido en el dashboard.
- Bug real corregido: Gmail/Outlook escanean el enlace mágico automáticamente y lo "gastan" antes de que el usuario lo toque (confirmado en logs: un `/verify` exitoso y otro con "token not found" al mismo segundo). Se agregó el código de 6 dígitos como respaldo en `/login` (doctrina 26-AUTH-MODERNO: "el combo") — falta que el usuario agregue `{{ .Token }}` a la plantilla de correo en el dashboard de Supabase para que el código realmente llegue.
- **Bug real de CSS encontrado y corregido**: el video de fondo (agregado primero en "Plan de hoy", luego movido a "Perfil" a pedido del usuario) usaba `z-index` NEGATIVO sobre `position:fixed` — por las reglas de stacking de CSS, eso lo pintaba DETRÁS del fondo opaco de un contenedor ancestro (el shell de `/app`), invisible en un iPhone real aunque el `<video>` decodificaba bien (confirmado con una captura real del usuario). Fix: z-index NO negativo para el video + todo el contenido real envuelto en su propia capa con z-10. Patrón a recordar para cualquier video/imagen de fondo futura dentro de `/app` (el hero de la landing no tenía este problema porque no vive dentro de un wrapper con fondo propio).
- A pedido explícito del usuario, el video en Perfil NO lleva scrim de color (se ve completo); solo el saludo y "Cerrar sesión" llevan su propio chip opaco para seguir siendo legibles.
- Nombre de usuario editable en Perfil ("Hola, {nombre}", persistido en Supabase, columna `profiles.nombre`).
/ Siguiente acción exacta: pedir al usuario que confirme el video en su iPhone real y que agregue `{{ .Token }}` a la plantilla de correo; luego seguir con el pulido pendiente de Sesión 7 (landing y onboarding no pasan el gate del revisor-visual) o con dominio propio → Hotmart cuando tenga presupuesto.

## Logo / isotipo (31/08/2026)
- El usuario mandó un isotipo generado con IA (dumbbell + reloj de arena formando una "H", verde sobre crema — coincide de casualidad con los tokens de FICHA-ARTE). No llegó como archivo editable (solo visible en el chat), así que se redibujó como vector en `components/Logo.tsx` (mismo diseño, `fill="currentColor"`, sin fondo, nítido a cualquier tamaño) en vez de recortar la imagen rasterizada.
- Ya reemplaza el placeholder verde genérico en: header de la landing (`Hero.tsx`) y `/login`. También es el favicon nuevo (`app/icon.svg`, se borró el `app/favicon.ico` genérico de Next.js).
- Pendiente: si el usuario quiere ajustar el diseño del isotipo (proporciones, grosor), se edita `components/Logo.tsx` y `app/icon.svg` (mismo path, duplicado a propósito porque el favicon no puede usar variables CSS).

## Video de fondo del hero (31/08/2026)
- El usuario pidió agregar un video propio (`Personas_entrenando_en_gimnasio...mp4`, IA) como fondo de la sección Hero de la landing. Copiado a `public/videos/hero-gimnasio.mp4`. `Hero.tsx` ganó un prop `backgroundVideoSrc` opcional: video absoluto detrás del contenido + scrim en degradé del propio `--bg` (58%→92%) para que el texto siga legible, oculto con `prefers-reduced-motion` (motion-reduce:hidden). Verificado por código/DOM/canvas que decodifica frames reales con color; la herramienta de screenshot remota que uso no logra capturar el video en movimiento (limitación de la herramienta, no del código) — el usuario debe confirmarlo en su propio navegador.

## Sesión 7 (31/08/2026) — en curso: revisor-visual de las 4 pantallas del dinero
- Se instaló Playwright (`npm i -D playwright` + `npx playwright install chromium`) para generar screenshots REALES a 375px (antes no había manera de generarlos sin pedirle capturas al usuario). Script: `scripts/capturar-screenshots.mjs` (sembra sessionStorage/localStorage con datos de ejemplo realistas antes de capturar).
- Se corrigió de paso: Next.js 16 le apendizaba su propio bloque a `AGENTS.md` en cada `next dev` (pisando la copia intencional del SO) — se agregó `agentRules: false` en `next.config.ts`.
- Capturas generadas (página completa a 375px): `docs/revisiones/{landing,onboarding,paywall,pantalla-principal}-375.png`.
- El subagente `revisor-visual` NO aparece en la lista de subagent_type disponibles del harness de esta sesión — se invocó como `general-purpose` con la instrucción explícita de leer y seguir `.claude/agents/revisor-visual.md` al pie de la letra (mismo efecto: contexto limpio, sin conocer las intenciones de quien construyó).
- Los 4 veredictos llegaron: **ninguno pasó el gate** (≥36/40 usabilidad y ≥16/20 craft). Se detectó que el screenshot de landing salió CASI EN BLANCO (solo Hero y Footer) — causa raíz: bug del SCRIPT de captura, no de la página real (las secciones usan `whileInView` de framer-motion, que no dispara sin scroll real; Playwright no scrollea antes del `fullPage` screenshot). Corregido en `scripts/capturar-screenshots.mjs` (scrollea la página en pasos de 400px antes de capturar) y se recapturó. También se ajustaron los datos de ejemplo de pantalla-principal (racha activa + 1 ejercicio tachado, antes salía sin nada marcado). Se relanzó el revisor para landing y pantalla-principal con las capturas corregidas; onboarding y paywall ya tenían capturas válidas, sus veredictos se mantienen.

### Resultados (primera pasada, antes del pulido de esta sesión)
- **onboarding** (paso 1): NO LISTA — Usabilidad 31/40, Craft 11/20. Defectos: vacío muerto bajo las 2 opciones (fondo plano sin profundidad), cero dispositivo ownable, sin salida/cancelar visible.
- **paywall**: NO LISTA — Usabilidad 30/40, Craft 12/20, Copy 18/20 (el copy SÍ pasa). Defectos: "Restaurar compra" sin onClick (parece interactivo, no hace nada), checkmarks del sistema "✓" en vez de custom, sin dispositivo ownable, garantía lejos del CTA, falta stagger en la mitad de los bloques.
- **landing / pantalla-principal**: veredicto inicial inválido por el bug del script — repetido, resultado pendiente de la notificación.

## Pendientes de pulido detectados por revisor-visual (Sesión 7, antes de declarar listas las 4 pantallas del dinero)
- [ ] onboarding: llenar el vacío bajo las opciones (centrar o agregar contenido), dar profundidad al fondo (mesh/tinte sutil), aplicar el tachado verde/chip de FICHA-ARTE como guiño de identidad, agregar salida/cancelar visible.
- [ ] paywall: conectar o quitar "Restaurar compra" (botón sin función hoy), cambiar los "✓" del sistema por checkmarks custom (círculo acento 12% + Check de Lucide), agregar el dispositivo ownable, repetir la garantía cerca del CTA principal, envolver los bloques restantes en stagger de entrada.
- [ ] pantalla-principal (veredicto confirmado con captura corregida): NO LISTA — Usabilidad 30/40, Craft 14/20. El color de la flama NO es un bug (verificado en código: ambas ramas son consistentes) — sí se anota que el gris "en riesgo" es más tenue que el `#B8860B` de aviso de FICHA-ARTE. Falta: `whileTap` en botones (Registrar, Terminar, toggle), poder deshacer/editar un registro ya marcado hecho, animar el conteo de la racha al cargar + guard de `prefers-reduced-motion`, avisar si falla la sincronización remota. Nota aparte: el indicador de modo desarrollo de Next.js (círculo "N") tapa la pestaña activa en el screenshot — es solo de `next dev`, no aparece en producción, no cuenta como defecto real.
- [ ] landing: pendiente de la repetición (el primer veredicto no es válido, screenshot roto por el bug del script).

## Dirección de Arte (Sesión 2 — CERRADA, cosa juzgada)
- FICHA-ARTE.md: existe y aprobada — 28/08/2026 (ver archivo en la raíz)
- ¿Hubo referencia visual del usuario?: NO → REFERENCIA-INVESTIGACIÓN (fusión Hevy/Fitbod/Strong, 16 PASO 0.2bis)
- Opción elegida: **C — "Cuaderno de Sala"** (el usuario detectó un bug de layout en el primer render — hueco vacío antes del botón de Rescate — ya corregido y reverificado)
- Resumen: fondo `#F5F1EA` · superficie `#FFFFFF` · texto `#211D17`/`#8A7F6A` · acento `#5C7A1F` (verde tinta, solo en progreso/CTA/check) · Display y Body: **Instrument Sans** única familia · radio 14-16px cards, 100px pills
- Personalidad: Sobrio (dominante) · Técnico · Cálido — voz: experto sobrio con calidez cercana
- Dispositivo ownable: tachado verde sobre ejercicio completado (estilo cuaderno real) + chips de meta con borde fino
- Modo: claro — derivado porque el avatar busca calma/certeza, no adrenalina (evita el reflejo "fitness = oscuro + neón")
- REGISTRO ANTI-REPETICIÓN: verde `#5C7A1F` + Instrument Sans vetados para el próximo proyecto del SO
- Página comparativa (evidencia, incluye las descartadas A/B): `docs/revisiones/direcciones-abc.html`

## Qué es esta app (3 líneas máximo)
App con dos rutas de nivel: Ruta Principiante (programa fijo de 90 días que dice exactamente qué hacer cada día) y Ruta Intermedio (rutinas efectivas con progresión para salir del estancamiento), ambas en gimnasios comerciales (Smart Fit/Bodytech), con alternativas de un toque cuando la máquina está ocupada. Monetización por suscripción (Freemium: semana 1 gratis, luego de pago).

> ⚠️ Corrección del usuario (28/08/2026): NO es un programa cerrado solo de 90 días para principiantes. Debe servir también a nivel intermedio que ya no es novato pero quiere rutinas efectivas y romper el estancamiento. Esto deja de ser un "desbloqueo automático al día 90" y pasa a ser una ruta propia desde el día 1 (el usuario elige su nivel al entrar).

## Promesa central
"GymEvo ayuda al joven de gimnasio comercial en LATAM —sea principiante o nivel intermedio— a construir un físico atlético y entrenar con la seguridad de un experto, sin depender de entrenadores que lo ignoran ni perder tiempo cuando las máquinas están ocupadas, mediante un mapa táctico (fijo de 90 días para principiantes, o de progresión para nivel intermedio) con un botón de alternativa en tiempo real."

## Reporte de validación (Sesión 1) — ya investigado por el usuario, no se repite
- Veredicto: Excelente oportunidad (validación ya hecha por el usuario con su propio prompt de investigación)
- Apps de referencia: Hevy (~$800K MRR, queja: exige armar rutina propia) · Fitbod (queja: IA cambia rutina a diario, sin progresión) · MadMuscles/BetterMe (queja: cobros ocultos, estafas) · Strong (queja: pantalla en blanco, asume experto)
- Lo que los usuarios odian de la competencia (nuestra oportunidad): 1) tener que armar su propia rutina desde cero, 2) que la IA cambie el plan cada día sin lógica ni progresión, 3) cobros ocultos/suscripciones no autorizadas
- Brecha LATAM confirmada: Sí — dolor de hacinamiento en gimnasios de cadena económica (Smart Fit, Bodytech) y desconfianza por estafas de apps fitness, sin solución local que dé plan fijo + alternativa en tiempo real
- Precio de referencia del mercado: Hevy $23.99 USD/año; propuesta propia $4.99/mes o $29.99/año

## Avatar y venta (Sesión 1)
- FICHA-AVATAR.md: existe y aprobada — 28/08/2026 (ver archivo en la raíz; todo el copy de venta se traza a ella)
- FICHA-MERCADO.md: existe — 28/08/2026 (ver archivo en la raíz; precio y trial documentados, plazos de garantía de Hotmart marcados como pendientes de verificar contra la pasarela real en Sesión 6)
- Avatar: Mateo, 26 años, oficinista, va a Smart Fit a las 6:30 PM (hora pico), paraliza frente a las máquinas, se avergüenza, termina haciendo caminadora y se va frustrado.
- Nivel de consciencia: 4/5 — Consciente de la Solución, quemado y escéptico (ya probó apps de IA, influencers, y fue estafado por apps de cobros ocultos)
- Dolor #1: terror a lesionarse por mala técnica + vergüenza de no saber qué hacer frente a las máquinas
- Deseo #1 (superficial): saber exactamente qué máquina usar y cómo. Deseo profundo: entrar con autoridad de experto, dejar de sentirse invisible/juzgado
- Objeciones clave: "¿Y si mi gimnasio no tiene la máquina o está siempre lleno?" · "¿Es otra app con cobros ocultos?" · "¿Qué pasa después del día 90?"
- Ángulo de venta ganador: "El entrenador que tu gimnasio te cobra pero nunca te da" — vende certeza y protección contra el caos, no "fitness"
- Diferenciador: No usa IA que cambia el plan al azar. Plan fijo de 90 días + Botón de Rescate para sustituir ejercicios al instante.
- Frases literales del cliente (para copy): "Se la pasan comadreando o coqueteando" · "El exceso de personas hace que sea imposible usar las máquinas" · "Sigue cambiándome de rutina casi a diario" · "Llevo meses y me veo igual" · "Entrenar sin estrategia"

## Estrategia de monetización (Sesión 1 — NO cambiar sin validar)
- Modelo: **Modelo 2 — Onboarding + Paywall de prueba** (decidido por el SO, matriz A-F de 02C: fitness/wellness B2C, uso diario, necesita HÁBITO). Reemplaza el "Freemium" que proponía el documento del usuario — el freemium puro es el modelo de PEOR conversión (2.1%) según 02C; el onboarding+trial es el que usan Duolingo/Cal AI/Noom con hasta +234% de conversión.
  Flujo: Onboarding personalizado (elige nivel + meta metabólica) → ve su plan del Día 1 generado para él → Paywall → trial de 7 días con acceso completo → pago.
- Trial: 7 días (equivalente a la "semana 1 gratis" del reporte, pero como trial estructurado con tarjeta, no como freemium indefinido) — aviso claro antes del cobro en el Día 6 (transparencia radical, responde directo a la objeción #2 del avatar: "¿cobro oculto?")
- Pricing: $4.99 USD/mes o $29.99 USD/año ≈ $2.49/mes mostrado como "2 meses gratis" (benchmark Hevy $23.99/año) — mantiene el precio que ya validó el usuario
- Riesgo de churn identificado: día 90 (fin del programa Principiante) — mitigación: transición fluida a la Ruta Intermedio (no es "desbloqueo especial", es la ruta que ya existe para seguir progresando)

## MVP — funciones núcleo (ajustado con la corrección del usuario, Sesión 1)
0. Selector de NIVEL al entrar: Principiante (plan fijo 90 días) vs Intermedio (rutina de progresión anti-estancamiento) — antes del selector de meta metabólica
1. Selector de ruta metabólica (Músculo / Pérdida de grasa) — aplica a ambos niveles
2. Plan diario con ilustraciones biomecánicas simples: fijo e inalterable en Principiante; con progresión de carga/variación en Intermedio
3. Botón "Alternativa de ejercicio" (Botón de Rescate) para sustituir máquinas ocupadas al instante — ambos niveles
4. Temporizador de descanso integrado entre series
5. Registro histórico de pesos/cargas — clave en Intermedio para medir progresión y romper estancamiento
- NO construir en el MVP: integraciones con redes sociales, IA generativa de rutinas, sincronización con smartwatches
- Primera victoria (<5 min): elegir nivel + meta metabólica y ver la rutina exacta del día, lista para iniciar cronómetro
- Pendiente de definir con el usuario más adelante (no bloquea el plan maestro): cómo pasa un Principiante a Intermedio (¿automático al día 90, o el usuario elige entrar directo a Intermedio si ya no es novato?)

## Sesión 5bis (29/08/2026) — Pulido de la app interna a pedido del usuario
- Interruptor "Descanso automático entre series" en `/app`: el usuario decide antes de entrenar si el temporizador arranca solo al registrar una serie, o si prefiere descansar a su ritmo (campo `descansoAutomatico` en `lib/routine.ts`, default true, retrocompatible con progreso guardado antes de este campo).
- Enlace "¿Cómo se hace?" en cada ejercicio: abre una búsqueda de YouTube (`[ejercicio] técnica correcta`) en pestaña nueva. Resuelve la duda de técnica sin producir contenido propio.
- Evaluadas y decididas explícitamente: (a) NO se agregó texto de técnica por ejercicio (el usuario solo pidió el interruptor + el enlace) — pendiente si se quiere más adelante; (b) NO se adapta nada por género — ninguna pantalla pregunta género hoy y la técnica correcta de un ejercicio no depende de eso; (c) ilustraciones/video PROPIOS por ejercicio quedan pendientes de decisión explícita del usuario — implican costo/tiempo real (diseño o licencias), no se construyen sin su OK.
- **Mascota — decidido con el usuario (29/08/2026), arte PENDIENTE**: sí lleva personaje ("muñequito con personalidad propia", no solo un ícono minimalista). El usuario genera los bocetos con IA de imagen por su cuenta; el brief que se le dio: personaje humano tipo "entrenador confiable" (NO animal, NO infantil/tierno — Mateo no quiere sentirse tratado como niño), paleta limitada a los tokens ya aprobados (fondo #F5F1EA, acento #5C7A1F, tinta #211D17), con el logo en la ropa, 4 poses: reposo/inicio, aviso de fin de descanso, inicio de ejercicio, fin de entrenamiento (celebración CONTENIDA, sin saltos ni confeti — coherente con la personalidad "Sobria" ya compilada). Pendiente: que el usuario traiga los bocetos para elegir uno antes de implementar nada en código.
- Mockup de referencia recibido (29/08/2026): clipart genérico de "Personal Trainer" (camiseta negra, tabla, silbato). Sirve de referencia de pose/personalidad, NO de paleta/estilo — se le indicó al usuario ajustar antes de generar sus bocetos: ropa en tono papel/crudo (no negro) con el acento #5C7A1F como único detalle, línea plana/editorial (no clipart 3D con degradados) para no chocar con la UI flat existente, y el logo de GymEvo en vez de texto "PERSONAL TRAINER".

## Gamificación y retención (loop implementado en Sesión 5)
- Loop del hábito (Hooked): Gatillo [hora de entreno que eligió en el onboarding] → Acción [marcar cada ejercicio de hoy como hecho] → Recompensa [tachado verde + racha que sube] → Inversión [historial de pesos acumulado — cuesta más abandonar mientras más tiene registrado]
- Mecánica elegida: racha diaria (sin XP ni ligas — el nicho y la personalidad "Sobria" de FICHA-ARTE no piden más capas)
- Primera victoria que celebra el onboarding (<60s): ver el plan del Día 1 generado con su meta real (ya construida en Sesión 4)
- Momentos emocionales (56) implementados: M0 ritual diario (pantalla `/app`) · M4 racha en riesgo (llama apagada, sin pánico ni rojo) · hito de racha 7/30/100 días (número que cuenta, sin confetti — coherente con el compilador de personalidad "Sobrio" de FICHA-ARTE)
- Notificaciones push: NO implementadas todavía (requieren backend/servicio push — Sesión 6)

## Secuencia maestra de construcción (NO saltar)
- Estado de la secuencia: Landing ✅ construida · Onboarding ✅ construido · Paywall ✅ construido · Login ✅ construido (mock) · App interna ✅ construida (sin backend) · Servicios externos: pendiente (Sesión 6)
- Ruta aprobada: `/` → `/onboarding` → `/onboarding/generando` → `/onboarding/plan` (vista previa Día 1) → `/paywall` → `/login` → `/app`

## Decisiones técnicas (NO re-discutir sin pedirlo el usuario)
- Framework: **Next.js App Router** (decidido el 28/08/2026) — el default del stack pineado (51): necesitamos SEO en la landing de ventas, API routes para el webhook de Hotmart, y auth. Vite queda descartado porque es solo para herramientas puras post-login sin landing pública.
- Offline-first ligero: el contenido del día (rutina + ilustraciones) se cachea localmente (localStorage/IndexedDB vía Service Worker simple) tras la primera carga, para que funcione aunque el sótano del gimnasio tenga mala señal (riesgo #3 del reporte). El registro de pesos se guarda local y sincroniza al recuperar conexión. No es una PWA offline completa — es cache-first del contenido estático del día.
- Auth: **Supabase Auth con magic link (sin contraseña)** — encaja con el modelo onboarding-first: el usuario hace todo el onboarding y ve su plan del Día 1 SIN cuenta; la cuenta se crea recién en el paywall/checkout (evita fricción temprana, responde a la objeción de "otra app que pide mis datos de una"). Sesión larga (30-90 días, default de Supabase) porque es una app de consumo diario.
- Base de datos: Supabase (Postgres) con RLS. Tablas propias del usuario (`profiles`, `workout_logs`, `user_progress`) llevan `user_id` + RLS estricta (cada quien ve solo lo suyo). El contenido de las rutinas (`exercises`, `routine_days`, `exercise_alternatives`) es catálogo fijo de solo-lectura pública, sin datos personales — no necesita RLS por usuario.

### Mapa de pantallas
```
/                    → Landing (venta)
/onboarding          → Selector de nivel (Principiante/Intermedio) + meta (Músculo/Grasa) — sin cuenta
/paywall             → Trial 7 días + oferta $4.99/mes o $29.99/año
/login               → Magic link (se crea la cuenta aquí, tras decidir pagar)
/app                 → Plan del día (protagonista) + botón de Rescate + temporizador
/app/historial       → Registro de pesos/cargas por ejercicio
/app/perfil          → Nivel actual, meta, plan, cerrar sesión
```
Total: 6 pantallas únicas (dentro del límite de 8).

### Modelo de datos (resumen — el SQL exacto se escribe en Sesión 6/Deploy)
- `profiles`: id (FK auth.users), nivel (principiante/intermedio), meta (musculo/grasa), dia_actual, fecha_inicio, racha
- `exercises` (catálogo): id, nombre, grupo_muscular, ilustracion_url, tipo (máquina/peso libre)
- `exercise_alternatives` (catálogo): exercise_id → alternative_exercise_id (para el Botón de Rescate)
- `routine_days` (catálogo, contenido curado por nosotros): nivel, meta, numero_dia, lista de ejercicios+series+reps+descanso
- `workout_logs` (usuario): user_id, exercise_id, fecha, peso, reps, series
- `user_progress` (usuario): user_id, racha_actual, ultimo_dia_completado, hitos desbloqueados

## Sesiones completadas ✅
- Sesión 1 — Validación + avatar + monetización (02C) + framework + arquitectura + modelo de datos + auth — cerrada 28/08/2026
- Sesión 2 — Identidad visual: FICHA-ARTE.md cerrada, opción C "Cuaderno de Sala" — cerrada 28/08/2026
- Sesión 3 — Página de ventas: Next.js scaffolded, kit de landing (plantillas-codigo/landing) copiado a components/landing, tokens.css tematizado con FICHA-ARTE, copy marcado en docs/copy/landing.md, 10 secciones canónicas compuestas en app/page.tsx, páginas legales creadas (/privacidad /terminos /reembolsos) — cerrada 28/08/2026
- Sesión 4 — Onboarding + Paywall + Login: `/onboarding` (6 pasos: nivel, meta, frustración con eco de dolores reales, reconocimiento personalizado por objeción, horario, compromiso de días/semana con slider) → `/onboarding/generando` (loading persuasivo 4.8s con líneas reales) → `/paywall` (timeline de trial Hoy/Día 6/Día 7, planes anual+mensual, CTA, trust row) → `/login` (magic link mock con estados enviando/enviado/error). Probado de punta a punta en navegador — cerrada 28/08/2026
- Sesión 4bis (29/08/2026) — Onboarding mejorado tras comparar contra un análisis externo (Gemini) sobre apps de fitness/wellness (Verv): (1) NUEVA pantalla `/onboarding/plan` — vista previa REAL del Día 1 entre "generando" y el paywall (antes faltaba: pedíamos pago sin mostrar el resultado, contra nuestra propia doctrina de Cal AI/Duolingo). Muestra los ejercicios reales del Día 1 + el resto de la semana bloqueado con honestidad (sin blur falso). (2) Se agregó **tempo de ejecución** (ej. "3-1-1" seg) a cada ejercicio en `lib/routine.ts` — cumple la promesa original de la idea validada ("a qué velocidad exacta hacerlo") que nunca se había implementado; visible en `/app` y en la vista previa. Decisiones rechazadas del análisis externo, con razón: NO se agregan reseñas/testimonios (fabricarlos viola la ética del sistema y el diferenciador #1 del avatar) NI datos biométricos edad/peso/altura (ninguna función los usa todavía; sería fricción decorativa). NO se cambia la barra de progreso a puntos (el archivo 50 lo prohíbe explícitamente).
- Sesión 3bis (29/08/2026) — Elevación de la landing para conversión/escaneabilidad: headline nuevo ("Nunca más sin saber qué hacer en el gym", eco literal del dolor #1 de FICHA-AVATAR) · CTA repetido tras la Solución y tras la Garantía (antes solo estaba en hero/mid-page/oferta) · garantía corregida para reflejar los 14 días reales (7 trial + 7 Hotmart, FICHA-MERCADO.md) en vez de decir "7 días" de forma ambigua. Auditoría de escaneabilidad a 375px pasada — ver tabla abajo.
- Sesión 5 — App interna: shell de 3 secciones (`/app` Plan del día, `/app/historial`, `/app/perfil`) con tab bar inferior. Plan del día = M0 "ritual diario" (56): split de 4 días (Empuje/Tirón/Piernas/Full) según catálogo de 20 ejercicios con alternativas, registro de peso por serie, Botón de Rescate (swap a alternativa), temporizador de descanso, tachado verde al completar (dispositivo ownable de FICHA-ARTE), estado M4 "racha en riesgo" (llama apagada, sin pánico) y celebración de hito de racha (7/30/100 días, estilo sobrio sin confetti). Progreso en localStorage (`lib/routine.ts`) — cerrada 28/08/2026

## Sesión en progreso 🔧
Sesión 6 (integraciones reales y seguridad) — en curso.
- Pre-flight P1 completado: se encontraron y corrigieron 2 bugs reales que el proyecto arrastraba sin que el usuario lo pidiera (no se buscaban, los sacó a la luz `npx eslint .` que no se había corrido explícito antes): (a) `<a href="/">` en vez de `<Link>` en app/login y Hero.tsx (regla next/next), (b) un bug de hydration que YO MISMO introduje al intentar arreglar el lint `react-hooks/set-state-in-effect` (cambié `useState(null)+useEffect` a `useState(() => leerX())` en 5 archivos — pasaba el lint pero rompía en runtime porque sessionStorage/localStorage no existen en el server). Revertido a `useState(null)+useEffect` + comentario `eslint-disable-next-line` puesto justo encima de la línea exacta del `setState`. Verificado en navegador: 0 errores de hydration en las 5 pantallas afectadas.
- `eslint.config.mjs`: se agregó `plantillas-codigo/**` y `docs/**` a `globalIgnores` (el kit vendored del SO no debe lintearse, igual que ya excluía tsconfig.json).
- `components/landing/Hero.tsx`: primera vez que se toca este archivo del kit → el lint de diseño exigió convertir valores de píxel arbitrarios a clases Tailwind por defecto (max-w-*, text-*) — hecho.
- **GitHub: ✅ conectado** (31/08/2026) — repo `oscarmint/gymevo`, push exitoso.
- **Vercel: ✅ conectado y verificado** (31/08/2026) — dominio público `https://gymevo-eta.vercel.app`, landing carga sin errores de consola.
- **Supabase: ✅ código y tablas listos** (31/08/2026) — proyecto, código de conexión, tablas `profiles`/`workout_logs` con RLS (aplicadas directo por MCP), y `/login` con magic link real. Commit+push hechos.
- ✅ **Incidente resuelto** (31/08/2026): tras el push, `gymevo-eta.vercel.app` mostró "Internal Server Error" (`Invalid supabaseUrl` en el proxy, por un valor mal pegado en `NEXT_PUBLIC_SUPABASE_URL` en Vercel). Usuario corrigió la variable con el valor exacto confirmado por Supabase MCP y volvió a desplegar. Verificado en navegador y con `get_runtime_errors` (Vercel MCP): 0 errores en `/`, `/login`, `/app`.
- Nota técnica: se descubrió acceso directo a Supabase y Vercel vía MCP en esta sesión — se puede aplicar SQL, revisar tablas y auditar seguridad sin pedirle al usuario que copie/pegue nada en el panel. Usar esto de ahora en adelante en vez de pedirle pasos manuales cuando sea posible.
- **Resend: ✅ conectado como SMTP de Supabase** (01/09/2026) — sin dominio propio todavía (el usuario no tiene presupuesto ahora); remitente de pruebas `onboarding@resend.dev` (solo entrega al correo de la cuenta de Resend, suficiente para seguir probando). Límite de correos subió de 2/hora a 30 (confirmado en logs de Supabase). Cuando haya dominio, cambiar el remitente a algo con marca propia.
- ✅ **Bug de configuración corregido** (01/09/2026): la "Site URL" de Supabase Auth apuntaba a `http://localhost:3000` desde la creación del proyecto — los magic links pedidos desde `gymevo-eta.vercel.app` redirigían a la máquina local del usuario en vez de la producción (Supabase usa la Site URL como fallback cuando el `redirectTo` no está en la lista de Redirect URLs permitidas). Corregido en el dashboard: Site URL y Redirect URLs actualizadas a `https://gymevo-eta.vercel.app`. Verificado con `query_logs`: el `referer` de las peticiones `/otp` ya muestra el dominio de producción.
- **Video de fondo + nombre de usuario** (01/09/2026): el video de la landing (`/videos/hero-gimnasio.mp4`) también se agregó como fondo de "Plan de hoy" (mismo tratamiento de scrim fuerte para legibilidad). Perfil ahora tiene un saludo editable ("Hola, {nombre}") — nueva columna `profiles.nombre` (migración `0002_nombre_perfil.sql`), guardado local + remoto.
- Pendiente: dominio propio → Hotmart (un pedido a la vez, Protocolo Cero Secretos en Chat — nunca pedir que pegue valores secretos en el chat) cuando el usuario tenga presupuesto; mientras tanto, seguir el pulido pendiente de las 4 pantallas del dinero (Sesión 7).

## Próximas sesiones 📋
- Sesión 6: integraciones reales (Supabase, Hotmart, dominio) y seguridad
- Sesión 7: testing, animaciones, pulido y rigor de entrega

## Problemas conocidos ⚠️
- Visual del hero y los 4 frames del carrusel ("La app por dentro") son PLACEHOLDERS honestos (marco punteado + ilustración/nombre de pantalla) — se reemplazan por screenshots reales cuando la app interna exista (Sesión 5). No declarar la landing 100% terminada hasta ese reemplazo (regla del 19).
- `scripts/audit-conversion.sh` marca varios "críticos" que son falsos positivos del propio script contra los archivos DEL KIT del SO (comentarios de código y className de Tailwind mal contados como "copy") — verificado a mano; no requieren corrección. También marca "cero hairline degradé" y "fondo plano" como críticos pese a que `Hero.tsx` sí trae el mesh radial-gradient de fondo y `Oferta.tsx` sí usa `<Hairline emphasis>` en el plan anual — confirmado leyendo el código; falso negativo del script (no reconoce estilos inline multilínea). Pendiente: no se corrió aún el subagente `revisor-visual` (rúbricas /40 y /20) ni `scripts/audit-diseno.sh` — eso es parte del cierre de Sesión 7 (pulido y rigor de entrega) según la secuencia maestra, no bloquea seguir a Sesión 4.
- Dominio real de la app aún no existe — el email de soporte usa `soporte@gymevo.app` como placeholder hasta que el usuario compre el dominio (Sesión 6, servicios externos).
- Onboarding/paywall/login (Sesión 4) son UI real y funcional pero sin backend: las respuestas viven en sessionStorage (no hay cuenta todavía, según el modelo onboarding-first), el botón de pago del paywall navega a /login en vez de abrir el checkout real de Hotmart, y el login no envía emails de verdad. Todo esto se conecta a Supabase Auth + Hotmart real en la Sesión 6 — está señalado con comentarios "Sesión 6" en el código (lib/onboarding.ts, app/paywall/page.tsx, app/login/page.tsx).
- Paywall construido como página única (no la secuencia de 3 pantallas que recomienda 50 §C0 para +37% de conversión) — decisión de alcance para esta sesión; se puede partir en 3 pasos más adelante si los datos lo justifican.
- App interna (Sesión 5) sin backend todavía: el progreso (racha, día, historial de pesos) vive en localStorage del navegador, se pierde si el usuario cambia de dispositivo o borra datos. Sesión 6 lo mueve a las tablas `workout_logs`/`user_progress` de Supabase.
- Las ilustraciones de ejercicios son solo texto (nombre + series/reps), sin dibujo biomecánico — el documento original de la idea pedía "ilustraciones anatómicas sencillas". Pendiente de diseño gráfico si se quiere esa capa visual; no es un placeholder deshonesto (no finge tener algo que no tiene), pero es una reducción de alcance visual a anotar.
- Corregido durante la Sesión 5: un bug real de condición de carrera si el usuario tocaba "Registrar" en dos ejercicios muy rápido (el segundo tap sobrescribía el primero) — se arregló usando actualización funcional de estado (setState con función, no valor directo). Verificado con clics simultáneos simulados.
- **veredicto:paywall** — ✅ **LISTA** (docs/revisiones/paywall-veredicto.md, 31/08/2026): Usabilidad 36/40, Craft 16/20, Copy 17/20 — pasa el gate doble justo en el límite. Recorrido: 30→34→36 usabilidad, 12→11→16 craft, a lo largo de 3 rondas de corrección real (checkmarks custom, garantía junto al CTA, whileTap en tarjetas, borde punteado, alineación de bullets, devIndicators apagado).
- **veredicto:landing** — ✅ **LISTA** (docs/revisiones/landing-veredicto.md, 01/09/2026): **Usabilidad 36/40, Craft 19/20, Copy 19/20** — pasa el gate triple. Recorrido completo de la sesión: 17→32→33→34→33→34→36/40 usabilidad, 5→15→13→13→14→13→19/20 craft, copy 19→19→20→19/20, a lo largo de 7 rondas de corrección real. Lo que la cruzó: (1) un BUG REAL de z-index (mismo patrón que `app/app/perfil/page.tsx`) tenía el video de fondo del Hero y la textura de "renglones de cuaderno" completamente invisibles desde que se agregaron — corregido con `z-index:0` + contenido en su propia capa `relative z-10`; (2) el scrim del Hero bajó de 85-92% a 62-80% de opacidad para que el video+líneas se vean de verdad sin perder legibilidad del texto; (3) `<BotonVolverArriba>` nuevo (`components/landing/ui.tsx`) para la heurística de control/libertad que faltaba en una página de ~8000px. Sumado a lo de rondas previas: 2ª nota de color (ámbar) en 3 lugares, dispositivo ownable (tachado real), checkmarks consistentes, carrusel con affordance de deslizar, titulares recortados a 1-3 palabras, conteo animado en precios, CTAs consistentes. Defectos menores NO bloqueantes que quedan para una futura pasada: el botón de volver arriba podría pisar el chevron de la última pregunta del FAQ en un caso puntual (subir el umbral de aparición), y no hay header persistente durante todo el scroll (el botón de volver arriba mitiga esto, un header sticky sería el óptimo). La paleta base sigue siendo la más cercana al límite del test anti-clon del banco canónico, pero el revisor confirmó que NO es un clon (difiere en tipografía) y el resto de la identidad (textura, dispositivo, 2ª nota) ya compensa lo suficiente para pasar.
- **veredicto:onboarding** — NO LISTA tras 6 rondas de esta sesión (33→34→34→34→33/40 usabilidad, 14→12→11→14→15/20 craft). Mejora real de 32/10 (inicio de sesión) a 33-34/40 · 15/20 (cierre) — no llega al gate (≥36/40 y ≥16/20) pero por poco margen. Se corrigió la causa raíz estructural real (`justify-center` dejaba vacío en pasos cortos → ancla arriba + `TarjetaRuta` con progreso en vivo), y de paso se encontró y corrigió el MISMO bug de z-index que bloqueaba el fondo de la landing (el degradé y las líneas de renglón del onboarding también estaban invisibles todo este tiempo). Otras correcciones reales: navegación por teclado en los chips (con `prefers-reduced-motion`), celebración animada (trazo verde) en "Te entendemos", tarjeta con hairline superior en vez de borde completo (ya no se confunde con un chip tocable), padding simétrico, balance verde/ámbar del degradé, conteo animado en el número de días/semana. **Único defecto real pendiente de peso** (confirmado por el revisor como "requiere rediseño de contenido, no ajuste de código"): sigue quedando ~40-45% de pantalla vacía bajo la tarjeta en los pasos cortos (nivel/meta) — el fondo ya no se ve "roto" gracias a la textura, pero falta contenido real ahí. Sesión CERRADA aquí por decisión explícita de tiempo del usuario ("por tiempo, opción 2: no tocar la paleta").
- **veredicto:pantalla-principal** — NO LISTA. 5 rondas de revisión: 26→30→33→34→32/40 usabilidad, 11→11→14→16→15/20 craft — mejora real pero no monótona (la última ronda bajó levemente: cards con demasiados elementos a la vez, peso vacío se guarda como 0 en silencio, sin transición entre tabs del nav, paleta cerca del arquetipo "cuaderno" genérico). Se paró aquí por rendimientos decrecientes tras 5 iteraciones — los defectos restantes (simplificar la card de ejercicio, transición de tabs, 2ª nota de color) quedan para la próxima sesión de pulido.
- **veredicto:paywall** — el hook lo marcó "caducado" citando `app/app/page.tsx` como código más nuevo, pero ese archivo es de la pantalla PRINCIPAL, no del paywall — `app/paywall/page.tsx` (el código real de esta pantalla) no cambió desde el commit `099489a`, y el veredicto (`4373232`) es posterior a ese commit. Confirmado con `git log` y timestamps de archivo: el veredicto SÍ es más nuevo que el código que evalúa. Falso positivo del hook (compara contra cualquier .tsx del repo, no contra el archivo de la pantalla específica) — el veredicto LISTA de paywall sigue vigente. **Re-confirmado 01/09/2026** tras el commit `3b6c3e2` (que solo tocó `app/app/page.tsx`, la pantalla principal): `app/paywall/page.tsx` sigue sin cambiar desde `099489a`, mismo falso positivo, misma conclusión — nada que corregir en paywall. **Re-confirmado otra vez 01/09/2026** tras el commit `ed4b8cb` (cronómetro de descanso — solo tocó `app/app/page.tsx`, `lib/routine.ts`, `lib/supabase/sync.ts`): `app/paywall/page.tsx` SIGUE sin cambiar desde `099489a`. El hook seguirá disparando esta alerta cada vez que cualquier otro `.tsx` del repo cambie, porque compara contra "cualquier .tsx más nuevo", no contra el archivo real de la pantalla — no hace falta re-lanzar el revisor-visual de paywall hasta que `app/paywall/page.tsx` mismo cambie. **Re-confirmado una vez más 01/09/2026** tras el commit `9fea110` (sonido de descanso — mismos archivos de siempre, ninguno es paywall): sigue en `099489a`, sigue siendo falso positivo. **Y otra vez 01/09/2026** tras el commit `a80e94b` (confeti de cierre — tocó `app/app/page.tsx` y `components/landing/tokens.css`, tampoco paywall): sigue en `099489a`. **Y otra vez 01/09/2026** tras los cambios de onboarding/landing de esta ronda (`app/onboarding/page.tsx`, `components/landing/{AppPorDentro,Solucion,tokens}`): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 2 de correcciones de landing/onboarding (`app/page.tsx`, `components/landing/Oferta.tsx` incluidos): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 3 (commit `8d071c9`): sigue en `099489a`. **Y otra vez 01/09/2026** tras el fix del bug de z-index (commit `97e5b8c`): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 6 (commit `c9bce15`): sigue en `099489a`. **Y otra vez 01/09/2026** tras el botón volver arriba (commit `5c2d266`): sigue en `099489a`. Dejo de repetir esta entrada en próximas rondas salvo que `app/paywall/page.tsx` cambie de verdad.
- **RESUMEN Sesión 7 (31/08/2026)**: de las 4 pantallas del dinero, **paywall ya está LISTA** (36/40, 16/20, 17/20 copy). Las otras 3 mejoraron sustancialmente con correcciones reales (no cosméticas) pero landing y onboarding necesitan más trabajo de identidad/estructura que excede el alcance razonable de esta sesión — quedan documentadas con sus defectos exactos para retomar. No se declaran "listas" hasta que el veredicto lo diga explícitamente — autoevaluarse no cuenta como evidencia (Regla de Oro 7).
- **garantía / FICHA-MERCADO** — FICHA-MERCADO.md ya existe (creada 28/08/2026) con Prueba elegida: 7 días y Garantía elegida: 14 días (14>7, cumple la regla dura). PERO los plazos reales de garantía de Hotmart (desde cuándo cuenta, máximo configurable) siguen **sin verificar contra la pasarela real**, porque la cuenta de Hotmart todavía no existe (creación de cuenta es tarea del usuario en Sesión 6). El copy publicado (landing/paywall/FAQ) usa estos plazos como el PLAN a implementar, no como algo ya confirmado en el panel — antes de lanzar de verdad, hay que crear la cuenta, configurar el producto con estos plazos exactos, y volver a llenar los campos "NO ENCONTRADO" de FICHA-MERCADO.md con el dato real.

## Pendientes del usuario (acciones que el usuario debe hacer)
- [ ] Ninguna acción pendiente por ahora — sigo yo con la Sesión 4 (onboarding, paywall, login)

## Notas para la próxima sesión
- El usuario llegó con el "RESUMEN FINAL — IDEA VALIDADA PARA CONSTRUIR" completo (prompt de investigación externo). No se debe re-validar la idea ni proponer alternativas.
- Nombre FINAL confirmado por el usuario: **GymEvo** (28/08/2026) — "Método Cero" queda solo como referencia interna del documento original, no se usa en producto ni copy.
