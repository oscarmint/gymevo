# ESTADO — GymEvo (nombre tentativo: Método Cero)
Última actualización: 2026-09-03 | Sesión actual: 8 (en curso)

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Planes Trimestral y Semestral agregados al paywall, a pedido explícito del usuario.**
- **Investigación real hecha antes de construir**: ni Hevy, ni Fitbod, ni Ladder (los 3 competidores directos citados en FICHA-MERCADO.md) ofrecen planes de 3 o 6 meses — solo mensual y anual. El usuario pidió agregarlos igual (válido: captura gente que no quiere comprometerse a un año) y luego pidió explícitamente "hagámoslo como los demás" — o sea, el patrón de tarjetas apiladas que ya tenía Mensual/Anual, no un selector nuevo.
- **Precios** (`PLANES` en `app/paywall/page.tsx`): Mensual $4.99 (sin descuento) · Trimestral $11.99 (20% de ahorro, ~$4.00/mes) · Semestral $19.99 (33% de ahorro, ~$3.33/mes) · Anual $29.99 (50% de ahorro, ~$2.50/mes — el mismo que ya tenía). Curva de descuento CRECIENTE real, no números al azar — investigado (el descuento anual típico de apps de suscripción en 2025-26 subió a ~28-50%).
- Cada tarjeta ahora muestra un badge "Ahorra N%" (excepto Mensual, que es la base) además del badge "MÁS POPULAR" que ya tenía Anual. El timeline del trial y el texto "Se cobra..." de cada tarjeta ahora son dinámicos según el plan (antes tenían "$29.99/año" fijo).
- **Pendiente REAL del usuario** (no lo puedo hacer yo — requiere su cuenta de Hotmart): crear los 2 productos/ofertas nuevos (3 y 6 meses) en el panel de Hotmart, y agregar `NEXT_PUBLIC_HOTMART_CHECKOUT_TRIMESTRAL` / `NEXT_PUBLIC_HOTMART_CHECKOUT_SEMESTRAL` en Vercel. Mientras tanto, esos 2 botones caen al mock de `/login` — mismo comportamiento honesto que ya tenían Mensual/Anual antes de conectarse.
- Verificado: tsc ✓ · build ✓ · estructura de la pantalla confirmada completa y correcta (árbol de accesibilidad) — la herramienta de captura de pantalla falló de forma intermitente (quedó en `about:blank` sin razón clara, posible falla transitoria de Playwright), así que la evidencia visual de esta ronda es el árbol de contenido, no un PNG. Pendiente tomar el screenshot real la próxima vez que se abra el paywall.
/ Siguiente acción exacta: pedirle al usuario los 2 checkout links de Hotmart cuando los tenga listos.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Bug real corregido: nivel y meta se reseteaban al iniciar sesión en un navegador nuevo — ahora viven en `Progreso` (persistente) y se pueden cambiar desde Perfil con confirmación.**
- **Cómo se descubrió**: el usuario reportó "un error en perfil" justo después de entrar con el truco del enlace directo (sin pasar por el onboarding en esa sesión). Causa real: `nivel`/`meta` vivían SOLO en `RespuestasOnboarding` (sessionStorage — se borra al cerrar el navegador o cambiar de dispositivo/sesión), nunca se leían de vuelta desde `profiles` aunque la columna ya existía en la base de datos. Cualquier sesión nueva sin ese sessionStorage veía la ruta reseteada a Principiante/Ganar músculo sin importar lo que el usuario había elegido.
- **Fix de raíz**: `nivel`/`meta` se movieron a `Progreso` (junto al resto de lo que sí persiste en localStorage + Supabase). `leerProgreso()` hereda de `leerRespuestas()` SOLO la primera vez que se crea el progreso (o si faltan por compatibilidad); de ahí en adelante `Progreso` manda. `app/app/page.tsx`, `app/app/perfil/page.tsx` y `app/app/historial/page.tsx` ahora leen `progreso.nivel`/`progreso.meta` en vez de `respuestas?.nivel`/`respuestas?.meta`.
- **Feature pedida explícitamente**: en Perfil, dos selectores (Nivel: Principiante/Intermedio, Meta: Ganar músculo/Perder grasa) — cualquier cambio abre un modal "¿Estás seguro?" (mismo patrón de accesibilidad del modal de salida del onboarding) explicando que reordena ejercicios/macros/cardio, antes de aplicarlo. Nueva función `cambiarRuta()` en `lib/routine.ts`.
- Verificado: tsc ✓ · eslint ✓ (0 errores) · build ✓. No se pudo probar en vivo el cambio de ruta (requiere sesión autenticada real).
/ Siguiente acción exacta: publicar. Pedirle al usuario que confirme que ahora su nivel/meta NO se resetean al volver a entrar, y que puede cambiarlos desde Perfil.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Foto de perfil — cámara o galería, con inicial de respaldo cuando no hay foto.**
- **Almacenamiento** (migración `0017`): bucket `avatars` en Supabase Storage, público de lectura (una foto de perfil no es un dato sensible) pero de escritura restringida — cada usuario solo puede subir/actualizar/borrar dentro de SU carpeta (`<user_id>/avatar.ext`), verificado con RLS real sobre `storage.objects`, no solo ocultando el botón. Columna nueva `profiles.avatar_url`.
- **Selector nativo cámara/galería**: el usuario pidió explícitamente ambas opciones (no solo selfie) — un `<input type="file" accept="image/*">` sin restringir el atributo `capture` deja que el propio sistema operativo ofrezca "Tomar foto" o "Elegir de galería", en vez de forzar una de las dos.
- **Subida** (`lib/supabase/sync.ts` → `subirAvatar`): siempre sube al mismo nombre de archivo (`avatar.ext`, con `upsert`) para no acumular fotos viejas en el bucket; cache-bust con `?t=timestamp` en la URL para que el navegador no siga mostrando la foto anterior tras cambiarla.
- **UI** (Perfil): círculo de 64px junto al nombre, con la inicial del nombre como respaldo honesto cuando no hay foto (nunca un espacio vacío ni un ícono genérico de persona), insignia de cámara superpuesta, y un spinner real mientras sube — con aviso si la subida falla (heurística 9, nunca en silencio).
- Verificado: tsc ✓ · eslint ✓ (0 errores) · build ✓ · `get_advisors` (seguridad) sin hallazgos nuevos. No se pudo probar la subida real en este entorno (requiere sesión autenticada + un archivo de imagen real desde el dispositivo del usuario).
/ Siguiente acción exacta: publicar. Pedirle al usuario que pruebe subir una foto (cámara y galería) desde su celular y confirme que se ve bien y que el selector le da ambas opciones.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Reestructuración completa del motor de entrenamiento y nutrición ("arquitectura REAL FISIC") — calendario de 7 días, cardio por ruta, día de recuperación activa, diferenciación principiante/intermedio, y seguimiento de progreso distinto por ruta.**
- **Motor de nutrición** (`lib/macros.ts`): proteína/grasa ajustadas a los rangos exactos dados por el usuario — Ruta A (músculo) proteína 1.6-2.0 g/kg (usa 1.8), grasa 0.6-1.0 g/kg (usa 0.8); Ruta B (grasa) proteína 1.8-2.4 g/kg (usa 2.1), grasa 0.6-0.8 g/kg (usa 0.7) — antes la grasa se calculaba como % de calorías, ahora es directo por kg como pide la ruta. El superávit/déficit (+225/-400 kcal) ya coincidía exacto con los rangos dados, sin cambios.
- **Calendario de 7 días** (`lib/routine.ts`, `SPLIT`): reestructurado para que cada grupo muscular viva en UN solo día (antes cuádriceps e isquios/glúteos se mezclaban en lunes+jueves) — Día 1 pierna/cuádriceps+abdomen, Día 2 empuje+cardio, Día 3 tracción+abdomen, Día 4 recuperación activa (sin pesas, día nuevo), Día 5 pierna/isquios-glúteos, Día 6 híbrido superior sin piernas, Día 7 descanso hormonal.
- **Cardio por ruta** (`cardioDeHoy(diaActual, meta)`, antes fijo por día): el Día 2 ahora es Zona 2 en Ruta A (no interferir con volumen muscular) o HIIT en Ruta B (acelerar metabolismo) — a especificación exacta del usuario.
- **Día 4 nuevo — recuperación activa** (`esDiaDeRecuperacionActiva`, `recuperacionActivaDeHoy`): pantalla propia en Plan de hoy (sin ejercicios de fuerza) con meta de 7.000-10.000 pasos para ambas rutas, más una sesión extra de Zona 2 opcional solo en Ruta B.
- **Principiante vs Intermedio, ahora sí diferenciados** (antes `nivel` solo cambiaba el título, ningún ejercicio real): los 4 ejercicios técnicos con barra libre (sentadilla, peso muerto, press militar, remo con barra) se marcaron `avanzado: true` — en Ruta Principiante se sustituyen automáticamente por su alternativa ya guiada (reusando `alternativaId`, ya existente en el catálogo) y las series bajan en 1 (piso de 3). Reutiliza datos que ya existían en vez de duplicar el catálogo entero.
- **Aviso de 7 días** (onboarding, paso "compromiso"): si el usuario mueve el slider a 7, aparece un aviso explicando que el plan siempre incluye descanso y por qué (verificado en vivo con captura — ver `.playwright-mcp/onboarding-7dias-aviso.png`).
- **Seguimiento de progreso distinto por ruta** (lo más grande de esta ronda): `Progreso` gana `pesoInicialKg`/`cinturaCm`/`cinturaInicialCm`/`fechaInicioMedidas` (migración `0016`, columnas nuevas en `profiles`) — el "inicial" se fija UNA sola vez (`registrarMedidasIniciales`) y nunca se sobrescribe, así el progreso no se "resetea" cada vez que el usuario edita su peso en Perfil. Perfil ahora pide cintura (cm) solo en Ruta B. Historial tiene una tarjeta nueva "Tu progreso" que en Ruta A compara el peso actual contra el inicial (+300 a +800 g/mes = meta sana) y en Ruta B compara la cintura — nunca inventa un número: si falta el dato o solo hay una medida, lo dice tal cual ("Registra tu cintura en Perfil...").
- **Imágenes de ejercicios**: sin cambios de comportamiento — `imagenExplicacion` ya era opcional desde antes, así que cualquier ejercicio nuevo sin imagen sigue funcionando con la silueta de respaldo (`CuerpoMuscular`), tal como pidió el usuario.
- Verificado: tsc ✓ · eslint ✓ (0 errores) · build ✓. El aviso de 7 días se confirmó en vivo con Playwright (sin sesión, alcanzable sin login). El resto (Día 4, cardio HIIT/Zona2 por ruta, sustitución de ejercicios en Ruta Principiante, tarjeta de progreso) se verificó por tipos/build pero NO se vio renderizado en vivo — requiere sesión autenticada real, que no se pudo simular en este entorno de pruebas.
/ Siguiente acción exacta: publicar (push + migración ya aplicada en Supabase). Pedirle al usuario que revise en su celular: (1) el Día 4 de recuperación activa cuando le toque, (2) que el cardio del Día 2 cambie según su ruta, (3) la tarjeta "Tu progreso" en Historial tras registrar peso/cintura en Perfil.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Bug real crítico corregido: un usuario con plan pro real veía el paywall — causado por una regla de seguridad mía con recursión infinita.**
- **Cómo se descubrió**: el usuario no podía iniciar sesión (el enlace de acceso por correo no llegaba — ver más abajo el diagnóstico de Resend/Gmail). Se le dio un enlace de acceso generado directo desde la API de administración de Supabase para destrabarlo YA. Al entrar, en vez de ir a su plan, lo mandó al paywall — pese a que su perfil SÍ tiene plan "pro" en la base de datos.
- **Causa raíz real**: las políticas `admin_reads_all_profiles`/`admin_updates_all_profiles` que agregué en la migración `0011` (sesión de hoy, panel de admin) consultaban la tabla `profiles` DESDE una política de `profiles` — Postgres lo detecta como recursión infinita (error `42P17`) y la consulta del plan del usuario fallaba en silencio. `proxy.ts` interpretaba "no hay perfil" como "no es pro" y mandaba al paywall — a CUALQUIER usuario pro real, no solo al dueño. **Esto pudo haber afectado a un usuario pagador real entre la publicación de la migración 0011 y este fix**, aunque hoy el único usuario real es el propio dueño.
- **Fix** (migraciones `0014`/`0015`): función `is_admin()` `SECURITY DEFINER` (mismo patrón que `reconciliar_membresia()`, ya usado en el proyecto) que consulta `profiles` SIN pasar por RLS, reemplazando el `EXISTS` inline en TODAS las políticas de admin (profiles, workout_logs, hotmart_purchases, webhook_log, event_log, error_log, acquisition_spend, cost_assumptions).
- Verificado con una consulta simulando exactamente el contexto del usuario autenticado (`set local role authenticated` + JWT claims): antes del fix, error de recursión; después, devuelve `plan: "pro"` correctamente. `get_advisors` (seguridad) sin hallazgos nuevos.
- **Lección para no repetir**: nunca escribir una política RLS de la tabla X que haga `EXISTS (select ... from X ...)` dentro de sí misma — usar siempre una función `SECURITY DEFINER` para checks de rol/permiso, sin excepción, incluso si "debería" resolver por la política de fila propia.
/ Siguiente acción exacta: confirmar con el usuario que ya puede entrar a `/app` y a `/admin` con normalidad.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Panel de administración del dueño construido (`/admin`) — versión 1, con el brand kit nuevo desde el día uno.**
- **Plan aprobado explícitamente por el usuario** antes de escribir código (leídos: 21-BACKOFFICE, 09-SEGURIDAD, 26-AUTH-MODERNO, 31-EVALS-OBSERVABILIDAD, 40-UNIT-ECONOMICS, 36-ANALÍTICA, 17-VISUALIZACIÓN, PROMPT-BACKOFFICE.txt).
- **Auditoría real de la base de datos ANTES de construir**: `profiles`, `workout_logs` (24 series reales), `hotmart_purchases` (1 compra de prueba), `processed_events`/`webhook_log` (0 filas). Hallazgo importante: **el webhook de Hotmart NUNCA guarda el monto del cobro** — no hay columna de precio en ningún lado. Por eso Ingresos/MRR/Ganancia real quedan "Sin datos" en la v1, no un número inventado.
- **Seguridad (migraciones `0011`-`0013`)**: columna `profiles.role` (`user`/`admin`, default `user`) — el único perfil existente (el del propio usuario) se marcó `admin` directo, porque es su cuenta de prueba. RLS nuevo: el admin puede leer TODAS las filas de `profiles`/`workout_logs`/`hotmart_purchases`/`webhook_log` (antes solo veía la suya, como cualquier usuario), y puede insertar/actualizar `hotmart_purchases` (para el acceso manual). Tablas nuevas con RLS propio: `event_log`, `error_log`, `acquisition_spend`, `cost_assumptions` — existen ya, vacías, listas para cuando se instrumenten. El GUARD real vive en `proxy.ts` (verificado en el servidor contra `profiles.role`, igual patrón que el guard de `/app`) — nunca ocultar el link nada más, eso es un IDOR.
- **Función de agregar acceso a mano** (pedido explícito): `app/admin/usuarios/actions.ts` — un Server Action que sube/actualiza una fila en `hotmart_purchases` con `status='active'` para ese correo; en cuanto esa persona entre con magic link, `reconciliar_membresia()` (ya existente) le da el plan pro automáticamente. Se guarda `agregado_manualmente=true` para poder distinguir después estas altas de las reales de Hotmart.
- **Lo que el panel muestra HOY con datos reales**: compras totales y por estado, suscriptores activos/en prueba/cancelados, total de usuarios y altas recientes, series de entrenamiento registradas (total y últimos 7 días). **Lo que queda "Sin datos" a propósito**: Ingresos/MRR/Ganancia real (falta el monto en el webhook), Negocio LTV/CAC por canal (falta `profiles.source` poblado y gasto real en `acquisition_spend`), Errores (falta conectar Error Boundaries a `error_log`). La sección de costo de IA se omitió: esta app no usa IA generativa, no aplica.
- **Avisos automáticos**: hoy solo puede evaluar la salud del webhook con datos reales (sin eventos / con fallos) — el resto de disparadores del 21 (IA cara, canal que pierde dinero, margen negativo) quedan apagados hasta tener sus fuentes, para no generar alarmas falsas.
- Verificado: tsc ✓ · build ✓ · eslint ✓ (0 errores) · `npx tsc` limpio tras el build (regenera los tipos de rutas de Next, que marcaban falsos positivos de `/admin` antes del primer build). Confirmado con Playwright SIN sesión que `/admin` redirige a `/login?desde=admin` (el guard funciona). **Pendiente**: verificación visual CON sesión real de admin — el usuario puede entrar a `/admin` en producción con su propia cuenta (ya marcada admin) para confirmar que se ve y funciona bien; no se pudo simular esa sesión en este entorno de pruebas.
/ Siguiente acción exacta: publicar (push + migraciones ya aplicadas en Supabase). Pedirle al usuario que entre a `/admin` con su cuenta y confirme. Iteraciones futuras (no en esta v1): conectar `event_log`/`error_log` de verdad desde las pantallas de la app, capturar `profiles.source` en el checkout, guardar el monto del cobro en el webhook para poder calcular Ingresos/Ganancia real/LTV.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **REDISEÑO COMPLETO de identidad visual — de "Cuaderno de Sala" (claro, papel cálido) a "Sala de Pesas" (oscuro, verde eléctrico) — extraído del ebook del usuario y aplicado a TODA la app de un solo golpe gracias a la arquitectura de tokens CSS.**
- **Pedido explícito**: el usuario compartió `TRANSFORMACIÓNEN 90DIAS1.pdf` (su ebook, base original de la app) diciendo que la identidad actual era "genérica y aburrida para una app de ejercicio" y pidió extraer colores/diseño de ahí para algo "ganador y motivante... que en publicidad dé ganas de tenerla" — y luego pidió aplicarlo a TODA la app (onboarding, paywall, app interna) de forma uniforme antes de seguir con el panel de administrador.
- **Investigación real**: se instaló poppler (`winget install oschwartz10612.Poppler`, no estaba disponible) para renderizar las 61 páginas del PDF a imágenes e inspeccionarlas directamente (portada + páginas de contenido) — no se inventó nada de memoria. Patrón constante en TODO el ebook: fondo casi-negro azulado, tarjetas en verde militar oscuro, números/énfasis en verde lima eléctrico, tipografía redondeada muy bold (Poppins), fotografía de gimnasio oscura de alto contraste.
- **Cambio real (2 archivos concentran todo)**: `components/landing/tokens.css` (paleta completa: `--bg #12161c`, `--surface #1a2029`, texto claro, `--accent #97d131` lima eléctrico, `--accent-2 #5c7a1f` — el MISMO verde de la identidad anterior, ahora de apoyo, no descartado) y `app/layout.tsx` (fuente Instrument Sans → **Poppins**, la real del ebook). Como TODO el código ya usaba `var(--accent)`/`var(--bg)`/etc. sin un solo hex hardcodeado (auditado con grep en toda la app antes de tocar nada), el cambio de paleta cascadeó solo a onboarding, paywall, login, Plan de hoy, Historial, Perfil y landing — sin tener que tocar archivo por archivo.
- Verificado visualmente con Playwright (sin sesión, así que solo landing/onboarding/paywall/login — las pantallas de `/app` que exigen login se confirmaron por auditoría de tokens, no por captura visual real todavía): el verde lima sobre fondo oscuro se ve exactamente con la energía que el usuario pidió, muy distinto del "cuaderno" anterior.
- **Pendiente honesto, anotado (no oculto)**: los 4 screenshots reales de "La app por dentro" en la landing (`public/screenshots/frame-*.png`) se tomaron con la identidad ANTERIOR (clara) — quedan desactualizados contra el nuevo fondo oscuro. Hace falta una sesión con bypass de auth para re-capturarlos. También falta una pasada visual con sesión real autenticada en `/app/historial` y `/app/perfil` (se confirmaron por arquitectura de tokens, no por captura).
- `FICHA-ARTE.md` reescrita completa documentando la redecisión (fecha, motivo, qué se conserva del verde anterior, qué queda pendiente).
- Verificado: tsc ✓ · build ✓.
/ Siguiente acción exacta: publicar (push). Después: (1) re-capturar los 4 screenshots de la landing con el nuevo tema, (2) verificar con sesión real las pantallas de `/app`, (3) seguir con el panel de administrador — DEBE nacer ya con este brand kit nuevo, no con el anterior.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Todo el texto de párrafo de la app ahora se justifica (pedido explícito: "se ve desordenado... no sigue línea").**
- Regla global en `app/globals.css`: `p { text-align: justify; text-justify: inter-word; hyphens: auto; }`. Se combina con `hyphens: auto` (parte palabras largas) para que el justificado no deje huecos grandes entre palabras en columnas angostas de mobile — `lang="es"` ya estaba fijado en `app/layout.tsx`, así que el navegador usa el diccionario de silabeo correcto. Es una regla de UNA línea que aplica a TODA la app (no hubo que tocar archivo por archivo): títulos, labels y botones de una sola línea no se ven afectados (el justificado no hace nada en una sola línea).
- Verificado: tsc ✓ · build ✓ · confirmado en el navegador (tarjeta de onboarding, antes despareja a la derecha, ahora pareja en las 2 líneas completas).
/ Siguiente acción exacta: publicar. Después, análisis del ebook del usuario (`TRANSFORMACIÓNEN 90DIAS1.pdf`, 61 páginas, ya renderizado a PNG en el scratchpad con poppler recién instalado vía winget) para proponer una nueva dirección de arte completa — el usuario pidió explícitamente aplicarla de forma uniforme a TODA la app (onboarding, paywall, app interna) y después seguir con el panel de administrador. Esto es una REDECISIÓN deliberada de FICHA-ARTE.md a pedido directo del usuario (no una iniciativa unilateral de la IA) — sí hay que actualizar la ficha con la nueva identidad una vez definida.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Corregida la tarjeta "Tu ruta se está armando" del onboarding — el usuario mandó captura señalando que se veía descuadrada ("unas cosas a un lado, otras centradas").**
- Causa real: una decoración de "espiral de cuaderno" (6 puntos repartidos con `justify-between` en toda la altura de la tarjeta, posición absoluta) no seguía el ritmo del texto real al lado (que cambia de alto según cuántas filas hay) — los puntos quedaban sueltos, sin relación visual con las líneas de texto, y el padding izquierdo (`pl-9`) estaba pensado solo para darles espacio a ellos.
- Se quitó la espiral por completo (no era el dispositivo ownable protegido en FICHA-ARTE — ese sigue siendo el tachado verde + los chips de borde punteado) y se volvió el padding simétrico (`px-6` en vez de `pr-9 pl-9`).
- Verificado: tsc ✓ · build ✓ · confirmado en el navegador — el texto ahora respira parejo a ambos lados, sin elementos descuadrados.
/ Siguiente acción exacta: publicar (push) y esperar confirmación del usuario.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Barrido completo de botones tras reporte del usuario ("calcular macros quedó sin efecto") — revisión de TODOS los botones de la app, no solo los que yo recordaba haber tocado.**
- Se hizo un grep de `bg-[var(--accent)]` y `border-[var(--accent)]` en TODO `app/` y `components/` (no solo las pantallas ya tocadas) para encontrar botones reales sin ninguna de las 3 clases de profundidad. Faltaban: **guardar nombre** (ícono ✓ junto al input), **Calcular mis macros** y **Cerrar sesión** en Perfil; los 2 CTA de **Login** (enviar enlace de acceso, confirmar código); y **"Ver mi plan completo"** en `/onboarding/plan` (una pantalla del flujo que no había revisado en las rondas anteriores).
- **Decisión de criterio, anotada para no repetir la revisión de estos casos**: los botones circulares de SOLO ícono (atrás, cerrar el modal, reemplazar ejercicio, deshacer) y los enlaces de texto subrayado/sin fondo (Reintentar, ¿Cómo se hace?, Ahora no, Cancelar) se dejan A PROPÓSITO sin el canto 3D — es el mismo criterio que usa Duolingo (su X de cerrar y sus iconos de ajustes tampoco llevan profundidad, solo los botones/chips rectangulares y las tarjetas seleccionables).
- Verificado: tsc ✓ · build ✓. Publicado (push).
/ Siguiente acción exacta: esperar confirmación visual del usuario en su celular (con caché limpia). Si aparece algún botón más sin el efecto, aplicar la misma clase según si es de acento lleno (`.boton-3d`), de contorno con acento (`.boton-3d-borde`) o neutro (`.superficie-3d`) — no inventar una 4ª variante.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Profundidad 3D reforzada y extendida a TODA la app (pedido explícito tras probar en vivo: "sigo sin ver nada... hazlo más grueso y aplícalo a toda la app").**
- **Causa de "no veo nada"**: confirmado con un navegador limpio (sin caché ni sesión, vía Playwright) contra `gymevo-eta.vercel.app` — el canto 3D SÍ estaba publicado y renderizando (`getComputedStyle` mostró el `box-shadow` correcto). El usuario probablemente veía una versión en caché de su navegador/PWA. Se le explicó y se le mandó la captura de prueba.
- **Reforzado**: el canto de `.boton-3d` pasó de 4px a 6px (más parecido al volumen de Duolingo).
- **2 clases nuevas** para poder ser consistentes en TODA la app sin salirse de FICHA-ARTE: `.boton-3d-borde` (botones/chips de contorno con acento — se hunden con `aria-pressed`/`:active`) y `.superficie-3d` (tarjetas y botones neutros tocables, canto gris derivado de `--text-tertiary`, no un color nuevo).
- **Aplicado en TODA la app** (no solo los CTA principales de la ronda anterior): tarjetas de plan Anual/Mensual del paywall, los chips de opción del onboarding (nivel/meta/horario/sexo — componente `Chips` compartido), los 2 botones del modal de "¿salir del cuestionario?", el toggle lb/kg y el botón "Terminar entrenamiento de hoy"/celebraciones/aviso de descanso de Plan de hoy, el selector de duración de descanso, el CTA vacío de Historial, y el botón compartido `BotonCTA` de la landing (Hero/Oferta/CTA final/CTA flotante móvil). Se quitó `whileTap` de Framer Motion donde hacía falta (su `transform` inline pisaba el `:active` de CSS).
- **Fix técnico**: el toggle lb/kg de Perfil tenía `overflow-hidden` en su contenedor — recortaba el canto 3D. Se quitó y se separaron los 2 botones con `gap` en vez de bordes compartidos.
- Verificado: tsc ✓ · build ✓ · confirmado visualmente en el navegador (paywall con tarjetas y CTA, onboarding con los chips Hombre/Mujer) — el canto se ve claramente más grueso y presente en toda la pantalla, no solo en un botón aislado.
- **Nota del linter de diseño** (`post-edit-diseno.sh`, archivo `components/landing/ui.tsx`): sigue señalando 3 valores arbitrarios (`22px`, `1140px`, `17px`/`52px`) que YA EXISTÍAN antes de esta ronda (tamaño de ícono, ancho máximo del contenedor, tipografía/alto del botón) — no se tocaron hoy, están fuera del alcance de este pedido (profundidad 3D), y corregirlos sería un refactor no solicitado. Queda anotado para una futura pasada de pulido de esa librería compartida.
/ Siguiente acción exacta: publicar (commit + push) para que el usuario lo vea de verdad esta vez. Después, retomar el panel de administración.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **3 ajustes reales tras probar en vivo: reps por defecto fijas en 6, etiqueta de la unidad de peso en Perfil más clara, y la llama de racha ahora se llena/vacía según el progreso real de hoy — a pedido explícito del usuario.**
- **Reps por defecto**: `repsPorDefecto()` en `app/app/page.tsx` ya NO calcula el techo del rango del ejercicio (10-12 → 12) — ahora siempre inicia en `6`, editable por el usuario. El rango completo del desplegable sigue siendo 1-15.
- **Etiqueta de unidad de peso en Perfil**: "Registras el peso en" → **"Registrar peso de cada serie en:"** para que quede claro DÓNDE se usa esa preferencia (en el registro de cada serie del entrenamiento), no solo que existe.
- **Llama de racha con relleno real**: antes era binaria (llena si la racha no está en riesgo, contorno si sí) — ahora se llena progresivamente según `ejercicios de hoy ya marcados hechos ÷ total de hoy` con un `clip-path` animado (0-100%, mismo verde de marca), y se vacía si el usuario deshace un registro. No es decorativo: refleja el progreso real de la sesión de hoy, dato que ya existe en `progreso.hechosHoy`.
- **Aviso importante**: el usuario reportó "no veo nada en 3D" tras el checkpoint anterior — la causa es que esos cambios (y los 3 de hoy) **todavía no se han publicado** (`git status` mostraba todo como cambios locales sin commit ni push). Falta confirmar con el usuario antes de subir a producción.
- Verificado: tsc ✓ · build ✓. Pendiente: verificación visual en vivo tras publicar.
/ Siguiente acción exacta: confirmar con el usuario si publica ahora (commit + push) para que vea los cambios en su celular, luego retomar el panel de administración.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Profundidad 3D puntual (referencia Duolingo) en botones primarios e ícono activo del menú — a pedido explícito del usuario, preguntado y acotado con AskUserQuestion antes de tocar código.**
- El usuario mandó dos capturas de Duolingo pidiendo el look "3D con profundidad... donde sea posible en la app". Antes de construir se le avisó del cruce con la identidad visual ya aprobada (FICHA-ARTE.md, "cosa juzgada") y se preguntó alcance: eligió **NO reemplazar** el estilo "cuaderno" (tachado a mano, hairlines) — solo **sumar profundidad puntual** a la acción primaria de cada pantalla y al ícono activo del menú, sin tocar paleta ni tipografía. También eligió priorizar esto ANTES del panel de administración (pedido pendiente, ver checkpoint de abajo).
- Implementado con 2 clases CSS nuevas en `app/globals.css` (`.boton-3d`, `.chip-3d`): canto sólido debajo del elemento que se "hunde" al presionar (`box-shadow` + `translateY(4px)` en `:active`), usando los MISMOS colores de marca oscurecidos ~30% (`--accent-deep #3f5416`, `--accent-2-deep #6e4413`, nuevos en `components/landing/tokens.css`) — nunca un acento nuevo. `prefers-reduced-motion` respetado (sin transición).
- Aplicado a: CTA del paywall ("Empezar mis 7 días gratis"), los 2 CTA principales del onboarding ("Continuar", "Fijar mi meta"), el botón "Registrar serie" de Plan de hoy (solo su variante activa/acento — la variante de contorno sigue plana), y el ícono activo del menú inferior (`app/app/layout.tsx`, ahora en una ficha/chip con volumen en vez de solo cambiar de color). Se quitó `whileTap` de Framer Motion en esos botones porque su `transform` inline pisaba el `:active` de CSS y anulaba el efecto de "hundido".
- FICHA-ARTE.md actualizada con la enmienda (fecha, alcance exacto, qué NO cambió) para que quede trazada como decisión del usuario, no una redecisión unilateral de la IA.
- Verificado: tsc ✓ · build ✓ · confirmado en el navegador (paywall real en `/paywall`, `getComputedStyle` mostró el `box-shadow` exacto de `--accent-deep`). No se corrió `revisor-visual` fresco: es un utilitario CSS aplicado sobre pantallas ya revisadas (paywall/onboarding), mismo criterio que el cambio de ícono del onboarding de la sesión pasada — no redefine composición ni jerarquía. Sí conviene pedir veredicto fresco la próxima vez que cualquiera de esas pantallas se revise a fondo.
/ Siguiente acción exacta: si el usuario quiere más profundidad 3D en otras superficies (cards de historial, chips de meta, etc.), extenderla reusando las mismas 2 clases — NO inventar una tercera variante de sombra. Después de esto: retomar el panel de administración (ver checkpoint siguiente).

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Calculadora de macros reescrita con método científico completo (Mifflin-St Jeor + TDEE + ISSN), a pedido explícito del usuario ("investiga en la web como si fueras un científico y un nutricionista").**
- **Investigación real hecha antes de tocar código** (no de memoria): ecuación Mifflin-St Jeor para gasto en reposo (BMR) — la más precisa según Academy of Nutrition and Dietetics/ACSM, ~10% de error vs. 15-20% de fórmulas viejas; factores de actividad (PAL) estándar de fisiología del ejercicio; ISSN Position Stand on Protein and Exercise (Jäger et al. 2017) — 1.4-2.0 g/kg para ganar/mantener músculo, hasta 2.3-3.1 g/kg para proteger músculo en déficit; ISSN para carbohidratos (3-5 g/kg en fuerza general) y grasas (20-35% de las calorías).
- **Antes**: los macros se calculaban con gramos/kg fijos y las calorías se sumaban desde ahí (aproximación simple). **Ahora**: se calcula primero el gasto calórico REAL de cada persona (BMR con peso+estatura+edad+sexo, luego TDEE según cuántos días entrena) y los macros se derivan de ahí — específico de cada usuario, no un promedio.
- **2 decisiones de alcance consultadas al usuario antes de construir** (correctamente, por ser recolección de datos personales nuevos, no una decisión técnica interna): (1) agregar estatura+edad en Perfil para el cálculo completo — eligió que SÍ; (2) cómo resolver el sexo biológico que pide la fórmula (afecta el resultado hasta 15%) — eligió preguntarlo en vez de promediar.
- **El sexo se pregunta en ONBOARDING** (a instrucción explícita del usuario, no en Perfil como el resto de estos datos) — nuevo primer paso del cuestionario, "Hombre"/"Mujer", con su propia `TarjetaBeneficio` explicando honestamente para qué se usa. `lib/onboarding.ts` gana el tipo `Sexo` y el campo en `RespuestasOnboarding`.
- **Estatura y edad se preguntan en Perfil**, junto al peso que ya se pedía — `Progreso` gana `estaturaCm`/`edad` (nullable, igual que `pesoKg`), migración `0010_estatura_edad.sql` aplicada (`profiles.estatura_cm`, `profiles.edad`).
- `lib/macros.ts` reescrito por completo: `calcularBMR()`, `factorActividad()` (deriva el PAL de `diasSemana`, ya conocido — no se pregunta aparte), `calcularMacros(datos)` ahora recibe un objeto con los 6 datos necesarios en vez de solo peso+meta. Grasas con piso de seguridad (0.6 g/kg), carbohidratos como remanente de calorías (nunca negativo, con piso de seguridad también), y las calorías nunca bajan del BMR sin importar qué tan agresivo sea el déficit.
- Verificado con Playwright y matemática real a mano: hombre, 78kg, 178cm, 26 años, 5 días/semana, Ruta A → BMR 1767.5 → TDEE 2739.6 → 2965 kcal/día · 133g proteína · 424g carbohidratos · 82g grasas — coincide exacto con el cálculo manual. tsc ✓ · eslint ✓ · build ✓.
/ Siguiente acción exacta: publicar (push a main). Pendiente de sesiones futuras: módulo de nutrición completo (guía de porciones, suplementos) y módulo "Aprende" del ebook — siguen sin construir.

⏸️ CHECKPOINT — Última acción completada (03/09/2026): **Precio en pesos colombianos (TRM oficial del día) junto al de dólares, en landing y paywall — a pedido explícito del usuario ("vamos a hacer mayor venta en Colombia... evitar fuga de clientes al ver dólares").**
- `app/api/trm/route.ts` (nuevo): pide la TRM del día a la fuente pública oficial del gobierno colombiano (`datos.gov.co`, dataset del Banco de la República, sin API key) y la cachea 1 hora (la TRM solo cambia una vez al día). Si la fuente falla, devuelve `valor: null` — nunca tumba la página por esto.
- `lib/trm.ts` (nuevo): `useTRM()` (hook que pide la TRM una vez), `formatearCOP(precioUSD, trm)` (formatea con `Intl.NumberFormat('es-CO', {style:'currency', currency:'COP'})`, sin decimales).
- `components/landing/Oferta.tsx` y `app/paywall/page.tsx`: cada precio en dólares ahora muestra debajo "≈ $X.XXX" en pesos colombianos, calculado con la TRM real. Si la TRM no cargó todavía o falló, esa línea simplemente no aparece — el precio en USD sigue siempre visible, la conversión es un añadido, nunca un bloqueo.
- Verificado en vivo: la API devolvió la TRM real del día (3140.55 al momento de la prueba) y los montos en pantalla coinciden con la multiplicación correcta ($2.50 → ≈$7.851, $4.99 → ≈$15.671). tsc ✓ · eslint ✓ · build ✓. Sin errores de consola.
- **Decisión técnica**: se aplicó tanto en landing como en paywall (el usuario dijo "al momento de la compra", que es más específicamente el paywall, pero el mismo riesgo de fuga de clientes existe desde la landing, que es donde el precio en USD se ve por primera vez) — decisión propia informada, no se le preguntó al usuario por ser una extensión de alcance técnica menor y directamente alineada con el objetivo que dio.
/ Siguiente acción exacta: publicar (push a main) para que el usuario lo vea en producción. Pendiente: decidir si el paywall se retoma con una idea de identidad visual distinta (ver checkpoint anterior, 10 rondas) o se deja como está.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Paywall — 10 rondas en total hoy. PARADO por recomendación explícita del propio revisor-visual tras confirmar rendimiento decreciente.** El usuario había elegido "opción 2" (dispositivo ownable nuevo, sin tocar la paleta) para las últimas 2 rondas.
- **Rondas 9-10** (dispositivo ownable nuevo): en vez de inventar un elemento decorativo nuevo (falló 2 veces con la espiral), se reutilizó el tachado verde que YA funciona en el resto de la app, aplicado al precio del plan "Anual" ($4.99 tachado → $2.50) — es además una técnica real de conversión (ancla de precio), no solo decorativa. Se reforzó su peso visual (12px gris tenue → 16px bold, línea más gruesa) tras la primera pasada. Se restauró también el borde punteado en la tarjeta no seleccionada — FICHA-ARTE.md (líneas 13/29) documenta esto como el SEGUNDO dispositivo ownable oficial ("chips de meta con borde fino punteado"); una ronda anterior lo había unificado a borde sólido pensando que era una inconsistencia, cuando en realidad era lo correcto y esa "corrección" estaba equivocada.
- **Resultado**: 34/40 usabilidad · 14/20 craft (identidad 2/4, sin más movimiento) · 19/20 copy. Sigue sin cruzar el gate (≥36/40, ≥16/20).
- **Diagnóstico final y textual del revisor, ronda 10**: *"el precio tachado es un patrón de industria (Duolingo, Noom, BetterMe), no una firma reconocible de esta app... si esta ronda tampoco cruza el gate, la recomendación es parar de iterar el paywall hoy: 34/40 usabilidad y 19/20 copy ya son sólidos para vender, y seguir puliendo el eje de identidad con ajustes marginales tiene rendimiento decreciente — mejor invertir el tiempo en otra pantalla y volver al paywall con una idea de identidad distinta, no la número 4 sobre el mismo concepto."*
- **Total de la sesión de hoy en esta pantalla**: 10 rondas, TODOS los bugs reales de comportamiento/accesibilidad cerrados (ver checkpoint anterior — cancelar durante espera, contraste AA, memoria de plan, viuda tipográfica, etc.), copy en 19/20 (sobresaliente). Lo único que no cruza es el eje de identidad visual/craft, y ya no es un problema de ejecución sino de CONCEPTO: hace falta una idea de dispositivo ownable distinta a "precio tachado", o aceptar el estado actual y compensar en otra pantalla.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en las 10 rondas. Sin errores de consola.
/ Siguiente acción exacta: NO seguir iterando el paywall hoy (instrucción explícita del propio revisor). Preguntarle al usuario si quiere: (a) dejar el paywall como está (usabilidad 34/40 es funcional, copy 19/20 vende bien) y seguir con otra prioridad, o (b) programar una sesión futura con una idea de identidad GENUINAMENTE distinta (no otra variación de precio tachado / bordes / grosor de línea).

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Paywall — 8 rondas de diseño dedicado en un solo día (a pedido explícito: "arreglemosla ahora que tenemos tiempo"). Se corrigieron TODOS los bugs reales de comportamiento y accesibilidad; lo que queda es una decisión de identidad, no más código. PARADO aquí, tal como recomendó el propio revisor-visual en la ronda 8.**
- **Bugs reales corregidos en el camino** (estos SÍ quedan cerrados, no son "problema conocido"):
  - Redirección a Hotmart sin aviso (objeción de confianza del avatar) → spinner + mensaje antes de salir.
  - `prefers-reduced-motion` no se respetaba en ninguna animación → corregido en todas.
  - Anglicismo "1 tap" → "un toque".
  - "Restaurar compra" prometía una acción automática que no existe → renombrado a lo que hace de verdad.
  - Sin memoria del plan elegido (anual/mensual) entre visitas → persistido en localStorage, verificado con Playwright.
  - Spinner eterno si la red falla o un bloqueador de popups frena la redirección → aviso real a los 2.5s + botón "Cancelar" que de verdad cancela los timers pendientes (no solo oculta la UI).
  - Banner de error con contraste real de accesibilidad reprobado (~2.67:1, bajo el mínimo AA) → texto movido a `--text-primary`, el color de aviso se queda en ícono/borde.
  - Botón principal con contraste de texto insuficiente (~4.4:1 en texto de 16.5px, bajo AA) → subido a `text-xl font-bold`, cruza el umbral de "texto grande" con margen.
  - Viuda tipográfica en el titular ("listo" solo en su línea) → espacio irrompible entre "está" y "listo".
  - Borde `dashed` sin precedente en el resto de la app (PlanCard "Mensual") → unificado a borde sólido.
- **Intento de dispositivo ownable (espiral de encuadernación) — RETIRADO tras 2 rondas fallidas**: primero invisible (bajo contraste), después visible pero mal alineado con los nodos reales del timeline (alturas de texto desiguales). Se decidió retirarlo en vez de seguir parchándolo — un timeline limpio es mejor que un rasgo de marca que se ve roto. Documentado explícitamente para que una futura sesión no reintente la misma idea sin resolver el problema de fondo (alinear un sistema decorativo a un layout de alturas variables).
- **PROBLEMA CONOCIDO — el paywall no cruza el gate del revisor-visual y NO es un bug de código**: 8 rondas dejaron el puntaje en 33/40 usabilidad · 13/20 craft · 19/20 copy (copy ya sobresaliente). El diagnóstico final y textual del revisor-visual: *"los defectos que quedan ya no son de bug puntual, son de decisión de identidad/sistema (paleta cerca del combo vetado 'papel cálido + tinta verde', dispositivo ownable ausente, jerarquía de tamaños sin consolidar): parar hoy y reportarle al usuario el estado real, porque otra ronda de parches puntuales no va a mover esto — necesita una decisión de diseño, no otro ajuste de CSS."* Esta es la MISMA tensión de paleta ya señalada (y aceptada como no bloqueante) para landing y onboarding en sesiones anteriores — es sistémica de FICHA-ARTE.md, no exclusiva del paywall.
- **Las 2 opciones reales para desbloquear esto, ninguna es "otro ajuste de código"**: (a) el usuario aprueba redecidir la paleta base de FICHA-ARTE.md (afecta TODA la app, no solo esta pantalla — es "cosa juzgada", requiere su aprobación explícita), o (b) invertir una sesión de diseño dedicada a un dispositivo ownable nuevo y bien resuelto (no un parche de 30 minutos como el espiral). Pendiente de que el usuario decida — no relanzar el revisor-visual sin una de estas dos.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en cada una de las 8 rondas. Sin errores de consola. Veredicto completo en `docs/revisiones/paywall-veredicto.md`.
/ Siguiente acción exacta: comunicarle al usuario el balance real (bugs cerrados vs. decisión de identidad pendiente) y esperar su elección entre (a) redecidir paleta o (b) sesión de diseño dedicada — o simplemente seguir sin el gate cruzado si decide que no vale la pena por ahora.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Paywall — 4ta ronda de diseño (a pedido explícito: "seguir con paywall"). Craft subió a 15/20 (1 punto del gate), usabilidad se mantiene en 30/40 — diagnóstico claro de por qué, documentado abajo como "Problema conocido" para no reabrir esta discusión sin evidencia nueva.**
- **Ronda 3** (dispositivo ownable + jerarquía): se agregó la espiral de encuadernación al timeline del trial (mismo dispositivo de `TarjetaRuta` en onboarding) y se fusionó la mini-FAQ con un eyebrow propio ("Preguntas frecuentes"). Resultado: 30/40 · 14/20 — el dispositivo se veía "demasiado sutil" según el revisor.
- **Ronda 4**: (a) la espiral pasó a círculos más grandes con anillo ámbar (mucho más visible); (b) los 3 bullets de confianza se fusionaron DENTRO de la misma tarjeta que la FAQ bajo un solo eyebrow ("Antes de empezar") — de 2 bloques consecutivos de peso idéntico a 1 bloque con jerarquía interna; (c) el borde `dashed` del plan "Mensual" (sin precedente en el resto de la app) se unificó a borde sólido. Resultado: 30/40 usabilidad · **15/20 craft** (cruza el mínimo de craft por primera vez) · 17/20 copy.
- **Además, en esta misma ronda** (sin re-evaluar todavía): se agregó persistencia del plan elegido (`localStorage`, sobrevive a cerrar y volver — verificado con Playwright) y un estado de error real si la redirección a Hotmart no ocurre en 4 segundos (antes: spinner eterno sin salida si fallaba la red).
- **Diagnóstico honesto del revisor, palabra por palabra**: *"La usabilidad tiene una brecha real de 6 puntos que NO se cierra con otro retoque visual: los defectos de esta ronda fueron todos de craft (identidad, jerarquía de bloques, borde), ninguno tocó heurísticas de usabilidad... Para llegar a 36/40 hace falta trabajo adicional real (estado de error, persistencia, densidad general), no un quinto ajuste cosmético."* Los dos fixes de comportamiento de esta ronda (persistencia + error de red) responden directo a esa brecha, pero no se han vuelto a medir con el revisor.
- **PROBLEMA CONOCIDO — paywall no cruza el gate del revisor-visual** (usabilidad 30/40 < 36, craft 15/20 < 16 por 1 punto): 4 rondas de corrección real (confianza en el checkout externo, reduced-motion, copy, dispositivo ownable, jerarquía de bloques, persistencia de plan, manejo de error de red) sin cruzar el umbral doble. La brecha de usabilidad restante es la MISMA tensión de identidad/paleta ya aceptada como no-bloqueante para landing en una sesión anterior (papel cálido + tinta verde, cerca del patrón vetado "Capítulo" del banco canónico) — cambiarla de raíz requiere aprobación explícita del usuario para redecidir FICHA-ARTE.md (cosa juzgada), no es un ajuste de esta sesión. Pospuesto por decisión de alcance: se avisó al usuario del diagnóstico y las opciones (más rondas de comportamiento/densidad, o redecidir paleta) — pendiente de su respuesta.
- Verificado: tsc ✓ · eslint ✓ · build ✓. Sin errores de consola. Veredicto completo en `docs/revisiones/paywall-veredicto.md`.
/ Siguiente acción exacta: esperar la decisión del usuario sobre el paywall (seguir iterando comportamiento/densidad, redecidir paleta, o pausar aquí) antes de volver a lanzar el revisor-visual — no relanzarlo sin un cambio real que justifique una nueva pasada.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Registro real serie por serie + libras + aviso de descanso entre ejercicios + Perfil renombrado, a pedido explícito del usuario.**
- **Registro por serie**: antes un solo tap con un peso registraba las 4 series de golpe (aproximación de la Sesión 5). Ahora cada serie se registra por separado — el peso puede cambiar de una serie a otra (ej. bajar carga al final), se ve "Serie X de 4" en cada ejercicio, y el botón dice "Registrar serie N" (o "Registrar última serie" en la 4ª). El ejercicio se marca "Hecho" solo al completar su última serie. `lib/routine.ts`: nueva `seriesHechasHoy(progreso, ejercicioId)` (cuenta los logs de hoy para ese ejercicio); cada log ahora representa UNA serie (`series: 1`), no un agregado.
- **Unidad de peso (kg/lb)**: se agregó `Progreso.unidadPeso` (`profiles.unidad_peso`, migración `0009_unidad_peso.sql`, default `'lb'` a pedido del usuario — "no siempre se sube peso en kilos") con un selector LB/KG en Perfil. El placeholder del input de peso, el Historial (gráfico, tabla, detalle por sesión) y el dato héroe de volumen ahora muestran la unidad elegida, no "kg" fijo. No se mezcla con `pesoKg` (el peso CORPORAL para las macros, siempre en kg porque las fórmulas del ebook son por kg — son dos cosas distintas).
- **Aviso de descanso ENTRE ejercicios** (distinto del cronómetro entre series, que ya existía): al terminar la última serie de un ejercicio aparece un modal informativo — "Descansa mínimo 3 minutos antes de tu siguiente ejercicio... Tú decides cuándo seguir" — SIN cronómetro, a pedido explícito del usuario (la decisión de cuánto esperar es de cada quien). Solo un botón "Entendido".
- **Perfil**: la tarjeta "Detalles del onboarding" se renombró a "Tu perfil de entrenamiento" (nombre interno de implementación, no debía verlo el usuario) — ahí mismo vive ahora el selector de unidad.
- **Historial**: como cada log es ahora una serie individual, el detalle por sesión agrupa las series de un mismo ejercicio en una sola fila mostrando el peso de cada una (ej. "4 series · 140 / 140 / 135 / 135 lb") en vez de una fila repetida por serie.
- Verificado con Playwright: flujo completo de 4 series de un ejercicio (timer entre series, indicador "Serie X de 4", botón "Registrar última serie", modal de aviso al terminar sin cronómetro), Historial agrupado en lb, y el toggle LB/KG persistiendo en localStorage. tsc ✓ · eslint ✓ · build ✓. Sin errores de consola.
/ Siguiente acción exacta: publicar (push a main) para que el usuario vea los cambios en su celular real, y retomar la decisión pendiente sobre paywall (ronda de diseño vs. seguir con Hotmart).

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **🐛 Bug real en producción encontrado y corregido: Historial se rompía para cuentas con entrenamientos registrados ANTES del reemplazo del catálogo de ejercicios.**
- El usuario reportó "cuando entro a historial sigue sin mostrar página" en su celular real, después de que se publicaran los cambios de esta sesión (rutina real del ebook). Causa raíz: al reemplazar por completo el catálogo de ejercicios (ids nuevos como `sentadilla_barra` en vez de los viejos `sentadilla`/`press_banca`/etc.), `obtenerEjercicio(id)` en `lib/routine.ts` devolvía `undefined` para cualquier registro (`workout_logs`) guardado con un id VIEJO — y `app/app/historial/page.tsx` explotaba al intentar leer `.nombre` de `undefined`. Afecta a CUALQUIER cuenta con historial de antes de hoy, incluida la cuenta real de prueba del usuario.
- **Corregido**: `obtenerEjercicio()` ahora devuelve un ejercicio de respaldo ("Ejercicio anterior") en vez de `undefined` cuando el id no existe en el catálogo actual — el historial viejo se sigue viendo (con nombre genérico para lo que ya no existe), nunca rompe la pantalla. Reproducido y verificado con Playwright: se sembraron logs con ids viejos (`press_banca`, `sentadilla`) + uno que sigue existiendo (`remo_barra`) — sin el fix habría reventado, con el fix se ve todo correctamente, sin errores de consola.
- **Lección para la próxima vez que se reemplace un catálogo de datos**: cualquier función que resuelva un id desde datos históricos persistidos (logs, reemplazos guardados, etc.) necesita un valor de respaldo — nunca asumir que todos los ids futuros van a seguir existiendo en el catálogo vigente.
- Verificado: tsc ✓ · eslint ✓ · build ✓. Publicado a producción (push a main → Vercel).
/ Siguiente acción exacta: confirmar con el usuario que Historial ya carga bien en su celular real. Después, retomar la decisión pendiente: ¿ronda de diseño al paywall, o seguir con Hotmart (#5)?

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Paywall — 2 rondas de correcciones reales, sigue NO LISTA (pendiente #4 de la secuencia que pidió el usuario).**
- **Ronda 1**: se corrigió el hallazgo de mayor peso — `empezarTrial()` saltaba en silencio (`window.location.href`) al checkout externo de Hotmart sin ningún aviso, justo en el momento del pago, para un avatar cuya objeción #1 es "¿es otra app con cobros ocultos?". Ahora el botón se deshabilita y muestra un spinner + "Te llevamos a Hotmart, pago seguro…" antes de redirigir. Se agregó `useReducedMotion()` a TODAS las animaciones de entrada (antes ninguna lo respetaba). Se corrigió el anglicismo "Cancela en 1 tap" → "Cancela con un toque". Resultado: 30/40 usabilidad · 13/20 craft · 17/20 copy (sin cambio en usabilidad/craft — los defectos que realmente pesan son estructurales, ver abajo).
- **Ronda 2**: se agregó una línea que reconoce el dolor real del avatar antes del alivio ("Se acabó adivinar qué máquina usar", encima del headline — FICHA-AVATAR marca esto como ancla obligatoria y el headline saltaba directo a la solución); se acortó el delay del spinner de 350ms a 200ms (menos fricción en redes lentas); "Restaurar compra" (que solo abre un correo a soporte, no restaura nada automático) se renombró a "¿Ya pagaste? Escríbenos" — evita prometer una acción que el botón no hace.
- **Defectos estructurales que NO se resolvieron y son los que realmente bloquean el gate** (mismo patrón que se documentó para landing/onboarding en sesiones anteriores): (1) sin dispositivo ownable propio en esta pantalla — la paleta papel-cálido+tinta-verde sigue cerca del patrón vetado "Capítulo" del banco canónico; (2) el bloque inferior (bullets + mini-FAQ + trust row) tiene 3 secciones con pesos tipográficos casi idénticos, aplanando la jerarquía. Corregir esto de verdad implica trabajo de diseño (no un ajuste de copy/código puntual) — mismo tipo de esfuerzo que le tomó a onboarding varias rondas dedicadas para el problema de densidad.
- Verificado: tsc ✓ · eslint ✓ · build ✓ · sin errores de consola en las 2 rondas. Veredicto completo en `docs/revisiones/paywall-veredicto.md`.
/ Siguiente acción exacta: preguntarle al usuario si quiere invertir una ronda de diseño dedicada en el paywall (dispositivo ownable + jerarquía del bloque inferior) o seguir con el pendiente #5 (conexión con Hotmart, que de todos modos requiere que el usuario cree el producto primero).

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **🎉 ONBOARDING declarado LISTA — 36/40 usabilidad, 16/20 craft.** Cierre de la maratón de rondas de esta pantalla (venía de 33/40 · 13/20 al empezar hoy). El usuario pidió seguir "en orden": primero onboarding (#3 de los pendientes), luego paywall (#4), luego Hotmart (#5) — este checkpoint cierra el #3.
- Se fusionó `TarjetaBeneficio` (era una tarjeta aparte) DENTRO de `TarjetaRuta` como un prop opcional `beneficio` — resuelve la densidad que venía señalando el revisor hace 2 rondas (2 chips + 2 tarjetas apiladas). Se agregó `whileTap` a los CTA primarios "Continuar"/"Fijar mi meta" (antes solo los chips daban feedback táctil). Conteo ascendente real (1→N con `animate()`, 0.6s) en el número de días del paso "compromiso", antes solo cruzaba-desvanecía entre valores del slider. Y el movimiento decisivo: **trampa de foco real en el modal "¿Salir sin terminar?"** (Tab/Shift+Tab ciclan solo entre sus 2 botones, verificado con Playwright en ambas direcciones) — cerró el último punto de usabilidad que faltaba (35→36/40).
- Defectos menores que quedan (NO bloquean, para una futura ronda de pulido si se retoma esta pantalla): jerarquía con 4 tamaños tipográficos en vez de 3; la paleta base (papel cálido + tinta verde) sigue siendo el punto más frágil frente al patrón vetado "Capítulo" del banco canónico (documentado y aceptado en rondas anteriores — tipografía y dispositivo ownable evitan el clon exacto); `/onboarding/generando` no avisa si `sessionStorage` falla (incógnito estricto); el borde punteado de chips no-seleccionados podría leerse como deshabilitado.
- Verificado: tsc ✓ · eslint ✓ · build ✓. Veredicto completo en `docs/revisiones/onboarding-veredicto.md`.
/ Siguiente acción exacta: seguir con el pendiente #4 — relanzar el revisor-visual de PAYWALL (su código de cobro real cambió después del último veredicto LISTA, así que ese veredicto quedó caducado de verdad, no es falso positivo del hook).

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Ruta A/Ruta B + calculadora de macros (ebook cap. 3), a pedido explícito del usuario.**
- Decisión clave para no inflar el alcance: **Ruta A y Ruta B YA EXISTÍAN conceptualmente** — son exactamente el campo `meta` del onboarding (`musculo`/`grasa`) que ya se le pregunta al usuario desde la Sesión 4. No hubo que agregar ningún paso nuevo al onboarding (evita tocar una pantalla ya frágil, con varias rondas de revisor-visual encima). Solo faltaba el DATO que la calculadora necesita y no se pedía nunca: el peso corporal.
- `lib/macros.ts` (nuevo): `calcularMacros(pesoKg, meta)` — usa los gramos/kg por ruta que da el ebook (Ruta A: proteína 1.6-2.0, carbohidratos 3-5, grasas 0.7-1 g/kg; Ruta B: proteína 1.8-2.4, carbohidratos más ajustados, grasas 0.6-0.8 g/kg — se tomó el punto medio de cada rango) y calcula las kcal SUMANDO los tres macros (4/4/9 kcal por gramo) — así el usuario nunca tiene que calcular su gasto calórico a mano, coherente con el "no necesitas contar calorías" del ebook.
- `profiles.peso_kg` (columna nueva, migración `0008_peso_corporal.sql`, aplicada) + `Progreso.pesoKg` (nuevo campo, `null` hasta que el usuario lo ingresa — nunca se le pide en el onboarding, solo cuando quiere ver sus macros) + sincronizado en `leerProgresoRemoto`/`guardarProgresoRemoto` igual que el resto de `Progreso`.
- **Perfil** (`app/app/perfil/page.tsx`): nueva tarjeta "Tus macros · Ruta A/Ruta B" — input de peso (kg) + botón "Calcular" → muestra kcal/día (dato héroe) + proteína/carbohidratos/grasas en gramos. Es una pantalla secundaria (no una de las 4 del dinero), así que no le corresponde el revisor-visual obligatorio — se verificó a mano.
- Verificado con Playwright a 375px: 78 kg + Ruta A → 2402 kcal · 140g proteína · 312g carbohidratos · 66g grasas; el mismo peso en Ruta B → 1931 kcal · 164g proteína · 195g carbohidratos · 55g grasas (más proteína, menos carbohidratos y menos calorías totales, tal como describe el ebook). tsc ✓ · eslint ✓ · build ✓. Sin errores de consola.
- **Pendiente reconocido, NO corregido esta ronda**: (1) el módulo de nutrición completo (guía de fuentes de alimento por macro, guía de porciones "sin contar calorías", suplementación con creatina/proteína/cafeína) sigue sin existir — esto era la calculadora sola, que fue lo que el usuario pidió explícitamente ("opción 1"); (2) el módulo "Aprende" (mitos, pilares, expectativas por mes) tampoco existe; (3) no hay ninguna advertencia si el usuario no ha puesto su peso — la tarjeta simplemente invita a calcularlo, sin bloquear nada.
/ Siguiente acción exacta: preguntarle al usuario si quiere seguir con el módulo de nutrición completo o el módulo "Aprende" — son los dos pendientes que quedaron de la sesión anterior.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Rutina real cargada — el catálogo de ejercicios de relleno fue reemplazado por el programa completo de 90 días del ebook del usuario ("Transformación en 90 días"), con las imágenes REALES del ebook (no dibujos propios).**
- El usuario pegó su ebook completo (PDF) + un prompt de especificación funcional muy detallado. Antes de tocar código se le preguntó por 3 decisiones grandes (tema oscuro vs. claro, si tenía el PDF de ilustraciones, y el alcance de la sesión) — cerró el diálogo sin responder y en su lugar mandó el PDF directo, así que se interpretó como "usa esto ahora" y se decidió con el mejor criterio: **se mantuvo el tema claro ya aprobado** (cosa juzgada de FICHA-ARTE, no se tocó) y **el alcance de esta sesión se limitó a la rutina de entrenamiento** (nutrición, suplementos y contenido educativo del ebook quedan para otra sesión — ver pendientes abajo).
- **Extracción de imágenes reales**: el PDF trae 93 imágenes JPEG incrustadas; se extrajeron con un script (sin herramientas de PDF instaladas, se buscaron los marcadores JPEG a mano con Node). 35 de esas imágenes son las láminas de ejercicio del programa (cada ejercicio ya viene como una tarjeta individual con técnica + músculos + consejo — no hubo que recortar nada) + 2 láminas de calentamiento (tren superior/inferior, cada una con sus 6 sub-ejercicios). Copiadas a `public/explicaciones/` con nombres en kebab-case.
- **`lib/routine.ts` reescrito por completo**: el modelo pasó de una rotación de 4 días (empuje/tirón/piernas/full, 3 ejercicios c/u — contenido de relleno) a un **calendario semanal FIJO lunes-sábado + domingo descanso**, tal como el ebook (`DiaSemana` reemplaza a `TipoDia`). 33 ejercicios reales del programa, cada uno con su imagen (`imagenExplicacion` ya no es opcional en la práctica: los 33 la tienen), 4 series × 10-12 reps (algunos accesorios 8-12), tempo real del ebook (3-1-1 compuestos, 2-1-1 aislados, isométrico para plancha). El ORDEN de cada día es el que trae el ebook y sigue el criterio correcto de entrenamiento (músculo grande/compuesto primero, aislado después — ej. pecho antes que tríceps, press plano antes que aperturas) — documentado en un comentario para que no se reordene sin ese criterio.
- Nuevo: `calentamientoDeHoy()` (imagen de calentamiento tren superior/inferior según el día), `cardioDeHoy()` (HIIT o Zona 2 con duración, informativo — no pasa por el flujo de series/peso), `esDiaDeDescanso()` (domingo).
- **`app/app/page.tsx`**: agrega la lámina de calentamiento arriba de la lista de ejercicios, una tarjeta de cardio al final (solo informativa), y una pantalla dedicada de "día de descanso" los domingos (sin cronómetro ni lista de ejercicios). **Bug real corregido de paso**: el registro de series solo tenía un input de "kg" y calculaba las repeticiones con `Number(ej.reps)` — con los rangos reales del ebook ("10-12") eso daba `NaN → 0` y el volumen del Historial se habría quedado en 0 para siempre. Se agregó un input real de "repeticiones hechas" (prellenado con el techo del rango, editable).
- Verificado con Playwright a 375px: lunes (pierna, calentamiento tren inferior, 7 ejercicios en orden correcto), miércoles (espalda-bíceps-hombro, calentamiento tren superior, 9 ejercicios), domingo (pantalla de descanso, sin crash), y el modal "Explicación del ejercicio" mostrando la imagen REAL (antes mostraba la silueta genérica de respaldo). tsc ✓ · eslint ✓ · build ✓.
- **Pendiente reconocido, NO corregido esta ronda** (fuera del alcance decidido): (1) Ruta A/Ruta B (ganar músculo vs. bajar grasa) con calculadora de macros — hoy sigue usando el sistema nivel/meta existente, no distingue las dos rutas del ebook; (2) módulo de nutrición (macros, guía de porciones, suplementos) — no existe todavía; (3) módulo "Aprende" (mitos, pilares, expectativas por mes) — no existe todavía; (4) la pantalla de "día de descanso" es funcional pero visualmente pobre (mucho vacío, sin revisor-visual — es pantalla nueva y secundaria, no una de las 4 del dinero); (5) la tarjeta de cardio es solo informativa, no se puede "marcar como hecho" ni registrar duración real; (6) las alternativas ("Cambiar ejercicio") se remapearon a otro ejercicio DENTRO del catálogo de 33 (mismo músculo/patrón) en vez de la tabla original del prompt (que pedía máquinas que no tienen imagen en el ebook, ej. "sentadilla goblet") — documentado en el código, funciona pero no es 1:1 con la tabla del prompt.
/ Siguiente acción exacta: si el usuario quiere seguir, la secuencia natural es (a) Ruta A/B + calculadora de macros en el onboarding, (b) módulo de nutrición, (c) módulo educativo. Cada uno es su propia sesión de tamaño considerable — no intentar los tres de una vez.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Onboarding — se atacó por fin el defecto de mayor peso que quedaba pendiente de la sesión de pulido anterior: el ~40-45% de vacío bajo "Tu ruta se está armando" en los pasos cortos (nivel/meta).** El revisor había sido explícito: "esto exige rediseño real de contenido, no un ajuste de estilo" — así que se agregó CONTENIDO, no otro cambio de espaciado.
- Nuevo componente `TarjetaBeneficio` (`app/onboarding/page.tsx`) — ícono + un recordatorio CONCRETO y VERDADERO de algo que la app ya resuelve, mostrado debajo de los chips en los pasos "nivel" ("cada ejercicio trae la técnica explicada") y "meta" ("el Botón de Rescate te da otro ejercicio al instante si la máquina está ocupada"). A propósito NO promete personalización que el catálogo de ejercicios no tiene todavía (el catálogo es igual para todos los niveles/metas hoy, solo cambia una etiqueta de texto) — honestidad primero, aunque eso sea una limitación de producto conocida y fuera de alcance de esta tarea.
- Verificado visualmente con Playwright (headless, sin el problema de `document.hidden` pausando animaciones que tiene la herramienta de navegador de esta sesión): ambos pasos (nivel y meta) muestran los chips + la nueva tarjeta de beneficio + "Tu ruta se está armando", con mucho menos vacío al fondo. Capturas en `docs/revisiones/onboarding-375.png` (nivel) y `docs/revisiones/onboarding-meta-375.png` (meta).
- tsc ✓ · eslint ✓ · build ✓.
**Resultado del revisor-visual**: 34/40 usabilidad, **17/20 craft (¡YA PASA!)** — confirmado explícitamente: "el defecto que exigía rediseño real (vacío de fondo) quedó resuelto — la TarjetaBeneficio es la corrección correcta y no hace falta tocarla de nuevo". Solo falta usabilidad (le faltan 2 puntos), con defectos de corrección rápida:
- **Corregido en esta ronda**: el botón "Salir" (X) cerraba el cuestionario SIN avisar, perdiendo las respuestas ya dadas de golpe — era el defecto de mayor peso señalado. Ahora: si es el primer paso (nada que perder) sale directo; si ya respondió algo, aparece un modal de confirmación ("¿Salir sin terminar? Vas a perder lo que respondiste hasta ahora") con opción de seguir o salir. Verificado con Playwright: paso 1 sale directo, paso 2+ muestra el modal correctamente.
- Pendientes menores identificados (no corregidos esta ronda, quedan para si se sigue puliendo): inconsistencia de que "frustración"/"horario" no llevan `TarjetaBeneficio` como sí la llevan "nivel"/"meta" (decisión consciente: esos pasos ya tienen 4 chips y no sufren el vacío); posible densidad excesiva de apilar `TarjetaBeneficio`+`TarjetaRuta` bajo una decisión de 2 chips; el slider nativo de "compromiso" no se verificó visualmente esta ronda; el piso de 8% de la barra de progreso podría notarse poco entre los pasos 1 y 2.
- tsc ✓ · eslint ✓ · build ✓ tras la corrección del botón Salir.

**Ronda siguiente (02/09/2026), tras veredicto 33/40 usabilidad · 16/20 craft (craft sigue pasando, usabilidad sigue faltando 3 puntos) — se corrigieron los 3 defectos de mayor peso que señaló esa ronda:**
- Modal "¿Salir sin terminar?": ya tiene `role="dialog"`, `aria-modal="true"`, `aria-labelledby` apuntando al `<h2>`; foco automático al botón seguro ("Seguir aquí", nunca el destructivo) al abrirse; tecla Escape lo cierra; el foco vuelve al botón X que lo abrió al cerrarse (por Escape o por click fuera). Verificado con Playwright headless: foco en "Seguir aquí" al abrir, atributos ARIA presentes, Escape cierra, foco vuelve a "Salir del cuestionario" — captura en `docs/revisiones/onboarding-confirmar-salir-a11y.png`.
- Slider de "¿Cuántos días entrenarás por semana?": se agregó `aria-valuetext` ("N días por semana") — antes un lector de pantalla solo anunciaba el número crudo del rango 1-7, sin unidad.
- Selección de chips (`Chips`): se agregó vibración corta (`navigator.vibrate(10)`, con guard de soporte) al tocar una opción — mismo patrón ya usado en el cronómetro de descanso de `app/app/page.tsx`.
- tsc ✓ · eslint ✓ (se corrigió también una advertencia real de `exhaustive-deps` en el `useEffect` del modal, copiando el ref a una variable local antes del cleanup) · build ✓.
/ Siguiente acción exacta: relanzar el revisor-visual una vez más para confirmar si estas 3 correcciones alcanzan el umbral de usabilidad (36/40). Si no, evaluar con el usuario si vale la pena seguir iterando los defectos menores restantes (densidad TarjetaBeneficio+TarjetaRuta, riesgo de paleta) o pausar aquí — ya van varias rondas en esta pantalla.

⏸️ CHECKPOINT — Última acción completada (02/09/2026): **Hallazgo #2 de la auditoría CERRADO — Historial rediseñado con gráfico de volumen animado.** Antes era una lista plana sin gráfico ni insight; a pedido explícito del usuario ("un gráfico dinámico y ganador").
- Se instaló `recharts` (pinneado, doctrina 22-LIBRERIAS-Y-CRAFT). Nuevo `app/app/historial/page.tsx`: dato héroe ("Volumen de tus últimas N sesiones", número grande animable) + insight interpretado (↑/↓ % vs la sesión anterior, o mensaje de arranque si aún no hay para comparar) + gráfico de ÁREA (serie temporal — el tipo correcto según la tabla de 17-VISUALIZACION-DATOS para "evolución en el tiempo") con degradado del acento, máximo 7 puntos visibles, tooltip tematizado con los tokens de la app, y tabla `sr-only` con los mismos datos (accesibilidad). El volumen se calcula como Σ peso×reps×series por fecha. Debajo se mantiene el detalle por sesión ya existente.
- Estado vacío: se le agregó el CTA que faltaba ("Ir a mi plan de hoy" → `/app`) — antes quedaba sin salida, otro hallazgo de la auditoría.
- **Verificado con datos reales del usuario** (no solo simulados): el gráfico mostró su volumen real de 3 sesiones (1216→2960→3120 kg), curva ascendente correcta, insight "↑ 5% vs tu sesión anterior — vas para arriba", sin errores de consola. tsc ✓ · eslint ✓ · build ✓.
/ Siguiente acción exacta: queda pendiente el hallazgo #3 de la auditoría (onboarding, offline-first nunca construido) o retomar Hotmart cuando el usuario cree el producto (ver sección "PENDIENTE — conexión con Hotmart" abajo).

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **🎉 Auditoría senior (--rápido) + hallazgo crítico #1 CERRADO: la app ya exige plan pagado real para entrar a `/app`, probado de punta a punta con una cuenta real (login por código + los dos casos: `free`→paywall y `pro`→app).** El usuario aprobó explícitamente el ítem #1 del reporte de auditoría.

**Reporte de auditoría** (6 dimensiones, modo rápido): producto/UX/backend/seguridad sólidos para el tamaño actual (RLS con `(select auth.uid())`, índices correctos, sin secretos en frontend); el hallazgo crítico fue que **el paywall no cobraba nada de verdad** — cualquier cuenta creada por magic link entraba a `/app` gratis para siempre. Puntaje dado: 6/10. Otros hallazgos (no críticos, documentados abajo en "Problemas conocidos"): Historial con vacío muerto sin gráfico/insight, estado vacío de Historial sin CTA, offline-first decidido pero nunca construido.

**Lo que ya se construyó para conectar Hotmart de verdad** (docs/sistema/18-VENTA-HOTMART.md, con alcance recortado — sin ledger económico completo ni reconciliación semanal todavía, documentado como pendiente):
- Migraciones aplicadas al proyecto real (`0005_hotmart_membership.sql`, `0006_reconciliar_membresia.sql`, `0007_reconciliar_membresia_upsert.sql`): tablas `hotmart_purchases` (fuente de verdad del pago, por email), `processed_events` (idempotencia), `webhook_log`; columnas nuevas en `profiles` (`email`, `plan`, `membership_status`, `trial_ends_at`, `access_until`, `grace_ends_at`); función `apply_hotmart_event` (las 4 defensas del webhook: autenticidad/frescura/idempotencia/máquina de estados, transaccional); función `reconciliar_membresia()` (SECURITY DEFINER, la única puerta que un usuario normal tiene para subir su propio perfil a pro, usando su propio email vía `auth.uid()` — sin IDOR posible).
- `lib/hotmart-verify.ts`: verificación del hottok en tiempo constante (`crypto.timingSafeEqual`, nunca `===`), fail-secure evaluado AL RECIBIR una petición (no al importar el módulo — un throw a nivel de módulo rompía `next build` porque Next.js recolecta la configuración de cada `route.ts` en build time, sin ninguna petición real de por medio; hallazgo real de esta sesión).
- `lib/membership-fsm.ts`: máquina de estados (trialing/active/past_due/cancelled/expired/refunded/chargeback), bloquea que un evento viejo reentregado reactive un reembolso/chargeback ya procesado.
- `app/api/webhooks/hotmart/route.ts`: el endpoint — raw body → autenticidad (hottok) → parseo → frescura (ventana anti-replay 5 min) → RPC atómica → log. Devuelve 200 siempre que decide algo (incluido duplicado/ilegal), 5xx solo en fallo real (para que Hotmart reintente).
- `lib/supabase/sync.ts`: `sincronizarPerfilInicial` ahora SIEMPRE llama a `reconciliar_membresia` (idempotente) y solo pisa nivel/meta/horario si hay respuestas de onboarding frescas (nunca resetea a defaults a un usuario que vuelve sin sessionStorage — bug que se evitó a tiempo). Nueva `leerMembresiaRemota()`.
- `app/auth/callback/route.ts`: llama a `reconciliar_membresia` ANTES de redirigir a `/app` — si se hiciera recién al montar `/app` en el cliente, `proxy.ts` revisaría el plan un instante antes de que existiera la fila reconciliada, y un usuario que acaba de pagar vería el paywall otra vez en su primer clic. (Bug real encontrado y corregido ANTES de que ocurriera, no después.)
- `proxy.ts`: ahora también exige `profiles.plan === 'pro'` para entrar a `/app` (antes solo exigía sesión) — redirige a `/paywall` si no.
- `app/paywall/page.tsx`: el CTA abre el checkout real de Hotmart si `NEXT_PUBLIC_HOTMART_CHECKOUT_{MENSUAL,ANUAL}` están configuradas; si no, cae de vuelta al mock anterior (`/login`) sin romper nada.
- `app/app/perfil/page.tsx`: nueva tarjeta "Tu plan" con enlace a `https://purchases.hotmart.com` para gestionar/cancelar — cierra la promesa "cancela cuando quieras" del paywall, que antes no tenía dónde cumplirse en la app.
- **Bug real encontrado y corregido de paso**: `app/login/page.tsx` asumía que el código de respaldo de Supabase siempre tiene 6 dígitos (`maxLength={6}`, validación `length !== 6`) — el código REAL que manda este proyecto tiene 8 dígitos. El campo de código nunca dejaba escribir el código completo. Corregido a un mínimo de 6 sin asumir un largo exacto (el servidor es quien valida si el código es correcto, no el cliente).
- **Bug de plantilla de correo, ya documentado antes, cerrado en esta sesión**: el correo de acceso no traía `{{ .Token }}` — el usuario lo agregó en el panel de Supabase.
- **PROBADO DE PUNTA A PUNTA con la cuenta real del usuario** (no solo en la base de datos): login completo por código de 8 dígitos → `/app` dejó entrar (plan pro) → se bajó el perfil a `plan: 'free'` por SQL → visitar `/app` con la MISMA sesión redirigió a `/paywall` → se restauró a `plan: 'pro'` → `/app` volvió a dejar entrar, con el nombre/racha/día del usuario intactos (la reconciliación de membresía no tocó sus datos de progreso). Perfil muestra la tarjeta "Tu plan · Activa · Gestionar o cancelar mi suscripción" con datos reales. Sin errores de consola.
## PENDIENTE — conexión con Hotmart (pospuesto a pedido del usuario, 02/09/2026)

El código y la base de datos del lado de GymEvo ya están 100% listos para recibir pagos reales (ver checkpoint arriba). Lo que falta es trabajo del lado de Hotmart que el usuario todavía no ha hecho: **el producto de suscripción ni siquiera está creado en Hotmart todavía** (aclarado el 02/09/2026 — antes se había dicho que sí existía, pero no es así). El usuario pidió dejarlo para después.

Guía completa ya entregada en el chat (no repetirla desde cero, solo retomarla) — 4 partes:
1. Crear el producto tipo SUSCRIPCIÓN en Hotmart + área de miembros con 1 clase (requisito de aprobación) + los 2 planes (mensual/anual) — ver `docs/sistema/18-VENTA-HOTMART.md` Pasos A-D.
2. Conseguir 3 datos: las 2 URLs de checkout (mensual/anual), el HOTTOK del webhook, y la Service Role Key de Supabase.
3. Ponerlos en Vercel → Settings → Environment Variables: `NEXT_PUBLIC_HOTMART_CHECKOUT_MENSUAL`, `NEXT_PUBLIC_HOTMART_CHECKOUT_ANUAL`, `HOTMART_HOTTOK`, `SUPABASE_SERVICE_ROLE_KEY`.
4. Configurar el webhook en Hotmart apuntando a `https://gymevo-eta.vercel.app/api/webhooks/hotmart`, marcar los eventos (aprobada/completa/reembolso/chargeback/cancelación/atrasado), enviar test.

Recién con eso hecho: PRUEBA E2E real (comprar de verdad o sandbox) antes de anunciar que se puede vender.

/ Siguiente acción exacta: cuando el usuario retome esto, avisarle que primero necesita CREAR el producto en Hotmart (parte 1 de la guía) antes de poder conseguir los datos de la parte 2. Mientras tanto, seguir con otros hallazgos de la auditoría: #2 (Historial con vacío muerto, sin gráfico/insight) o #3 (onboarding, ver checkpoint de sesión de pulido anterior).

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **"Explicación del ejercicio" simplificada — queda lista para recibir imágenes reales que el usuario va a enviar más adelante.** Decisión del usuario: no seguir dibujando cortes anatómicos a mano (se descartó el intento de "flexión de codo" de la ronda anterior) — en su lugar, cuando arme cada rutina va a mandar la imagen ya hecha por ejercicio.
- `lib/routine.ts`: se quitó el tipo `PatronMovimiento` y el campo `patronMovimiento` (ya no hace falta agrupar por patrón de movimiento). Se agregó `imagenExplicacion?: string` a `Ejercicio` — ruta opcional dentro de `public/` a la imagen real del ejercicio.
- `components/CorteAnatomico.tsx`: eliminado (era el dibujo a mano, ya no se usa).
- `app/app/page.tsx`: el modal de "Explicación del ejercicio" muestra `ej.imagenExplicacion` si existe; si no, sigue cayendo de vuelta a la silueta de cuerpo (`CuerpoMuscular`, ya construida para los 12 grupos musculares) — nunca queda roto ni vacío mientras faltan imágenes.
- `public/explicaciones/README.md` (nuevo): instrucciones para el propio usuario — dónde poner la imagen, cómo nombrarla, y qué línea agregar en `lib/routine.ts` para conectarla. Así puede agregar imágenes él mismo sin depender de mí para cada una, si quiere.
- `components/landing/tokens.css`: se quitaron los 3 tokens de color de anatomía que ya no se usan (piel/hueso/tendón).
- tsc ✓ · eslint ✓ · build ✓.
/ Siguiente acción exacta: esperar a que el usuario mande las imágenes de cada ejercicio (por rutina) y agregarlas una por una con `imagenExplicacion` en `lib/routine.ts` — el trabajo restante es solo de datos, no de código.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Corte anatómico (hueso+tendón+músculo) — primer patrón de movimiento, en revisión con el usuario.** El usuario mandó una referencia distinta a la silueta de cuerpo completo que ya existía: un corte transversal de una extremidad (hueso, tendón, vientre muscular, mancuerna, flecha de movimiento) tipo "Contracción excéntrica: el músculo se alarga". Decisión: NO reemplaza la silueta de cuerpo — se agrega como una segunda ilustración, más detallada, que se usa cuando existe para el PATRÓN DE MOVIMIENTO del ejercicio (no por grupo muscular: varios ejercicios comparten patrón, ej. curl de bíceps y curl martillo son ambos "flexión de codo").
- `lib/routine.ts`: nuevo tipo `PatronMovimiento` (9 patrones: flexión de codo, extensión de codo, empuje horizontal, empuje vertical, tirón horizontal, tirón vertical, extensión de rodilla, bisagra de cadera, flexión de tronco) + campo `patronMovimiento` en cada ejercicio del catálogo existente.
- `components/CorteAnatomico.tsx` (nuevo): por ahora SOLO tiene dibujado "flexión de codo" (curl de bíceps/martillo) — los otros 8 patrones quedan pendientes de que el usuario apruebe el estilo antes de invertir el tiempo de dibujarlos todos. `hayCorteAnatomico(patron)` le dice al llamador si ya existe o si debe caer de vuelta a la silueta de cuerpo completo.
- `components/landing/tokens.css`: 3 colores nuevos de contenido educativo (`--anatomia-piel`, `--anatomia-hueso`, `--anatomia-tendon`) — misma excepción a 60-30-10 que el confeti, no son colores de marca/UI.
- `app/app/page.tsx`: el modal de "Explicación del ejercicio" ahora muestra el corte anatómico cuando existe para el patrón del ejercicio, y si no, cae de vuelta a la silueta de cuerpo (`CuerpoMuscular`) — nunca se rompe aunque falten 8 patrones por dibujar.
- Verificado en el navegador (curl de bíceps → corte de brazo con bíceps en rojo, tendones, mancuerna, flecha) y se le mandó una captura al usuario para que apruebe el estilo antes de dibujar los 8 patrones restantes.
- **Aclaración importante ya dada al usuario**: no hay herramienta de generación de imágenes fotorrealistas disponible — esto es dibujo plano en SVG, no puede verse como su imagen de referencia (que parece ser de una app con licencia propia).
- tsc ✓ · eslint ✓ · build ✓.
/ Siguiente acción exacta: esperar el visto bueno (o los ajustes) del usuario sobre el estilo del corte de bíceps, y solo después dibujar los 8 patrones restantes (mismo sistema, coordenadas nuevas por patrón).

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Nueva sección "Explicación del ejercicio"** — a pedido del usuario, cada ejercicio de "Plan de hoy" ahora tiene un botón junto a "¿Cómo se hace?" que abre una hoja inferior con una silueta de cuerpo (ilustración PROPIA en SVG, no fotos de terceros con licencia ajena — el usuario aprobó este camino tras preguntarle) con el músculo trabajado resaltado en rojo. Cambios:
- `lib/routine.ts`: nuevo tipo `GrupoMuscular` (12 grupos: pecho, hombro, bíceps, tríceps, trapecio, femoral, cuádriceps, pantorrilla, dorsal, glúteo, espalda, core) + `MUSCULO_LABEL` + campo `grupoMuscular` en cada ejercicio del catálogo (los 12 grupos ya están soportados aunque hoy no todos tengan ejercicio — quedan listos para cuando se agreguen sentadillas de pantorrilla, encogimientos de trapecio, hip thrust, etc.) + `generoIlustracion(id)` que alterna masculino/femenino de forma ESTABLE según la posición del ejercicio en el catálogo (nunca al azar — el mismo ejercicio siempre muestra el mismo género).
- `components/CuerpoMuscular.tsx` (nuevo): siluetas simples masculina/femenina (frontal o posterior según el músculo) con la región resaltada — 12 regiones definidas a mano en el mismo sistema de coordenadas para que calcen en ambos cuerpos.
- `app/app/page.tsx`: botón "Explicación del ejercicio" (ícono mancuerna, color ámbar) junto a "¿Cómo se hace?" en cada tarjeta; hoja inferior con el nombre del músculo, el ejercicio y la ilustración.
- Verificado en el navegador: pecho, hombro, tríceps (vista posterior, ambos brazos), dorsal (vista posterior, laterales) y espalda (vista posterior, centro) — silueta correcta, alternancia masculino/femenino confirmada, sin errores de consola. tsc ✓ · eslint ✓ · build ✓.
/ Siguiente acción exacta: si se agregan ejercicios nuevos que usen pantorrilla/trapecio/glúteo (grupos sin ejercicio todavía), la ilustración ya está lista — solo hay que ponerles `grupoMuscular` correcto. Fuera de eso, retomar el pulido de onboarding (vacío bajo la tarjeta en pasos cortos, ver checkpoint anterior) o seguir con otra parte de la app.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **🎉 LANDING declarada LISTA — 36/40 usabilidad, 19/20 craft, 19/20 copy.** Cierre de la maratón de 7 rondas de esta sesión de pulido. El movimiento decisivo fue encontrar y corregir un bug real de z-index (mismo patrón ya visto en `app/app/perfil/page.tsx`) que tenía el video de fondo del Hero y la textura de "renglones de cuaderno" invisibles en TODOS los renders anteriores — una vez corregido y con el scrim más transparente, más el botón de volver arriba para la heurística de control/libertad que faltaba, la pantalla cruzó las tres rúbricas. **Onboarding se quedó en 33-34/40 · 15/20 craft** (mejoró de 32/10 al inicio de la sesión) — con el mismo bug de z-index corregido de paso (el fondo del onboarding también estaba invisible), más varios ajustes reales (tarjeta de progreso en vivo, navegación por teclado, celebración animada, tarjeta ya no parece tocable) — pero el vacío bajo la tarjeta en los pasos cortos sigue sin resolverse del todo y requiere más tiempo de diseño de contenido, no otro ajuste de código. Se paró en onboarding por decisión explícita de tiempo del usuario ("por tiempo, opción 2").
⏸️ CHECKPOINT — Última acción completada (01/09/2026): **🎉 LANDING declarada LISTA — 36/40 usabilidad, 19/20 craft, 19/20 copy.** Cierre de la maratón de 7 rondas de esta sesión de pulido. El movimiento decisivo fue encontrar y corregir un bug real de z-index (mismo patrón ya visto en `app/app/perfil/page.tsx`) que tenía el video de fondo del Hero y la textura de "renglones de cuaderno" invisibles en TODOS los renders anteriores — una vez corregido y con el scrim más transparente, más el botón de volver arriba para la heurística de control/libertad que faltaba, la pantalla cruzó las tres rúbricas. **Onboarding se quedó en 33-34/40 · 15/20 craft** (mejoró de 32/10 al inicio de la sesión) — con el mismo bug de z-index corregido de paso (el fondo del onboarding también estaba invisible), más varios ajustes reales (tarjeta de progreso en vivo, navegación por teclado, celebración animada, tarjeta ya no parece tocable) — pero el vacío bajo la tarjeta en los pasos cortos sigue sin resolverse del todo y requiere más tiempo de diseño de contenido, no otro ajuste de código. Se paró en onboarding por decisión explícita de tiempo del usuario ("por tiempo, opción 2").
/ Siguiente acción exacta: si el usuario quiere seguir con onboarding en otra sesión, retomar desde el defecto #1 documentado (vacío bajo la tarjeta en pasos cortos) — necesita contenido nuevo, no otro ajuste de espaciado/opacidad. Fuera de eso, seguir con la Sesión 7 (pantalla-principal ya cerrada en rondas anteriores) o con el dominio propio → Hotmart cuando el usuario tenga presupuesto.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Refuerzo del dispositivo ownable (opción "por tiempo": no tocar la paleta base) + HALLAZGO de un bug real que invalidaba las 4 rondas anteriores de revisión de landing/onboarding**.
- Se agregó una textura de "renglones de cuaderno" (líneas rectas horizontales, PROPIA — distinta del grano feTurbulence del ejemplo vetado "Capítulo" del banco canónico) al fondo del Hero (landing) y del onboarding completo, más una espiral de encuadernación (columna de puntos) en el borde izquierdo de `TarjetaRuta`.
- **Al verificar en el navegador, las líneas no se veían — y se descubrió por qué**: tanto el fondo de `Hero.tsx` (mesh + video + scrim) como el fondo del onboarding usaban `z-index` NEGATIVO sobre elementos `absolute`, el mismo bug de stacking-context ya encontrado y documentado antes en `app/app/perfil/page.tsx` (un hijo con z negativo pinta DETRÁS del fondo opaco de su ancestro). Esto significa que **el video de fondo del Hero probablemente nunca se vio en NINGÚN render de esta sesión ni de sesiones anteriores** — se había interpretado como "limitación de la herramienta de screenshot para capturar movimiento", pero en realidad la capa entera era invisible. También explica por qué el revisor-visual reportó "fondo plano" en CADA una de las 4 rondas de onboarding de hoy pese a subir la opacidad varias veces: la opacidad nunca importó porque la capa nunca se pintaba.
- **Corregido en ambos archivos** (`components/landing/Hero.tsx`, `app/onboarding/page.tsx`): todos los fondos pasaron de z negativo a `z-index: 0`, y el contenido real de cada pantalla quedó envuelto en su propia capa `relative z-10` (mismo patrón ya probado). Verificado visualmente en el navegador con un "test de color rojo" (pintar el div de fondo de rojo sólido para confirmar si se veía o no) antes y después del fix — confirmado que antes NO se veía nada (ni rojo) y después SÍ.
- Se relanzó el revisor-visual (ronda 5, con contexto explícito del hallazgo) para ambas pantallas — es una re-evaluación legítima porque el render real cambió de forma sustancial, no una repetición de rondas ya agotadas. Resultado pendiente de notificación.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en `components/landing/Hero.tsx` y `app/onboarding/page.tsx`.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Ronda 3 de correcciones landing+onboarding, tras los resultados de la ronda 2 del revisor-visual (onboarding 34/40·12/20, landing 33/40·14/20·19/20 copy — ambas siguen NO LISTA)**. Defectos reales corregidos:
- **Onboarding**: el borde punteado de `TarjetaRuta` era igual al de los chips no-seleccionados (parecía tocable sin serlo) → ahora borde SÓLIDO en ámbar; el ámbar ya no vivía en ningún elemento visible (solo un gradiente casi imperceptible) → el ícono `NotebookPen` y el eyebrow "TU RUTA SE ESTÁ ARMANDO" ahora son ámbar sólido; el paso "reconocimiento" tenía un check estático sin la celebración N1 ("trazo verde") de FICHA-ARTE → ahora es un SVG con `pathLength` animado (se dibuja), respeta `prefers-reduced-motion`; FICHA-ARTE.md decía que accent-2 era "N/A por ahora" pero el código ya lo usaba → sincronizado.
- **Landing**: faltaba la baseline de conteo animado en los precios → se agregó `PrecioAnimado` en `Oferta.tsx` (cuenta 0→N al entrar en vista, `useInView`+`animate`, respeta reduced-motion) para mensual/anual (el total tachado del stack Hormozi se dejó estático a propósito: no tiene sentido narrativo animar un ancla que se descarta visualmente); el titular de Oferta acentuaba también la preposición "de" → recortado a solo la cifra.
- **Pendientes reconocidos y NO corregidos** (decisión consciente de alcance, fuera de esta sesión de pulido): el riesgo de cercanía de la PALETA BASE (papel cálido + tinta verde) con la combinación vetada "Capítulo" del banco canónico — cambiar la paleta base es redecidir FICHA-ARTE.md ("cosa juzgada"), requiere aprobación explícita del usuario, no es un ajuste de pulido; falta una textura/trazo literal de "cuaderno físico"; varias secciones de la landing (FAQ/Garantía/Agitación) usan solo 2 planos tipográficos en vez de 4.
- Verificado: tsc ✓ · eslint ✓ · build ✓ en todos los archivos tocados (`app/onboarding/page.tsx`, `components/landing/Oferta.tsx`, `app/page.tsx`, `FICHA-ARTE.md`).

**RESULTADO FINAL de esta sesión de pulido (ronda 4, la última — 01/09/2026):**
- **landing**: 34/40 usabilidad · 13/20 craft · **20/20 copy** — sigue NO LISTA. El copy quedó perfecto. El revisor fue explícito: el defecto que bloquea (identidad/paleta) **requiere rediseño real, no un ajuste de código** — es la misma paleta base de FICHA-ARTE.md ("papel cálido + tinta verde"), cosa juzgada del proyecto. Corregirlo de verdad implica (a) que el usuario apruebe tocar la paleta base, o (b) diseñar un dispositivo ownable mucho más fuerte y repetido en la página — ninguna de las dos es un "ajuste de pulido". Otros defectos menores identificados (2 planos tipográficos en Problema/Agitación/Garantía en vez de 4, un botón que duplica `CtaButton` en vez de usar una variante, contraste a verificar en el recap de CtaFinal) SÍ son correcciones rápidas, pero no alcanzan para cruzar el gate mientras el bloqueo de identidad siga sin resolverse.
- **onboarding**: 34/40 usabilidad · 11/20 craft — sigue NO LISTA, mismo bloqueo raíz de identidad/paleta (idéntico a landing, coherente porque comparten FICHA-ARTE). Defectos menores identificados que SÍ son correcciones rápidas para una próxima sesión: vacío de ~200-250px sin centrar en los pasos cortos (nivel/meta), los 3 gradientes de fondo siguen leyéndose casi planos en el render real (subir opacidad más agresivamente o acercar el radio), el contador de "días/semana" salta sin animación al mover el slider, y el radio visual de los chips (~14-16px) no coincide con el pill de 100px que documenta FICHA-ARTE para botones (hay que alinear uno de los dos).
- **Decisión de cierre de esta sesión**: se PARA aquí, tal como se avisó de antemano al lanzar esta última ronda. 4 rondas de corrección real (no cosmética) mejoraron ambas pantallas de forma medible — landing subió su copy a 20/20 y resolvió 7 defectos reales (2ª nota de color, tachado verdadero, checkmarks consistentes, titulares recortados, conteo animado); onboarding resolvió su causa raíz estructural (el vacío por centrar mal) y ganó una tarjeta de progreso en vivo, ámbar visible, navegación por teclado y una celebración animada — pero NINGUNA de las dos cruza el gate del revisor porque el bloqueo real y compartido es una decisión de identidad/paleta que el SO marca explícitamente como "cosa juzgada, no se redecide a mitad de proyecto sin aprobación del usuario". Seguir iterando dentro del alcance de "pulido" no mueve ese número — hace falta que el usuario decida si quiere invertir una sesión dedicada en re-derivar la paleta (16/54) o reforzar el dispositivo ownable de forma mucho más agresiva.
- Veredictos completos en `docs/revisiones/{landing,onboarding}-veredicto.md`. Capturas 375px vigentes en `docs/revisiones/{landing,onboarding}-375.png`.

⏸️ CHECKPOINT — Última acción completada (01/09/2026): **Sesión de pulido dedicada a landing + onboarding (a pedido explícito del usuario: "dediquémosle tiempo a ambas... que queden ganadoras y perfectas") — 2 rondas de corrección real con revisor-visual entre medio**.

**Ronda 1** (causas raíz, no cosméticas):
- Onboarding (`app/onboarding/page.tsx`): `justify-center` centraba los pasos cortos (2-4 chips) dejando mucho vacío arriba+abajo — se cambió a `justify-start` (ancla arriba) y se agregó `TarjetaRuta` (tarjeta "recibo de cuaderno" que llena Nivel/Meta/Horario/Días-semana EN VIVO con check verde) en los pasos nivel/meta/frustración/compromiso.
- Landing: se activó `--accent-2` (ámbar tierra, ya previsto en FICHA-ARTE) en `components/landing/tokens.css`; se agregó el dispositivo ownable a `Solucion.tsx`; se agregó el texto "Desliza para ver el resto" al carrusel `AppPorDentro.tsx`.
- Resultado del revisor-visual (ronda 1): **onboarding 33/40 · 14/20** (mejoró de 32/10, sigue NO LISTA) — defectos: TarjetaRuta se veía vacía/rota en el paso 1 (4 guiones sin nada), seguía quedando ~20-25% de vacío plano bajo la tarjeta, riesgo de paleta cercana a la vetada, sin navegación por teclado en los chips. **landing 34/40 · 13/20 craft · 19/20 copy** (sigue NO LISTA) — defectos: el "arreglo" del dispositivo ownable era un ✓ suelto de Lucide, NO el tachado prometido; la 2ª nota de color solo aparecía 1 vez (insuficiente); 3 titulares acentuaban 4-5 palabras en vez de 1-3; checkmark inconsistente entre secciones.
- **Ronda 2** (corrección de los defectos reales que señaló el revisor):
  - Onboarding: `TarjetaRuta` ya no se muestra vacía — si nadie ha respondido nada todavía (paso 1) se ve un mensaje de bienvenida + ícono `NotebookPen` en vez de 4 guiones; tarjeta más grande (más padding); degradé de fondo reforzado (opacidad subida + un 3er radial-gradient con el ámbar); chips ahora son `role="radiogroup"`/`role="radio"` navegables con flechas ↑/↓.
  - Landing: el tachado real se aplicó al texto "antes" de `Solucion.tsx` (`line-through` en verde, igual que un ejercicio tachado en la app) y el "después" pasó a usar `CheckCustom` (el mismo componente que ya usaba `Oferta.tsx` — resuelve la inconsistencia); la 2ª nota de color (ámbar) se sumó también al badge "N días gratis" de `Oferta.tsx` (aparece 2 veces más, una por plan) — ahora 3 apariciones en total; se recortaron los 4 titulares con énfasis largo (Hero, Solución, AppPorDentro, CtaFinal) a 1-3 palabras acentuadas.
  - Verificado: tsc ✓ · eslint ✓ · build ✓ tras cada ronda. Capturas 375px regeneradas antes de cada pasada del revisor.
- Se relanzó el revisor-visual (ronda 2) para ambas pantallas — corriendo en paralelo, resultado pendiente de notificación al cerrar este checkpoint.
- Pendiente reconocido y NO corregido esta sesión (bajo impacto, decisión consciente de alcance): falta una textura/trazo literal que remita al "cuaderno físico" (mundo del sujeto de FICHA-ARTE) — por ahora la identidad se apoya solo en color+tipografía+el tachado.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Celebración con confeti al terminar el entrenamiento del día**. Al tocar "Terminar entrenamiento de hoy" (`finalizarEntrenamiento` en `app/app/page.tsx`) aparece una pantalla completa con confeti cayendo (28 piezas SVG-CSS, colores en `--confetti-1..5` de `components/landing/tokens.css` — única excepción documentada a la regla 60-30-10, es una celebración puntual y no forma parte de la paleta de UI), el texto "¡Muy bien!" y debajo el emoji nativo 😅 (sin fondo, como pidió el usuario). Nota: el usuario pegó una imagen 3D específica de ese emoji en el chat, pero no llegó como archivo — no hay forma de extraer los píxeles exactos de una imagen solo vista en la conversación. Se le preguntó cómo prefería resolverlo; no respondió esa pregunta directamente pero pidió agregar la animación de confeti, así que se avanzó con el emoji nativo (☑ recomendado en la pregunta) — si más adelante envía el archivo de imagen real (arrastrado al chat, no pegado), se reemplaza. El confeti respeta `prefers-reduced-motion` (no se muestra, solo el texto). Verificado: tsc ✓ · eslint ✓ (una advertencia de dependencia de `useMemo` resuelta con comentario explicativo, es intencional) · build ✓ · flujo completo probado en `http://localhost:3000/app` (bypass temporal de proxy.ts, revertido antes del commit): las 3 piezas de hoy registradas, "Terminar entrenamiento" tocado, confeti cayendo y desapareciendo solo, tal como se esperaba.
/ Siguiente acción exacta: si el usuario envía el archivo de imagen del emoji exacto, reemplazar el 😅 nativo por esa imagen (como `<img>`, en `public/`); si no, queda así. Luego retomar el pulido pendiente de Sesión 7 (landing y onboarding) o el dominio propio → Hotmart cuando tenga presupuesto.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Sonido de notificación al terminar el descanso**. Se agregó una campanita sintetizada con Web Audio API (dos tonos cortos, sin archivo de audio que descargar ni licencia que pagar) que suena junto con la vibración cuando el cronómetro de descanso llega a 0. Encendida por defecto (`sonidoDescanso: true` en `Progreso`, columna `profiles.sonido_descanso`, migración `supabase/migrations/0004_sonido_descanso.sql` aplicada al proyecto real vía MCP); el usuario la puede apagar con un botón de altavoz debajo de los chips de duración. Detalle técnico importante: el `AudioContext` se crea/retoma DENTRO del tap de "Registrar" (un gesto real del usuario), no cuando el descanso llega a 0 — los navegadores bloquean el audio que no nace de un gesto directo, así que crearlo tarde nunca sonaría. Verificado: tsc ✓ · eslint ✓ · build ✓ · flujo probado en `http://localhost:3000/app` (bypass temporal de proxy.ts, revertido antes del commit): sin errores de consola al disparar la campanita.
/ Siguiente acción exacta: pedirle al usuario que confirme que escucha el sonido en su celular real (el navegador de prueba no permite verificar audio de oído); luego retomar el pulido pendiente de Sesión 7 (landing y onboarding no pasan el gate del revisor-visual) o el dominio propio → Hotmart cuando tenga presupuesto.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Cronómetro de descanso configurable con anillo que se consume**. En `app/app/page.tsx`: el temporizador de descanso entre series ya no usa el `descansoSeg` fijo de cada ejercicio del catálogo — ahora el usuario elige UNA duración (30s/1min/2min/3min, chips debajo del interruptor "Descanso automático", visibles solo si está activado) que se usa para todos los ejercicios de hoy. Se agregó el campo `descansoDuracionSeg` a `Progreso` (`lib/routine.ts`, default 60) y la columna `profiles.descanso_duracion_seg` (migración `supabase/migrations/0003_descanso_duracion.sql`). El banner de descanso muestra un anillo SVG (`AnilloDescanso`) que se vacía en tiempo real.

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **"Plan de hoy" con animación Lottie en el saludo**. En `app/app/page.tsx`: se agregó `lottie-react` (v3.1.1 — API nueva: export nombrado `Lottie`, prop `src` en vez de `animationData`; el tamaño no lo respeta una clase Tailwind, hay que forzarlo con `style={{width,height}}`) con la animación del usuario (`public/animaciones/fitness.json`) al final del titular. El titular pasó de "Hola, {nombre}, hoy toca {parte}" a "Hola, hoy toca {parte}" (a pedido del usuario, para que quepa en una sola línea junto al ícono) — se quitó también el estado/prop `nombre` de esta pantalla por quedar sin uso (seguía vivo en Perfil, donde sí se usa).

⏸️ CHECKPOINT ANTERIOR (01/09/2026): **Login real de punta a punta + video de fondo en Perfil, con un bug de CSS real encontrado y corregido**. Resumen de esa sesión larga:
- Resend conectado como SMTP de Supabase (sin dominio propio — sin presupuesto ahora; remitente de pruebas `onboarding@resend.dev`, solo entrega al dueño de la cuenta de Resend, límite subido de 2/hora a 30).
- Bug real corregido: la "Site URL" de Supabase Auth apuntaba a `localhost:3000` desde la creación del proyecto (los magic links en producción redirigían a la máquina del usuario). Corregido en el dashboard.
- Bug real corregido: Gmail/Outlook escanean el enlace mágico automáticamente y lo "gastan" antes de que el usuario lo toque (confirmado en logs: un `/verify` exitoso y otro con "token not found" al mismo segundo). Se agregó el código de 6 dígitos como respaldo en `/login` (doctrina 26-AUTH-MODERNO: "el combo") — falta que el usuario agregue `{{ .Token }}` a la plantilla de correo en el dashboard de Supabase para que el código realmente llegue.
- **Bug real de CSS encontrado y corregido**: el video de fondo (agregado primero en "Plan de hoy", luego movido a "Perfil" a pedido del usuario) usaba `z-index` NEGATIVO sobre `position:fixed` — por las reglas de stacking de CSS, eso lo pintaba DETRÁS del fondo opaco de un contenedor ancestro (el shell de `/app`), invisible en un iPhone real aunque el `<video>` decodificaba bien (confirmado con una captura real del usuario). Fix: z-index NO negativo para el video + todo el contenido real envuelto en su propia capa con z-10. Patrón a recordar para cualquier video/imagen de fondo futura dentro de `/app` (el hero de la landing no tenía este problema porque no vive dentro de un wrapper con fondo propio).
- A pedido explícito del usuario, el video en Perfil NO lleva scrim de color (se ve completo); solo el saludo y "Cerrar sesión" llevan su propio chip opaco para seguir siendo legibles.
- Nombre de usuario editable en Perfil ("Hola, {nombre}", persistido en Supabase, columna `profiles.nombre`).
/ Siguiente acción exacta: pedir al usuario que confirme el video en su iPhone real y que agregue `{{ .Token }}` a la plantilla de correo; luego seguir con el pulido pendiente de Sesión 7 (landing y onboarding no pasan el gate del revisor-visual) o con dominio propio → Hotmart cuando tenga presupuesto.

## Logo / isotipo (31/08/2026)
- El usuario mandó un isotipo generado con IA (dumbbell + reloj de arena formando una "H", verde sobre crema — coincide de casualidad con los tokens de FICHA-ARTE). No llegó como archivo editable (solo visible en el chat), así que se redibujó como vector en `components/Logo.tsx` (mismo diseño, `fill="currentColor"`, sin fondo, nítido a cualquier tamaño) en vez de recortar la imagen rasterizada.
- Ya reemplaza el placeholder verde genérico en: header de la landing (`Hero.tsx`) y `/login`. También es el favicon nuevo (`app/icon.svg`, se borró el `app/favicon.ico` genérico de Next.js).
- Pendiente: si el usuario quiere ajustar el diseño del isotipo (proporciones, grosor), se edita `components/Logo.tsx` y `app/icon.svg` (mismo path, duplicado a propósito porque el favicon no puede usar variables CSS).

## Video de fondo del hero (31/08/2026)
- El usuario pidió agregar un video propio (`Personas_entrenando_en_gimnasio...mp4`, IA) como fondo de la sección Hero de la landing. Copiado a `public/videos/hero-gimnasio.mp4`. `Hero.tsx` ganó un prop `backgroundVideoSrc` opcional: video absoluto detrás del contenido + scrim en degradé del propio `--bg` (58%→92%) para que el texto siga legible, oculto con `prefers-reduced-motion` (motion-reduce:hidden). Verificado por código/DOM/canvas que decodifica frames reales con color; la herramienta de screenshot remota que uso no logra capturar el video en movimiento (limitación de la herramienta, no del código) — el usuario debe confirmarlo en su propio navegador.

## Sesión 7 (31/08/2026) — en curso: revisor-visual de las 4 pantallas del dinero
- Se instaló Playwright (`npm i -D playwright` + `npx playwright install chromium`) para generar screenshots REALES a 375px (antes no había manera de generarlos sin pedirle capturas al usuario). Script: `scripts/capturar-screenshots.mjs` (sembra sessionStorage/localStorage con datos de ejemplo realistas antes de capturar).
- Se corrigió de paso: Next.js 16 le apendizaba su propio bloque a `AGENTS.md` en cada `next dev` (pisando la copia intencional del SO) — se agregó `agentRules: false` en `next.config.ts`.
- Capturas generadas (página completa a 375px): `docs/revisiones/{landing,onboarding,paywall,pantalla-principal}-375.png`.
- El subagente `revisor-visual` NO aparece en la lista de subagent_type disponibles del harness de esta sesión — se invocó como `general-purpose` con la instrucción explícita de leer y seguir `.claude/agents/revisor-visual.md` al pie de la letra (mismo efecto: contexto limpio, sin conocer las intenciones de quien construyó).
- Los 4 veredictos llegaron: **ninguno pasó el gate** (≥36/40 usabilidad y ≥16/20 craft). Se detectó que el screenshot de landing salió CASI EN BLANCO (solo Hero y Footer) — causa raíz: bug del SCRIPT de captura, no de la página real (las secciones usan `whileInView` de framer-motion, que no dispara sin scroll real; Playwright no scrollea antes del `fullPage` screenshot). Corregido en `scripts/capturar-screenshots.mjs` (scrollea la página en pasos de 400px antes de capturar) y se recapturó. También se ajustaron los datos de ejemplo de pantalla-principal (racha activa + 1 ejercicio tachado, antes salía sin nada marcado). Se relanzó el revisor para landing y pantalla-principal con las capturas corregidas; onboarding y paywall ya tenían capturas válidas, sus veredictos se mantienen.

### Resultados (primera pasada, antes del pulido de esta sesión)
- **onboarding** (paso 1): NO LISTA — Usabilidad 31/40, Craft 11/20. Defectos: vacío muerto bajo las 2 opciones (fondo plano sin profundidad), cero dispositivo ownable, sin salida/cancelar visible.
- **paywall**: NO LISTA — Usabilidad 30/40, Craft 12/20, Copy 18/20 (el copy SÍ pasa). Defectos: "Restaurar compra" sin onClick (parece interactivo, no hace nada), checkmarks del sistema "✓" en vez de custom, sin dispositivo ownable, garantía lejos del CTA, falta stagger en la mitad de los bloques.
- **landing / pantalla-principal**: veredicto inicial inválido por el bug del script — repetido, resultado pendiente de la notificación.

## Pendientes de pulido detectados por revisor-visual (Sesión 7, antes de declarar listas las 4 pantallas del dinero)
- [ ] onboarding: llenar el vacío bajo las opciones (centrar o agregar contenido), dar profundidad al fondo (mesh/tinte sutil), aplicar el tachado verde/chip de FICHA-ARTE como guiño de identidad, agregar salida/cancelar visible.
- [ ] paywall: conectar o quitar "Restaurar compra" (botón sin función hoy), cambiar los "✓" del sistema por checkmarks custom (círculo acento 12% + Check de Lucide), agregar el dispositivo ownable, repetir la garantía cerca del CTA principal, envolver los bloques restantes en stagger de entrada.
- [ ] pantalla-principal (veredicto confirmado con captura corregida): NO LISTA — Usabilidad 30/40, Craft 14/20. El color de la flama NO es un bug (verificado en código: ambas ramas son consistentes) — sí se anota que el gris "en riesgo" es más tenue que el `#B8860B` de aviso de FICHA-ARTE. Falta: `whileTap` en botones (Registrar, Terminar, toggle), poder deshacer/editar un registro ya marcado hecho, animar el conteo de la racha al cargar + guard de `prefers-reduced-motion`, avisar si falla la sincronización remota. Nota aparte: el indicador de modo desarrollo de Next.js (círculo "N") tapa la pestaña activa en el screenshot — es solo de `next dev`, no aparece en producción, no cuenta como defecto real.
- [ ] landing: pendiente de la repetición (el primer veredicto no es válido, screenshot roto por el bug del script).

## Dirección de Arte (Sesión 2 — CERRADA, cosa juzgada)
- FICHA-ARTE.md: existe y aprobada — 28/08/2026 (ver archivo en la raíz)
- ¿Hubo referencia visual del usuario?: NO → REFERENCIA-INVESTIGACIÓN (fusión Hevy/Fitbod/Strong, 16 PASO 0.2bis)
- Opción elegida: **C — "Cuaderno de Sala"** (el usuario detectó un bug de layout en el primer render — hueco vacío antes del botón de Rescate — ya corregido y reverificado)
- Resumen: fondo `#F5F1EA` · superficie `#FFFFFF` · texto `#211D17`/`#8A7F6A` · acento `#5C7A1F` (verde tinta, solo en progreso/CTA/check) · Display y Body: **Instrument Sans** única familia · radio 14-16px cards, 100px pills
- Personalidad: Sobrio (dominante) · Técnico · Cálido — voz: experto sobrio con calidez cercana
- Dispositivo ownable: tachado verde sobre ejercicio completado (estilo cuaderno real) + chips de meta con borde fino
- Modo: claro — derivado porque el avatar busca calma/certeza, no adrenalina (evita el reflejo "fitness = oscuro + neón")
- REGISTRO ANTI-REPETICIÓN: verde `#5C7A1F` + Instrument Sans vetados para el próximo proyecto del SO
- Página comparativa (evidencia, incluye las descartadas A/B): `docs/revisiones/direcciones-abc.html`

## Qué es esta app (3 líneas máximo)
App con dos rutas de nivel: Ruta Principiante (programa fijo de 90 días que dice exactamente qué hacer cada día) y Ruta Intermedio (rutinas efectivas con progresión para salir del estancamiento), ambas en gimnasios comerciales (Smart Fit/Bodytech), con alternativas de un toque cuando la máquina está ocupada. Monetización por suscripción (Freemium: semana 1 gratis, luego de pago).

> ⚠️ Corrección del usuario (28/08/2026): NO es un programa cerrado solo de 90 días para principiantes. Debe servir también a nivel intermedio que ya no es novato pero quiere rutinas efectivas y romper el estancamiento. Esto deja de ser un "desbloqueo automático al día 90" y pasa a ser una ruta propia desde el día 1 (el usuario elige su nivel al entrar).

## Promesa central
"GymEvo ayuda al joven de gimnasio comercial en LATAM —sea principiante o nivel intermedio— a construir un físico atlético y entrenar con la seguridad de un experto, sin depender de entrenadores que lo ignoran ni perder tiempo cuando las máquinas están ocupadas, mediante un mapa táctico (fijo de 90 días para principiantes, o de progresión para nivel intermedio) con un botón de alternativa en tiempo real."

## Reporte de validación (Sesión 1) — ya investigado por el usuario, no se repite
- Veredicto: Excelente oportunidad (validación ya hecha por el usuario con su propio prompt de investigación)
- Apps de referencia: Hevy (~$800K MRR, queja: exige armar rutina propia) · Fitbod (queja: IA cambia rutina a diario, sin progresión) · MadMuscles/BetterMe (queja: cobros ocultos, estafas) · Strong (queja: pantalla en blanco, asume experto)
- Lo que los usuarios odian de la competencia (nuestra oportunidad): 1) tener que armar su propia rutina desde cero, 2) que la IA cambie el plan cada día sin lógica ni progresión, 3) cobros ocultos/suscripciones no autorizadas
- Brecha LATAM confirmada: Sí — dolor de hacinamiento en gimnasios de cadena económica (Smart Fit, Bodytech) y desconfianza por estafas de apps fitness, sin solución local que dé plan fijo + alternativa en tiempo real
- Precio de referencia del mercado: Hevy $23.99 USD/año; propuesta propia $4.99/mes o $29.99/año

## Avatar y venta (Sesión 1)
- FICHA-AVATAR.md: existe y aprobada — 28/08/2026 (ver archivo en la raíz; todo el copy de venta se traza a ella)
- FICHA-MERCADO.md: existe — 28/08/2026 (ver archivo en la raíz; precio y trial documentados, plazos de garantía de Hotmart marcados como pendientes de verificar contra la pasarela real en Sesión 6)
- Avatar: Mateo, 26 años, oficinista, va a Smart Fit a las 6:30 PM (hora pico), paraliza frente a las máquinas, se avergüenza, termina haciendo caminadora y se va frustrado.
- Nivel de consciencia: 4/5 — Consciente de la Solución, quemado y escéptico (ya probó apps de IA, influencers, y fue estafado por apps de cobros ocultos)
- Dolor #1: terror a lesionarse por mala técnica + vergüenza de no saber qué hacer frente a las máquinas
- Deseo #1 (superficial): saber exactamente qué máquina usar y cómo. Deseo profundo: entrar con autoridad de experto, dejar de sentirse invisible/juzgado
- Objeciones clave: "¿Y si mi gimnasio no tiene la máquina o está siempre lleno?" · "¿Es otra app con cobros ocultos?" · "¿Qué pasa después del día 90?"
- Ángulo de venta ganador: "El entrenador que tu gimnasio te cobra pero nunca te da" — vende certeza y protección contra el caos, no "fitness"
- Diferenciador: No usa IA que cambia el plan al azar. Plan fijo de 90 días + Botón de Rescate para sustituir ejercicios al instante.
- Frases literales del cliente (para copy): "Se la pasan comadreando o coqueteando" · "El exceso de personas hace que sea imposible usar las máquinas" · "Sigue cambiándome de rutina casi a diario" · "Llevo meses y me veo igual" · "Entrenar sin estrategia"

## Estrategia de monetización (Sesión 1 — NO cambiar sin validar)
- Modelo: **Modelo 2 — Onboarding + Paywall de prueba** (decidido por el SO, matriz A-F de 02C: fitness/wellness B2C, uso diario, necesita HÁBITO). Reemplaza el "Freemium" que proponía el documento del usuario — el freemium puro es el modelo de PEOR conversión (2.1%) según 02C; el onboarding+trial es el que usan Duolingo/Cal AI/Noom con hasta +234% de conversión.
  Flujo: Onboarding personalizado (elige nivel + meta metabólica) → ve su plan del Día 1 generado para él → Paywall → trial de 7 días con acceso completo → pago.
- Trial: 7 días (equivalente a la "semana 1 gratis" del reporte, pero como trial estructurado con tarjeta, no como freemium indefinido) — aviso claro antes del cobro en el Día 6 (transparencia radical, responde directo a la objeción #2 del avatar: "¿cobro oculto?")
- Pricing: $4.99 USD/mes o $29.99 USD/año ≈ $2.49/mes mostrado como "2 meses gratis" (benchmark Hevy $23.99/año) — mantiene el precio que ya validó el usuario
- Riesgo de churn identificado: día 90 (fin del programa Principiante) — mitigación: transición fluida a la Ruta Intermedio (no es "desbloqueo especial", es la ruta que ya existe para seguir progresando)

## MVP — funciones núcleo (ajustado con la corrección del usuario, Sesión 1)
0. Selector de NIVEL al entrar: Principiante (plan fijo 90 días) vs Intermedio (rutina de progresión anti-estancamiento) — antes del selector de meta metabólica
1. Selector de ruta metabólica (Músculo / Pérdida de grasa) — aplica a ambos niveles
2. Plan diario con ilustraciones biomecánicas simples: fijo e inalterable en Principiante; con progresión de carga/variación en Intermedio
3. Botón "Alternativa de ejercicio" (Botón de Rescate) para sustituir máquinas ocupadas al instante — ambos niveles
4. Temporizador de descanso integrado entre series
5. Registro histórico de pesos/cargas — clave en Intermedio para medir progresión y romper estancamiento
- NO construir en el MVP: integraciones con redes sociales, IA generativa de rutinas, sincronización con smartwatches
- Primera victoria (<5 min): elegir nivel + meta metabólica y ver la rutina exacta del día, lista para iniciar cronómetro
- Pendiente de definir con el usuario más adelante (no bloquea el plan maestro): cómo pasa un Principiante a Intermedio (¿automático al día 90, o el usuario elige entrar directo a Intermedio si ya no es novato?)

## Sesión 5bis (29/08/2026) — Pulido de la app interna a pedido del usuario
- Interruptor "Descanso automático entre series" en `/app`: el usuario decide antes de entrenar si el temporizador arranca solo al registrar una serie, o si prefiere descansar a su ritmo (campo `descansoAutomatico` en `lib/routine.ts`, default true, retrocompatible con progreso guardado antes de este campo).
- Enlace "¿Cómo se hace?" en cada ejercicio: abre una búsqueda de YouTube (`[ejercicio] técnica correcta`) en pestaña nueva. Resuelve la duda de técnica sin producir contenido propio.
- Evaluadas y decididas explícitamente: (a) NO se agregó texto de técnica por ejercicio (el usuario solo pidió el interruptor + el enlace) — pendiente si se quiere más adelante; (b) NO se adapta nada por género — ninguna pantalla pregunta género hoy y la técnica correcta de un ejercicio no depende de eso; (c) ilustraciones/video PROPIOS por ejercicio quedan pendientes de decisión explícita del usuario — implican costo/tiempo real (diseño o licencias), no se construyen sin su OK.
- **Mascota — decidido con el usuario (29/08/2026), arte PENDIENTE**: sí lleva personaje ("muñequito con personalidad propia", no solo un ícono minimalista). El usuario genera los bocetos con IA de imagen por su cuenta; el brief que se le dio: personaje humano tipo "entrenador confiable" (NO animal, NO infantil/tierno — Mateo no quiere sentirse tratado como niño), paleta limitada a los tokens ya aprobados (fondo #F5F1EA, acento #5C7A1F, tinta #211D17), con el logo en la ropa, 4 poses: reposo/inicio, aviso de fin de descanso, inicio de ejercicio, fin de entrenamiento (celebración CONTENIDA, sin saltos ni confeti — coherente con la personalidad "Sobria" ya compilada). Pendiente: que el usuario traiga los bocetos para elegir uno antes de implementar nada en código.
- Mockup de referencia recibido (29/08/2026): clipart genérico de "Personal Trainer" (camiseta negra, tabla, silbato). Sirve de referencia de pose/personalidad, NO de paleta/estilo — se le indicó al usuario ajustar antes de generar sus bocetos: ropa en tono papel/crudo (no negro) con el acento #5C7A1F como único detalle, línea plana/editorial (no clipart 3D con degradados) para no chocar con la UI flat existente, y el logo de GymEvo en vez de texto "PERSONAL TRAINER".

## Gamificación y retención (loop implementado en Sesión 5)
- Loop del hábito (Hooked): Gatillo [hora de entreno que eligió en el onboarding] → Acción [marcar cada ejercicio de hoy como hecho] → Recompensa [tachado verde + racha que sube] → Inversión [historial de pesos acumulado — cuesta más abandonar mientras más tiene registrado]
- Mecánica elegida: racha diaria (sin XP ni ligas — el nicho y la personalidad "Sobria" de FICHA-ARTE no piden más capas)
- Primera victoria que celebra el onboarding (<60s): ver el plan del Día 1 generado con su meta real (ya construida en Sesión 4)
- Momentos emocionales (56) implementados: M0 ritual diario (pantalla `/app`) · M4 racha en riesgo (llama apagada, sin pánico ni rojo) · hito de racha 7/30/100 días (número que cuenta, sin confetti — coherente con el compilador de personalidad "Sobrio" de FICHA-ARTE)
- Notificaciones push: NO implementadas todavía (requieren backend/servicio push — Sesión 6)

## Secuencia maestra de construcción (NO saltar)
- Estado de la secuencia: Landing ✅ construida · Onboarding ✅ construido · Paywall ✅ construido · Login ✅ construido (mock) · App interna ✅ construida (sin backend) · Servicios externos: pendiente (Sesión 6)
- Ruta aprobada: `/` → `/onboarding` → `/onboarding/generando` → `/onboarding/plan` (vista previa Día 1) → `/paywall` → `/login` → `/app`

## Decisiones técnicas (NO re-discutir sin pedirlo el usuario)
- Framework: **Next.js App Router** (decidido el 28/08/2026) — el default del stack pineado (51): necesitamos SEO en la landing de ventas, API routes para el webhook de Hotmart, y auth. Vite queda descartado porque es solo para herramientas puras post-login sin landing pública.
- Offline-first ligero: el contenido del día (rutina + ilustraciones) se cachea localmente (localStorage/IndexedDB vía Service Worker simple) tras la primera carga, para que funcione aunque el sótano del gimnasio tenga mala señal (riesgo #3 del reporte). El registro de pesos se guarda local y sincroniza al recuperar conexión. No es una PWA offline completa — es cache-first del contenido estático del día.
- Auth: **Supabase Auth con magic link (sin contraseña)** — encaja con el modelo onboarding-first: el usuario hace todo el onboarding y ve su plan del Día 1 SIN cuenta; la cuenta se crea recién en el paywall/checkout (evita fricción temprana, responde a la objeción de "otra app que pide mis datos de una"). Sesión larga (30-90 días, default de Supabase) porque es una app de consumo diario.
- Base de datos: Supabase (Postgres) con RLS. Tablas propias del usuario (`profiles`, `workout_logs`, `user_progress`) llevan `user_id` + RLS estricta (cada quien ve solo lo suyo). El contenido de las rutinas (`exercises`, `routine_days`, `exercise_alternatives`) es catálogo fijo de solo-lectura pública, sin datos personales — no necesita RLS por usuario.

### Mapa de pantallas
```
/                    → Landing (venta)
/onboarding          → Selector de nivel (Principiante/Intermedio) + meta (Músculo/Grasa) — sin cuenta
/paywall             → Trial 7 días + oferta $4.99/mes o $29.99/año
/login               → Magic link (se crea la cuenta aquí, tras decidir pagar)
/app                 → Plan del día (protagonista) + botón de Rescate + temporizador
/app/historial       → Registro de pesos/cargas por ejercicio
/app/perfil          → Nivel actual, meta, plan, cerrar sesión
```
Total: 6 pantallas únicas (dentro del límite de 8).

### Modelo de datos (resumen — el SQL exacto se escribe en Sesión 6/Deploy)
- `profiles`: id (FK auth.users), nivel (principiante/intermedio), meta (musculo/grasa), dia_actual, fecha_inicio, racha
- `exercises` (catálogo): id, nombre, grupo_muscular, ilustracion_url, tipo (máquina/peso libre)
- `exercise_alternatives` (catálogo): exercise_id → alternative_exercise_id (para el Botón de Rescate)
- `routine_days` (catálogo, contenido curado por nosotros): nivel, meta, numero_dia, lista de ejercicios+series+reps+descanso
- `workout_logs` (usuario): user_id, exercise_id, fecha, peso, reps, series
- `user_progress` (usuario): user_id, racha_actual, ultimo_dia_completado, hitos desbloqueados

## Sesiones completadas ✅
- Sesión 1 — Validación + avatar + monetización (02C) + framework + arquitectura + modelo de datos + auth — cerrada 28/08/2026
- Sesión 2 — Identidad visual: FICHA-ARTE.md cerrada, opción C "Cuaderno de Sala" — cerrada 28/08/2026
- Sesión 3 — Página de ventas: Next.js scaffolded, kit de landing (plantillas-codigo/landing) copiado a components/landing, tokens.css tematizado con FICHA-ARTE, copy marcado en docs/copy/landing.md, 10 secciones canónicas compuestas en app/page.tsx, páginas legales creadas (/privacidad /terminos /reembolsos) — cerrada 28/08/2026
- Sesión 4 — Onboarding + Paywall + Login: `/onboarding` (6 pasos: nivel, meta, frustración con eco de dolores reales, reconocimiento personalizado por objeción, horario, compromiso de días/semana con slider) → `/onboarding/generando` (loading persuasivo 4.8s con líneas reales) → `/paywall` (timeline de trial Hoy/Día 6/Día 7, planes anual+mensual, CTA, trust row) → `/login` (magic link mock con estados enviando/enviado/error). Probado de punta a punta en navegador — cerrada 28/08/2026
- Sesión 4bis (29/08/2026) — Onboarding mejorado tras comparar contra un análisis externo (Gemini) sobre apps de fitness/wellness (Verv): (1) NUEVA pantalla `/onboarding/plan` — vista previa REAL del Día 1 entre "generando" y el paywall (antes faltaba: pedíamos pago sin mostrar el resultado, contra nuestra propia doctrina de Cal AI/Duolingo). Muestra los ejercicios reales del Día 1 + el resto de la semana bloqueado con honestidad (sin blur falso). (2) Se agregó **tempo de ejecución** (ej. "3-1-1" seg) a cada ejercicio en `lib/routine.ts` — cumple la promesa original de la idea validada ("a qué velocidad exacta hacerlo") que nunca se había implementado; visible en `/app` y en la vista previa. Decisiones rechazadas del análisis externo, con razón: NO se agregan reseñas/testimonios (fabricarlos viola la ética del sistema y el diferenciador #1 del avatar) NI datos biométricos edad/peso/altura (ninguna función los usa todavía; sería fricción decorativa). NO se cambia la barra de progreso a puntos (el archivo 50 lo prohíbe explícitamente).
- Sesión 3bis (29/08/2026) — Elevación de la landing para conversión/escaneabilidad: headline nuevo ("Nunca más sin saber qué hacer en el gym", eco literal del dolor #1 de FICHA-AVATAR) · CTA repetido tras la Solución y tras la Garantía (antes solo estaba en hero/mid-page/oferta) · garantía corregida para reflejar los 14 días reales (7 trial + 7 Hotmart, FICHA-MERCADO.md) en vez de decir "7 días" de forma ambigua. Auditoría de escaneabilidad a 375px pasada — ver tabla abajo.
- Sesión 5 — App interna: shell de 3 secciones (`/app` Plan del día, `/app/historial`, `/app/perfil`) con tab bar inferior. Plan del día = M0 "ritual diario" (56): split de 4 días (Empuje/Tirón/Piernas/Full) según catálogo de 20 ejercicios con alternativas, registro de peso por serie, Botón de Rescate (swap a alternativa), temporizador de descanso, tachado verde al completar (dispositivo ownable de FICHA-ARTE), estado M4 "racha en riesgo" (llama apagada, sin pánico) y celebración de hito de racha (7/30/100 días, estilo sobrio sin confetti). Progreso en localStorage (`lib/routine.ts`) — cerrada 28/08/2026

## Sesión en progreso 🔧
Sesión 6 (integraciones reales y seguridad) — en curso.
- Pre-flight P1 completado: se encontraron y corrigieron 2 bugs reales que el proyecto arrastraba sin que el usuario lo pidiera (no se buscaban, los sacó a la luz `npx eslint .` que no se había corrido explícito antes): (a) `<a href="/">` en vez de `<Link>` en app/login y Hero.tsx (regla next/next), (b) un bug de hydration que YO MISMO introduje al intentar arreglar el lint `react-hooks/set-state-in-effect` (cambié `useState(null)+useEffect` a `useState(() => leerX())` en 5 archivos — pasaba el lint pero rompía en runtime porque sessionStorage/localStorage no existen en el server). Revertido a `useState(null)+useEffect` + comentario `eslint-disable-next-line` puesto justo encima de la línea exacta del `setState`. Verificado en navegador: 0 errores de hydration en las 5 pantallas afectadas.
- `eslint.config.mjs`: se agregó `plantillas-codigo/**` y `docs/**` a `globalIgnores` (el kit vendored del SO no debe lintearse, igual que ya excluía tsconfig.json).
- `components/landing/Hero.tsx`: primera vez que se toca este archivo del kit → el lint de diseño exigió convertir valores de píxel arbitrarios a clases Tailwind por defecto (max-w-*, text-*) — hecho.
- **GitHub: ✅ conectado** (31/08/2026) — repo `oscarmint/gymevo`, push exitoso.
- **Vercel: ✅ conectado y verificado** (31/08/2026) — dominio público `https://gymevo-eta.vercel.app`, landing carga sin errores de consola.
- **Supabase: ✅ código y tablas listos** (31/08/2026) — proyecto, código de conexión, tablas `profiles`/`workout_logs` con RLS (aplicadas directo por MCP), y `/login` con magic link real. Commit+push hechos.
- ✅ **Incidente resuelto** (31/08/2026): tras el push, `gymevo-eta.vercel.app` mostró "Internal Server Error" (`Invalid supabaseUrl` en el proxy, por un valor mal pegado en `NEXT_PUBLIC_SUPABASE_URL` en Vercel). Usuario corrigió la variable con el valor exacto confirmado por Supabase MCP y volvió a desplegar. Verificado en navegador y con `get_runtime_errors` (Vercel MCP): 0 errores en `/`, `/login`, `/app`.
- Nota técnica: se descubrió acceso directo a Supabase y Vercel vía MCP en esta sesión — se puede aplicar SQL, revisar tablas y auditar seguridad sin pedirle al usuario que copie/pegue nada en el panel. Usar esto de ahora en adelante en vez de pedirle pasos manuales cuando sea posible.
- **Resend: ✅ conectado como SMTP de Supabase** (01/09/2026) — sin dominio propio todavía (el usuario no tiene presupuesto ahora); remitente de pruebas `onboarding@resend.dev` (solo entrega al correo de la cuenta de Resend, suficiente para seguir probando). Límite de correos subió de 2/hora a 30 (confirmado en logs de Supabase). Cuando haya dominio, cambiar el remitente a algo con marca propia.
- ✅ **Bug de configuración corregido** (01/09/2026): la "Site URL" de Supabase Auth apuntaba a `http://localhost:3000` desde la creación del proyecto — los magic links pedidos desde `gymevo-eta.vercel.app` redirigían a la máquina local del usuario en vez de la producción (Supabase usa la Site URL como fallback cuando el `redirectTo` no está en la lista de Redirect URLs permitidas). Corregido en el dashboard: Site URL y Redirect URLs actualizadas a `https://gymevo-eta.vercel.app`. Verificado con `query_logs`: el `referer` de las peticiones `/otp` ya muestra el dominio de producción.
- **Video de fondo + nombre de usuario** (01/09/2026): el video de la landing (`/videos/hero-gimnasio.mp4`) también se agregó como fondo de "Plan de hoy" (mismo tratamiento de scrim fuerte para legibilidad). Perfil ahora tiene un saludo editable ("Hola, {nombre}") — nueva columna `profiles.nombre` (migración `0002_nombre_perfil.sql`), guardado local + remoto.
- Pendiente: dominio propio → Hotmart (un pedido a la vez, Protocolo Cero Secretos en Chat — nunca pedir que pegue valores secretos en el chat) cuando el usuario tenga presupuesto; mientras tanto, seguir el pulido pendiente de las 4 pantallas del dinero (Sesión 7).

## Próximas sesiones 📋
- Sesión 6: integraciones reales (Supabase, Hotmart, dominio) y seguridad
- Sesión 7: testing, animaciones, pulido y rigor de entrega

## Problemas conocidos ⚠️
- **veredicto:pantalla-principal / veredicto:onboarding — cambio real grande, pendiente intencional (03/09/2026)**: la reestructuración del motor de entrenamiento (Día 4 nuevo, cardio por ruta, principiante/intermedio real, seguimiento de progreso) tocó `app/app/page.tsx`, `app/app/perfil/page.tsx`, `app/app/historial/page.tsx` y `app/onboarding/page.tsx` de verdad — no es solo estilo, es contenido y flujo nuevo. Antes de la próxima venta real, correr `revisor-visual` en Plan de hoy (con los 3 estados: entrenamiento normal, Día 4, descanso) y en el paso de días del onboarding con el aviso de 7 días visible.
- **veredicto:paywall — cambio real de composición (4 planes), pendiente intencional (03/09/2026)**: se pasó de 2 a 4 tarjetas de plan (Mensual/Trimestral/Semestral/Anual) con badges de ahorro nuevos, a pedido explícito del usuario. El paywall ya estaba NO LISTA de antes por el pendiente de identidad — este cambio de precios se suma a esa misma cola. Antes de la próxima venta real, correr `revisor-visual` sobre el paywall con el tema oscuro Y los 4 planes juntos en una sola pasada (no separada por cada cambio).
- **veredicto:onboarding — cambio real de copy, pendiente intencional (03/09/2026)**: se reescribió el subtítulo del paso "sexo", el texto vacío de `TarjetaRuta` (compartido por las 7 pantallas del cuestionario) y el texto de su tarjeta de beneficio, con la redacción exacta que dio el usuario. Es copy más claro/profesional sobre la MISMA estructura ya revisada (mismo layout, mismos componentes, mismo largo aproximado) — no amerita una re-pasada aislada del `revisor-visual` por sí sola, pero se suma a la lista de cambios reales de onboarding pendientes de una revisión de cierre conjunta (ver la entrada del rediseño 3D/oscuro más abajo).
- **screenshots de la landing desactualizados tras el rediseño oscuro (03/09/2026)**: `public/screenshots/frame-onboarding.png`, `frame-plan-del-dia.png`, `frame-rescate.png` y `frame-historial.png` (usados en `AppPorDentro` de `app/page.tsx`) se capturaron con la identidad clara anterior — se ven con fondo claro dentro de un mockup de teléfono sobre una landing ahora oscura. Re-capturar con la app corriendo en el tema nuevo (requiere sesión autenticada o el bypass documentado de `GYMEVO_SKIP_AUTH_GUARD` en `proxy.ts`) antes de dar la landing por terminada.
- **veredicto:landing / veredicto:onboarding / veredicto:paywall / veredicto:pantalla-principal — REDISEÑO COMPLETO, 03/09/2026 (pendiente intencional pero de MAYOR peso que las rondas anteriores)**: este cambio SÍ es un rediseño real de identidad (paleta completa clara→oscura + cambio de tipografía) en las 4 pantallas del dinero simultáneamente, a pedido explícito y aprobado del usuario. Los veredictos anteriores (todos calibrados sobre la identidad CLARA) ya no aplican como medición de esta pantalla — quedan formalmente obsoletos, no solo "caducados" por ruido del hook. Antes de la próxima venta real, se recomienda una ronda completa de `revisor-visual` en las 4 pantallas del dinero sobre el tema oscuro nuevo (no se hizo en esta sesión por alcance/tiempo — el usuario pidió continuar directo al panel de administrador).
- **veredicto:onboarding — pendiente intencional, esta vez SÍ es un cambio real de composición (03/09/2026)**: se quitó la decoración de "espiral de cuaderno" de `TarjetaRuta` (afecta las 7 pantallas del cuestionario, ya que la tarjeta es compartida) y se cambió su padding de `pr-9 pl-9` a `px-6` simétrico — a pedido directo del usuario, que mandó captura señalando el desorden visual. Es una CORRECCIÓN (arregla un defecto real que el usuario vio, no agrega nada nuevo) y usa el mismo lenguaje visual ya aprobado (mismo hairline, mismo ícono, misma tipografía) — no cambia el dispositivo ownable protegido (tachado + chips punteados). Aun así, al ser un cambio de composición real (no solo una clase CSS), se recomienda pedir un veredicto fresco del `revisor-visual` la próxima vez que se cierre el onboarding, en vez de seguir posponiéndolo indefinidamente.
- **veredicto:landing / veredicto:onboarding / veredicto:paywall — ronda de refuerzo 3D, 03/09/2026 (pendiente intencional, mismo criterio que las rondas anteriores)**: esta ronda tocó `components/landing/ui.tsx` (el `BotonCTA` compartido, usado por Hero/Oferta/CtaFinal/CTA flotante — SÍ es código real de landing, a diferencia de rondas anteriores que solo tocaban `app/app/*`), `app/onboarding/page.tsx` (chips de opción + modal salir), `app/paywall/page.tsx` (tarjetas de plan), `app/app/page.tsx`, `app/app/perfil/page.tsx` y `app/app/historial/page.tsx`. Es la MISMA adición de profundidad consistente (canto 3D, ahora más grueso) sobre elementos ya aprobados, sin cambiar composición/jerarquía/copy — se pospone el re-lanzamiento del `revisor-visual` de las 4 pantallas del dinero hasta que se acumulen más cambios reales o el usuario pida una revisión de cierre; si se sigue iterando el 3D, no volver a re-lanzar por cada ajuste de grosor, solo cuando el usuario dé el visto bueno final al look.
- **veredicto:landing / veredicto:onboarding — reconfirmado otra vez 03/09/2026 (ronda de reps/llama/etiqueta Perfil)**: los 3 ajustes de esta ronda tocaron `app/app/page.tsx` (reps por defecto en 6, llama de racha con relleno real) y `app/app/perfil/page.tsx` (etiqueta de unidad de peso) — ninguno de los dos es `app/page.tsx` (landing) ni `app/onboarding/page.tsx` (onboarding). El hook los lista como "código más nuevo" porque compara contra CUALQUIER `.tsx` del repo, no contra el archivo real de cada pantalla — mismo patrón ya documentado abajo. Nada nuevo que re-lanzar en landing. Onboarding sigue con su pendiente intencional de la ronda anterior (cambio real en sus 2 CTA, ver entrada de abajo).
- **veredicto:paywall — sin cambios en esta ronda (03/09/2026)**: `app/paywall/page.tsx` no se tocó en los 3 ajustes de reps/llama/etiqueta. Sigue NO LISTA por el pendiente de identidad ya documentado, sin novedad.
- **veredicto:landing — sigue siendo falso positivo, reconfirmado 03/09/2026**: el cambio de hoy (profundidad 3D puntual) solo tocó `app/globals.css`, `components/landing/tokens.css` (nuevas variables `--accent-deep`/`--accent-2-deep`, sin uso en ningún botón de la landing) y pantallas de `/app`, `/onboarding`, `/paywall` — `app/page.tsx` (el archivo real de landing) no cambió. Confirmable con `git log -- app/page.tsx`. No hace falta re-lanzar el revisor de landing.
- **veredicto:onboarding — CADUCADO de verdad esta vez, pendiente intencional de re-lanzar (03/09/2026)**: `app/onboarding/page.tsx` sí cambió hoy (se quitó `whileTap` de Framer Motion en los 2 CTA "Continuar"/"Fijar mi meta" y se agregó la clase `.boton-3d` — canto 3D pedido explícitamente por el usuario, ver checkpoint de arriba). Es una adición de profundidad visual consistente sobre un botón ya aprobado (mismo color, mismo texto, misma posición), no un rediseño de composición/jerarquía — verificado en el navegador que el `box-shadow` se aplica correctamente y que el botón sigue siendo tappable. Se pospone el re-lanzamiento del `revisor-visual` (criterio ya usado en la sesión pasada para el cambio de ícono del paso "sexo"): si se agregan MÁS cambios visuales a onboarding antes de la próxima revisión de esta pantalla, correr el revisor con todos juntos en vez de uno por uno.
- **veredicto:paywall — sigue NO LISTA, ahora con el cambio de hoy encima (03/09/2026)**: además del pendiente de identidad ya documentado abajo (02/09/2026), hoy se le quitó `whileTap` al CTA "Empezar mis 7 días gratis" y se le agregó `.boton-3d` (mismo pedido del usuario). El veredicto NO LISTA sigue siendo válido y esperado — esta pantalla no se re-lanza hasta que el usuario decida cómo resolver el eje de identidad pendiente (ver entrada de abajo); el cambio de hoy no pretende cruzar el gate por sí solo, es la misma profundidad puntual aplicada de forma consistente en toda la app.
- **veredicto:onboarding — CADUCADO es ruido del hook otra vez (03/09/2026)**: tras el veredicto LISTA con el paso de sexo (36/40 · 16/20), se aplicaron los 2 defectos menores no-bloqueantes que el propio revisor recomendó en esa misma pasada: (1) cambiar el ícono `PlayCircle` (repetido de otro paso) por `Activity` en la `TarjetaBeneficio` del paso "sexo", (2) corregir un comentario interno que decía "6 pasos" en vez de "7". Ningún cambio de layout, texto visible al usuario (fuera del ícono, que es exactamente el cambio que el revisor pidió) ni estructura — no amerita una re-pasada completa del revisor-visual. El hook seguirá marcando "caducado" en cada commit futuro que tenga cualquier `.tsx` más nuevo que `docs/revisiones/onboarding-veredicto.md`; verificar con `git log -- app/onboarding/page.tsx` antes de asumir que hace falta re-lanzar.
- **veredicto:landing — sigue siendo falso positivo (03/09/2026)**: `app/page.tsx` no ha cambiado desde su último veredicto LISTA — los commits recientes (TRM/COP, macros científicos, onboarding sexo) tocaron `components/landing/Oferta.tsx` (solo agregó la línea de precio en COP, sin rediseño) y otros archivos, no la landing en sí. Confirmable con `git log -- app/page.tsx`.
- **veredicto:paywall — NO LISTA, pendiente intencional (02/09/2026, fin de sesión)**: 8 rondas de corrección real hoy mismo (ver checkpoint arriba) dejaron el puntaje en 33/40 usabilidad · 13/20 craft · 19/20 copy — todos los bugs de comportamiento/accesibilidad quedaron cerrados; lo que falta es una decisión de identidad/paleta (FICHA-ARTE, "cosa juzgada") que el usuario todavía no ha tomado. NO relanzar el revisor-visual de paywall hasta que el usuario elija entre redecidir la paleta base o invertir una sesión de diseño dedicada a un dispositivo ownable nuevo — relanzarlo sin uno de esos dos cambios reales solo repetirá el mismo puntaje.
- **veredicto:landing / veredicto:onboarding — CADUCADO es ruido del hook, no un pendiente real (02/09/2026)**: el hook compara contra CUALQUIER `.tsx` más nuevo que el veredicto, no contra el archivo real de cada pantalla. Los commits recientes (rutina real del ebook, calculadora de macros, registro por serie, paywall) tocaron `app/app/page.tsx`, `app/app/historial/page.tsx`, `app/app/perfil/page.tsx` y `app/paywall/page.tsx` — NINGUNO tocó `app/page.tsx` (landing) ni `app/onboarding/page.tsx` desde sus respectivos veredictos LISTA (landing 01/09, onboarding 02/09). Confirmable con `git log -- app/page.tsx` / `git log -- app/onboarding/page.tsx`. No hace falta re-lanzar ninguno de los dos revisores.
- **veredicto:paywall — CADUCADO DE VERDAD esta vez (no falso positivo)**: `app/paywall/page.tsx` cambió de verdad (01/09/2026, capa de cobro real — el CTA ahora abre el checkout de Hotmart en vez de navegar directo a `/login`). El cambio es solo de comportamiento (a dónde lleva el botón), no tocó nada visual/layout, así que es muy improbable que mueva el puntaje de 36/40·16/20·17/20 — pero no se re-lanzó el revisor-visual todavía. Re-lanzarlo cuando se cierre por completo la capa de pago (tras la prueba E2E real). **Reconfirmado 02/09/2026 con `git merge-base --is-ancestor`**: `app/paywall/page.tsx` (`b0489b3`) es efectivamente posterior al veredicto (`277d414`) — sigue pendiente, no es ruido del hook esta vez.
- **veredicto:landing — sigue siendo falso positivo, reconfirmado 02/09/2026**: con `git merge-base --is-ancestor` se confirmó que `app/page.tsx` (`5c2d266`) es ANTERIOR al veredicto LISTA (`92a9954`) — los cambios que disparan la alerta del hook (Historial, "Explicación del ejercicio", modal de onboarding) tocaron otros `.tsx`, no el archivo real de landing. Nada que re-lanzar.
- Hallazgos de la auditoría senior (--rápido, 01/09/2026) NO críticos, pendientes para una futura sesión: (1) el Historial de entrenamientos (`app/app/historial/page.tsx`) es una lista plana con más de la mitad de la pantalla vacía — falta un dato destacado + gráfico de progresión por ejercicio; (2) su estado vacío no tiene botón, solo texto; (3) la decisión de offline-first ligero (ver "Decisiones técnicas") nunca se construyó — sin Service Worker ni manifest; (4) Supabase avisa "Leaked Password Protection Disabled" — no aplica hoy (la app no usa contraseñas) pero es gratis activarlo; (5) `app/auth/callback/route.ts` no valida que el parámetro `next` empiece con `/` (riesgo bajo de open-redirect, corrección barata).
- Visual del hero y los 4 frames del carrusel ("La app por dentro") son PLACEHOLDERS honestos (marco punteado + ilustración/nombre de pantalla) — se reemplazan por screenshots reales cuando la app interna exista (Sesión 5). No declarar la landing 100% terminada hasta ese reemplazo (regla del 19).
- `scripts/audit-conversion.sh` marca varios "críticos" que son falsos positivos del propio script contra los archivos DEL KIT del SO (comentarios de código y className de Tailwind mal contados como "copy") — verificado a mano; no requieren corrección. También marca "cero hairline degradé" y "fondo plano" como críticos pese a que `Hero.tsx` sí trae el mesh radial-gradient de fondo y `Oferta.tsx` sí usa `<Hairline emphasis>` en el plan anual — confirmado leyendo el código; falso negativo del script (no reconoce estilos inline multilínea). Pendiente: no se corrió aún el subagente `revisor-visual` (rúbricas /40 y /20) ni `scripts/audit-diseno.sh` — eso es parte del cierre de Sesión 7 (pulido y rigor de entrega) según la secuencia maestra, no bloquea seguir a Sesión 4.
- Dominio real de la app aún no existe — el email de soporte usa `soporte@gymevo.app` como placeholder hasta que el usuario compre el dominio (Sesión 6, servicios externos).
- Onboarding/paywall/login (Sesión 4) son UI real y funcional pero sin backend: las respuestas viven en sessionStorage (no hay cuenta todavía, según el modelo onboarding-first), el botón de pago del paywall navega a /login en vez de abrir el checkout real de Hotmart, y el login no envía emails de verdad. Todo esto se conecta a Supabase Auth + Hotmart real en la Sesión 6 — está señalado con comentarios "Sesión 6" en el código (lib/onboarding.ts, app/paywall/page.tsx, app/login/page.tsx).
- Paywall construido como página única (no la secuencia de 3 pantallas que recomienda 50 §C0 para +37% de conversión) — decisión de alcance para esta sesión; se puede partir en 3 pasos más adelante si los datos lo justifican.
- App interna (Sesión 5) sin backend todavía: el progreso (racha, día, historial de pesos) vive en localStorage del navegador, se pierde si el usuario cambia de dispositivo o borra datos. Sesión 6 lo mueve a las tablas `workout_logs`/`user_progress` de Supabase.
- Las ilustraciones de ejercicios son solo texto (nombre + series/reps), sin dibujo biomecánico — el documento original de la idea pedía "ilustraciones anatómicas sencillas". Pendiente de diseño gráfico si se quiere esa capa visual; no es un placeholder deshonesto (no finge tener algo que no tiene), pero es una reducción de alcance visual a anotar.
- Corregido durante la Sesión 5: un bug real de condición de carrera si el usuario tocaba "Registrar" en dos ejercicios muy rápido (el segundo tap sobrescribía el primero) — se arregló usando actualización funcional de estado (setState con función, no valor directo). Verificado con clics simultáneos simulados.
- **veredicto:paywall** — ✅ **LISTA** (docs/revisiones/paywall-veredicto.md, 31/08/2026): Usabilidad 36/40, Craft 16/20, Copy 17/20 — pasa el gate doble justo en el límite. Recorrido: 30→34→36 usabilidad, 12→11→16 craft, a lo largo de 3 rondas de corrección real (checkmarks custom, garantía junto al CTA, whileTap en tarjetas, borde punteado, alineación de bullets, devIndicators apagado).
- **veredicto:landing** — ✅ **LISTA** (docs/revisiones/landing-veredicto.md, 01/09/2026): **Usabilidad 36/40, Craft 19/20, Copy 19/20** — pasa el gate triple. Recorrido completo de la sesión: 17→32→33→34→33→34→36/40 usabilidad, 5→15→13→13→14→13→19/20 craft, copy 19→19→20→19/20, a lo largo de 7 rondas de corrección real. Lo que la cruzó: (1) un BUG REAL de z-index (mismo patrón que `app/app/perfil/page.tsx`) tenía el video de fondo del Hero y la textura de "renglones de cuaderno" completamente invisibles desde que se agregaron — corregido con `z-index:0` + contenido en su propia capa `relative z-10`; (2) el scrim del Hero bajó de 85-92% a 62-80% de opacidad para que el video+líneas se vean de verdad sin perder legibilidad del texto; (3) `<BotonVolverArriba>` nuevo (`components/landing/ui.tsx`) para la heurística de control/libertad que faltaba en una página de ~8000px. Sumado a lo de rondas previas: 2ª nota de color (ámbar) en 3 lugares, dispositivo ownable (tachado real), checkmarks consistentes, carrusel con affordance de deslizar, titulares recortados a 1-3 palabras, conteo animado en precios, CTAs consistentes. Defectos menores NO bloqueantes que quedan para una futura pasada: el botón de volver arriba podría pisar el chevron de la última pregunta del FAQ en un caso puntual (subir el umbral de aparición), y no hay header persistente durante todo el scroll (el botón de volver arriba mitiga esto, un header sticky sería el óptimo). La paleta base sigue siendo la más cercana al límite del test anti-clon del banco canónico, pero el revisor confirmó que NO es un clon (difiere en tipografía) y el resto de la identidad (textura, dispositivo, 2ª nota) ya compensa lo suficiente para pasar. **Nota sobre "caducado" (falso positivo, mismo patrón que paywall):** el hook marca este veredicto como caducado cada vez que CUALQUIER `.tsx` del repo cambia (p. ej. la "Explicación del ejercicio" de `app/app/page.tsx`), no solo cuando cambia `app/page.tsx` (el archivo real de landing) — confirmado con `git log -- app/page.tsx`: sigue en el commit `5c2d266`, anterior al veredicto LISTA (`92a9954`). No hace falta re-lanzar el revisor mientras `app/page.tsx` mismo no cambie.
- **veredicto:onboarding** — NO LISTA tras 6 rondas de esta sesión (33→34→34→34→33/40 usabilidad, 14→12→11→14→15/20 craft). Mejora real de 32/10 (inicio de sesión) a 33-34/40 · 15/20 (cierre) — no llega al gate (≥36/40 y ≥16/20) pero por poco margen. Se corrigió la causa raíz estructural real (`justify-center` dejaba vacío en pasos cortos → ancla arriba + `TarjetaRuta` con progreso en vivo), y de paso se encontró y corrigió el MISMO bug de z-index que bloqueaba el fondo de la landing (el degradé y las líneas de renglón del onboarding también estaban invisibles todo este tiempo). Otras correcciones reales: navegación por teclado en los chips (con `prefers-reduced-motion`), celebración animada (trazo verde) en "Te entendemos", tarjeta con hairline superior en vez de borde completo (ya no se confunde con un chip tocable), padding simétrico, balance verde/ámbar del degradé, conteo animado en el número de días/semana. **Único defecto real pendiente de peso** (confirmado por el revisor como "requiere rediseño de contenido, no ajuste de código"): sigue quedando ~40-45% de pantalla vacía bajo la tarjeta en los pasos cortos (nivel/meta) — el fondo ya no se ve "roto" gracias a la textura, pero falta contenido real ahí. Sesión CERRADA aquí por decisión explícita de tiempo del usuario ("por tiempo, opción 2: no tocar la paleta").
- **veredicto:pantalla-principal** — NO LISTA. 5 rondas de revisión: 26→30→33→34→32/40 usabilidad, 11→11→14→16→15/20 craft — mejora real pero no monótona (la última ronda bajó levemente: cards con demasiados elementos a la vez, peso vacío se guarda como 0 en silencio, sin transición entre tabs del nav, paleta cerca del arquetipo "cuaderno" genérico). Se paró aquí por rendimientos decrecientes tras 5 iteraciones — los defectos restantes (simplificar la card de ejercicio, transición de tabs, 2ª nota de color) quedan para la próxima sesión de pulido.
- **veredicto:paywall** — el hook lo marcó "caducado" citando `app/app/page.tsx` como código más nuevo, pero ese archivo es de la pantalla PRINCIPAL, no del paywall — `app/paywall/page.tsx` (el código real de esta pantalla) no cambió desde el commit `099489a`, y el veredicto (`4373232`) es posterior a ese commit. Confirmado con `git log` y timestamps de archivo: el veredicto SÍ es más nuevo que el código que evalúa. Falso positivo del hook (compara contra cualquier .tsx del repo, no contra el archivo de la pantalla específica) — el veredicto LISTA de paywall sigue vigente. **Re-confirmado 01/09/2026** tras el commit `3b6c3e2` (que solo tocó `app/app/page.tsx`, la pantalla principal): `app/paywall/page.tsx` sigue sin cambiar desde `099489a`, mismo falso positivo, misma conclusión — nada que corregir en paywall. **Re-confirmado otra vez 01/09/2026** tras el commit `ed4b8cb` (cronómetro de descanso — solo tocó `app/app/page.tsx`, `lib/routine.ts`, `lib/supabase/sync.ts`): `app/paywall/page.tsx` SIGUE sin cambiar desde `099489a`. El hook seguirá disparando esta alerta cada vez que cualquier otro `.tsx` del repo cambie, porque compara contra "cualquier .tsx más nuevo", no contra el archivo real de la pantalla — no hace falta re-lanzar el revisor-visual de paywall hasta que `app/paywall/page.tsx` mismo cambie. **Re-confirmado una vez más 01/09/2026** tras el commit `9fea110` (sonido de descanso — mismos archivos de siempre, ninguno es paywall): sigue en `099489a`, sigue siendo falso positivo. **Y otra vez 01/09/2026** tras el commit `a80e94b` (confeti de cierre — tocó `app/app/page.tsx` y `components/landing/tokens.css`, tampoco paywall): sigue en `099489a`. **Y otra vez 01/09/2026** tras los cambios de onboarding/landing de esta ronda (`app/onboarding/page.tsx`, `components/landing/{AppPorDentro,Solucion,tokens}`): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 2 de correcciones de landing/onboarding (`app/page.tsx`, `components/landing/Oferta.tsx` incluidos): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 3 (commit `8d071c9`): sigue en `099489a`. **Y otra vez 01/09/2026** tras el fix del bug de z-index (commit `97e5b8c`): sigue en `099489a`. **Y otra vez 01/09/2026** tras la ronda 6 (commit `c9bce15`): sigue en `099489a`. **Y otra vez 01/09/2026** tras el botón volver arriba (commit `5c2d266`): sigue en `099489a`. **Y otra vez 01/09/2026** tras cerrar la sesión de pulido (commit `92a9954`) y tras "Explicación del ejercicio" (commit `7003430`): sigue en `099489a`. Dejo de repetir esta entrada de verdad — el hook seguirá avisando en cada commit ajeno, es ruido esperado, no se vuelve a anotar salvo que `app/paywall/page.tsx` mismo cambie.
- **RESUMEN Sesión 7 (31/08/2026)**: de las 4 pantallas del dinero, **paywall ya está LISTA** (36/40, 16/20, 17/20 copy). Las otras 3 mejoraron sustancialmente con correcciones reales (no cosméticas) pero landing y onboarding necesitan más trabajo de identidad/estructura que excede el alcance razonable de esta sesión — quedan documentadas con sus defectos exactos para retomar. No se declaran "listas" hasta que el veredicto lo diga explícitamente — autoevaluarse no cuenta como evidencia (Regla de Oro 7).
- **garantía / FICHA-MERCADO** — FICHA-MERCADO.md ya existe (creada 28/08/2026) con Prueba elegida: 7 días y Garantía elegida: 14 días (14>7, cumple la regla dura). PERO los plazos reales de garantía de Hotmart (desde cuándo cuenta, máximo configurable) siguen **sin verificar contra la pasarela real**, porque la cuenta de Hotmart todavía no existe (creación de cuenta es tarea del usuario en Sesión 6). El copy publicado (landing/paywall/FAQ) usa estos plazos como el PLAN a implementar, no como algo ya confirmado en el panel — antes de lanzar de verdad, hay que crear la cuenta, configurar el producto con estos plazos exactos, y volver a llenar los campos "NO ENCONTRADO" de FICHA-MERCADO.md con el dato real.

## Pendientes del usuario (acciones que el usuario debe hacer)
- [ ] Ninguna acción pendiente por ahora — sigo yo con la Sesión 4 (onboarding, paywall, login)

## Notas para la próxima sesión
- El usuario llegó con el "RESUMEN FINAL — IDEA VALIDADA PARA CONSTRUIR" completo (prompt de investigación externo). No se debe re-validar la idea ni proponer alternativas.
- Nombre FINAL confirmado por el usuario: **GymEvo** (28/08/2026) — "Método Cero" queda solo como referencia interna del documento original, no se usa en producto ni copy.
